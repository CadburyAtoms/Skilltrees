#!/usr/bin/env bash
# Git pre-commit hook — thin shim. All logic lives in scripts/pre-commit-body, so an edit to the
# body is live immediately; re-run `bash scripts/install-hooks.sh` only if this shim itself changes.
exec bash "$(git rev-parse --show-toplevel)/scripts/pre-commit-body" "$@"
