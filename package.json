{
  "name": "skilltrees",
  "version": "0.2.0",
  "private": true,
  "description": "Edha homebrew talent trees for the Cosmere RPG: the Foundry VTT module (edha-content) and the data pipeline that builds it.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check module-src/scripts/register-skills.js",
    "validate": "node scripts/validate.js",
    "lint": "node scripts/lint-refs.js",
    "test": "node scripts/gates.js --only unit-tests,audit-parser-test",
    "audit": "node scripts/gates.js --only tree-audit",
    "docs": "node scripts/build-dashboard.js --check && node scripts/build-canon-codex.js --check && node scripts/build-player-primer.js --check",
    "gates": "node scripts/gates.js",
    "gates:ci": "node scripts/gates.js --ci"
  }
}
