{
  "name": "skilltrees",
  "version": "0.2.0",
  "private": true,
  "description": "Edha homebrew talent trees for the Cosmere RPG: web atlas, Foundry VTT module (edha-content), and the data pipeline that builds both.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check module-src/scripts/register-skills.js",
    "validate": "node scripts/validate.js",
    "lint": "node scripts/lint-refs.js",
    "test": "node tests/run.js && python3 tests/audit_parser_test.py",
    "audit": "python3 .claude/skills/leyline-tree-authoring/audit.py",
    "gates": "npm run check && npm run validate && npm run lint && npm run test && npm run audit"
  }
}
