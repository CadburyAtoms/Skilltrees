# PM Board — the ongoing repo project (started 2026-09-04)

The scheduling state for the project that works `TODO_REPO_HYGIENE.md` items 4–25 (and whatever
gets added). **Substance lives in the TODO file; this board holds only order, ownership, status,
cost, and the decisions the PM is waiting on.** The operating procedure is
`.claude/skills/project-manager/` (PM) and `.claude/skills/work-item/` (workers). A fresh PM
session resumes from this file alone.

> **PM session of record: the 2026-09-07 17:10 ET hand-started Fable session (Ben, desktop, in his own hours — chat wins: *"pull main, run the PM skill … You're the new pm session"*).** Model `claude-fable-5-1`, guard passed; `git rev-parse --show-toplevel` = `C:/dev/Skilltrees`. The 09:52 session is **RETIRED for the day** (out of context window, Ben's word; told by session message at 17:10). Any `edha-pm-weeknight` session that fires at 21:00 tonight while this line stands is NOT the session of record: re-read this board and stand down unless the run log's last row is a handoff. **True state at 17:12 ET:** `main` = `a278186`, clean, no open PRs, no worktrees; Foundry up (302). **Ben ran `deploy-to-foundry.bat` at ~17:03** (all five pack dirs stamped 17:03, after item 89's 13:54 merge — Unbreakable Line's `use` rule is at the table), pressed F5 (item 86 live; served `a23e70d1…` = `main`, 6 in sync), and clicked ⟳ Sync Adversaries + Players — **nothing is owed to Ben's machine.** Phone inbox empty. **Ben's dashboard paste (17:11): R-83 (a), R-88 (a), R-89 (a)** answered; four ⚑ wizard-v2 rows PASS; the rest are done-ticks on rulings item 79 already recorded → items **91** (close-out, sonnet, DOCS-ONLY), **92** (R-88, sonnet, REBUILD) and **93** (R-89, sonnet M, adversaries REBUILD) filed on this PR and dispatched in worktrees the moment it merges; item **70** unblocked (Opus). **Opus 3 of 3 spent today (PM-R16) — nothing Opus until 00:00 ET; then 70 → 80 (two S engine-only items, worktrees), push live, and bench run 44 (Opus 3 of 3 tomorrow) for item 89's (b) row + 70's and 80's rows; 81 and 62 the night after.** Still WAITING on Ben: **R-56 go/no-go** (item 83 — the dashboard text is the same "let me know before changing"; the cost is ONE adversaries REBUILD + an engine F5 + docs/tests, Opus M ≈ 5–8M), **R-90** (item 88 — default (a) whisper adversary triggered-effect cards to the GMs when no player owns the actor), **R-91** (default (a) retire the R-62 audience row under R-86). **17:40 update:** board PR #287 merged 17:20; items 91 / 92 / 93 dispatched 17:20 (sonnet, worktrees); **item 92 merged 17:29 (#288, 1.7M) — a leyline REBUILD + ⟳ Sync Talents is now OWED** (item 93 will add the adversaries pack; one bat run covers both). The phone inbox delivered seven notes 17:23–17:25 (all `seen` 17:30): R-83 / R-88 / R-89 (a) again; **R-90 (a) → item 88 unblocked** (opus, after 00:00); **R-91 (a) → item 95** (sonnet, after 91 lands); the waiting-list "done"; and **R-56 tapped (a) = KEEP the Edha table for everyone (item 55 as shipped) — the OPPOSITE of the morning's "cosmere ladder for everyone" note. The PM asked Ben in chat which he means; item 83 stays blocked until he says, and (a) as tapped would close 83 as won't-do at zero cost.** Ben's phone-board complaint (his Sent ✓ marks vanish, the page jumps to the top) → **item 94** (sonnet M, TOOLING + PM republish), root-caused to `tick()` re-rendering the Needs-you cards every 30 s and every `pm/state` push re-rendering the whole page. This repo's harness has NO agent-messaging tool, so a running worker cannot be bounced or re-scoped — corrections are the PM's one-liners on the branch, or a reassign.
>
> _(The line this replaces, for the record:)_ PM session of record: NONE — the 2026-09-07 09:52 ET hand-started Fable session STOPPED its loop at 13:55 ET after the queue ran dry (13 PRs merged, #272 → #285); the next PM is the scheduled `edha-pm-weeknight` task at 21:00 ET tonight (MODEL GUARD first) or whatever Ben starts by hand.** Nothing is running; no PR is open; the live module is in sync with `main` (served `a23e70d1…`, item 86). **What the 21:00 session can take:** nothing Opus until 00:00 ET (PM-R16: 3 per calendar day, all three spent today — bench 42, item 84, bench 43); after midnight, items **80**, **81** and **62** (Opus S each, engine-only) in that order, and a bench run only when a fix has landed that needs one (the open 🤖 rows are 5, every one gated). Sonnet has nothing left that a ruling does not gate. **Waiting on Ben:** an F5 (item 86 live), his ⟳ Sync Talents click, ONE `deploy-to-foundry.bat` for item 89's adversaries REBUILD (+ ⟳ Sync Adversaries), and rulings R-83, R-56 go/no-go, R-88, R-89, R-90, R-91.
>
> _(The line this replaces, for the record:)_ PM session of record was the 2026-09-07 09:52 ET Fable session, hand-started by Ben in his own hours (chat wins — "Let's start the pull main and run the pm skill"). Model `claude-fable-5-1`; `git rev-parse --show-toplevel` = `C:/dev/Skilltrees`.
>
> **True state as of 2026-09-07 10:49 ET (this session's step 0):** `main` = `14f2aa9`, clean, no open PRs, no worktrees. **Ben ran `deploy-to-foundry.bat` this morning**: served engine `609c7e45…` = repo, 6 in sync, 0 stale — the whole REBUILD stack (34a, 57, 58, 59, 63, 55, 56, 65 + 67) is at the table for the first time; Foundry answers on :30000 with Ben's `Gamemaster` client connected. **Two rulings from Ben in chat:** PM-R8 is **REVOKED** (no more `fable-worker` dispatches — "we ate up most of my usage overnight") and **PM-R16** sets today's budget: **3 Opus dispatches per calendar day, Sonnet and Haiku unlimited.** Phone inbox: 2 `new` notes, both R-83's row forwarded from the phone with NO answer text (the "reply in inbox" button sends the row itself) — **R-83 is still WAITING**, and Ben's dashboard note on it says he wants examples (given in chat 10:1x, recorded in the ruling by item 79). Ben's dashboard paste (48 rulings rows, 12 bench ⚑ rows, 1 art row) → items 79–83, rulings R-86 / R-87, and 13 checklist retirements that ride bench run 42. **Dispatched 10:45: bench run 42 (opus, worktree, `pm/bench-42`) and item 79 (sonnet, worktree, `pm/79-rulings-closeout-0907`).** Opus today: 1 of 3. **12:10 update:** item 79 merged #273 (6.5M, two review corrections), bench run 42 merged #274 (16.9M; open 🤖 58 → 20, ⚑ 21 → 10; 38 rows retired on evidence; ONE defect → item 84, weapon-borne `edha-on-hit` rules are inert because `edhaRulesForEvent` still gates on `edhaIsTalent`; R-88 / R-89 filed WAITING; **Ben owes his own ⟳ Sync Talents click** — the bench synced only its 16 PCs). Dispatched 12:15: item 84 (opus, 2 of 3 today) + item 85 (sonnet). **Iron-rule-6 debt, mine:** the PM's commits on #272 and the #273 correction carry the harness's `Co-Authored-By` trailer; #274 was squash-merged trailer-free; no trailer from here on. **12:32 update:** item 84 merged #276 (1.8M) and **pushed live 12:23** (served `0a677dad…` = `main` 1c52cfd; **Ben pressed F5 at 12:25**); item 85 merged #277 (5.6M; R-56 now surfaces on the phone card); **bench run 43 dispatched 12:30 (Opus 3 of 3 today)** for the 20 remaining 🤖 rows including item 84's live re-test. Opus for today is spent; the count resets at midnight for the overnight shift. **13:25 update:** item 87 merged #279 (2.8M); **bench run 43 merged #280 (11.1M) — open 🤖 20 → 10 and every one of the ten is blocked on a fix, a ruling, or Ben, so the bench queue is DRAINED**; items 88 / 89 filed by the bench, R-90 / R-91 WAITING; item 86 dispatched 13:22 (sonnet, engine-only, worktree); item 90 (retire the five harness-only rows) dispatched after #280 landed. Opus 3 of 3 used; nothing Opus until 00:00 ET. **13:40 update:** item 90 merged #282 (2.4M; open 🤖 now 5 on the dashboard); item 86 merged #283 (3.3M; the PM merged `main` into its branch — changelog top conflict, both deltas kept, dashboard regenerated, gates green, CI re-run on the merge head) and **pushed live 13:40** (served `a23e70d1bad4…` = `main`); **Ben owes an F5**. Item 89 dispatched 13:35 (sonnet, DATA → REBUILD). Every other queued row needs Opus (80, 81, 62 — tomorrow) or a ruling (70 ← R-83, 88 ← R-90, 83 ← R-56, 82 ← 83, 9 ← map). **13:55 close:** item 89 merged #285 (4.1M; DATA → **REBUILD + ⟳ Sync Adversaries owed to Ben's next `deploy-to-foundry.bat`**). Queue dry; loop stopped. Day's tally: 13 PRs (#272 → #285), 2 bench runs (48 rows retired, open 🤖 58 → 5), 3 engine fixes live (84, 86; 85/87 tooling), items 79–90 all closed except 80/81/62 (Opus, tomorrow), 82/83/88/70/9 (rulings), R-86 → R-91 filed. PM's own cost this session 13.8M; workers 54.7M.
>
> _(History — the previous session-of-record line, kept for the record:)_ **PM session of record was NONE — the 2026-09-06 16:56 ET Fable session (Ben, desktop) STOPPED its loop at 05:58 ET Monday 2026-09-07 at the last wake before Ben's hours.** Nothing was running; no PR was open; the live module was in sync with `main` (`c12339b`).
> (model `claude-fable-5-1`, guard passed; `git rev-parse --show-toplevel` = `C:/dev/Skilltrees`). Any
> `edha-pm-daily` / `edha-pm-weeknight` session that wakes while this line stands is NOT the session of
> record: re-read this board and stand down unless the run log's last row is a handoff.
>
> **True state as of 2026-09-06 17:05 ET (this session's step 0):** `main` = `7e34961` (#209 on top of #210;
> #206 → #210 all merged; no open PRs, no worktrees, tree clean). Engine **6 in sync** (live = repo
> `57a8c950…`; every merge since the 03:47 deploy was DOCS-ONLY, so **nothing is owed to
> `deploy-to-foundry.bat` today**). **Foundry is CLOSED** (`localhost:30000` refused at 16:56) → lane R
> only until Ben opens it. **The phone inbox held 66 `new` notes** — Ben's answers to EVERY open ruling
> (R-3, R-6, R-8 → R-17, R-20 → R-33, R-35 → R-42, R-46, R-47, R-50 → R-57, R-70 → R-78, the GM-less
> traps, all 14 §I defaults accepted, TODO 41 OK'd, the Playtest Map scene licence, remote control by
> default, the TODO 43 shape, the stall post-mortem) — all marked `seen` at 17:05 with a one-line
> disposition each; the verbatim dump is `tmp/pm/inbox-2026-09-06/inbox/*.json` (gitignored; the worker
> source for items 45 and 46). Consequences are on this board: the rulings table, queue rows **43–62**, the
> run log. **Waiting on Ben now:**
> - every engine-only merge of the night is live (last push 23:50, `main` 9f3f529) — press F5 on the `Gamemaster` client (it still runs the 18:59 engine until reloaded — four pushes behind)
> - ONE `deploy-to-foundry.bat` run (Foundry closed) for the REBUILD stack — 34a #220, 57 #226, 58 #227, 65 + 67 #232, 59 #234, 63 #239, 55 #240, 56 #242 — and the bat copies the engine, so 69 #237, 68 #241 and 24 #244 go live with it; the PM push-notifies ONCE when bench 40 has reported, not before
> - answer **R-83** (three heal writers still bypass the No-Healing gate; default (a) = gate them at their emitters) — item 70 waits on it because it moves HP at the table
> - veto **PM-R15** if you want the engine SOURCES to be the only tracked copy — default: the assembled `register-skills.js` stays tracked and deployed, and item 4 is building it that way now
>
> _(Remote Control by default — PM-R12 — was already on before this session, via the Desktop toggle; Ben, 17:55.
> Whether scheduler-started sessions honour it is what Monday 21:10 will show. The list above is one bullet per
> ask on purpose: item 43's `parseBenOnly` turns each into a card with a Done button on the phone.)_
>
> _(History — the 07:02 session's failure, kept for the record:)_
> **What happened (read this before trusting anything below):** that session fired as **`claude-opus-5`,
> not Fable**, because nothing pins a scheduled task's model — the MCP tools expose no `model` field, so a
> task inherits the app's default at fire time, and the old "must remain Fable" note was an assumption with
> no mechanism. It then **froze twice**: 82.7 min dead from 07:02:34, and **7h04m dead from 09:01:18 to
> 16:05**, during which Ben's "stop" sat undelivered and he lost the working day, ran a cloud Fable session
> instead (PRs #208, #209), and merged **#207 himself at 12:11**. Both scheduled tasks now carry a **MODEL
> GUARD as step 0** (exit without spending if the model is not `claude-fable*`) and a rule to re-read
> `date` before every dispatch and budget calculation. Cost of the bad session: **~10.3M weighted (4.1M PM
> + 6.2M for one subagent), 159 turns.**
>
> **True state as of 2026-09-06 16:15:** `main` has #206, #207, #208, #209 merged. Engine **6 in sync**
> (live `57a8c950…`), Foundry was up at 08:30 — **re-probe it, that fact is 8 hours old.** Bench run 38
> retired 5 rows and disproved run 22's "structural and permanent" harness blocker: **fronting the browser
> pane flips `document.hidden` to false and observers run, so every row ever filed BLOCKED-on-rendering
> should be re-read.** New ruling **R-78** (`edha-aoe-template` has zero consumers; recommend retiring).
> New 🤖 defect: the weapon picker prints "a Agent" / "a Envoy". **The wizard block's remaining 4 rows are
> ruling-gated, not run-gated — R-41, R-42, R-54 are what unblock them.**
>
> **Relay correction, 2026-09-06 16:40 ET (cloud session, on Ben's word):** #209 was still OPEN at 16:15 and is
> merged with this note. And **every ruling in "Waiting on Ben" below was ANSWERED by Ben on 09-06 through the
> phone inbox** — R-70 → R-77, R-6 sharpened, the GM-less traps, TODO 41's OK, R-3, R-8, R-9 → R-17, R-20 →
> R-33, R-35 → R-42, R-46, R-47, R-50 → R-57, R-78, and all fourteen §I defaults accepted — 40+ notes from
> `phone-chat-relay`, every one still `new`. **Step 0 of the next PM session is to read them**; the queue and
> the waiting list in this line are stale until it does.
>
> ⚠️ **Debt on `main`:** #207's four commits carry `Co-Authored-By: Claude Opus 5` trailers (iron rule 6).
> They are merged; stripping them means rewriting `main`, so leave them unless Ben says otherwise.
> _(Superseded 17:05 — budget and waiting-on-Ben now live in the paragraph at the top of this block.)_

## Operating rules (the short form — the skill carries the long form)

- **One PM, and it is Fable.** The PM plans, writes briefs, dispatches, reviews, gates, merges,
  and keeps this board. **The PM does not write fixes itself** beyond one-line corrections found
  in review.
- **Workers are Sonnet or Opus by default.** Sonnet for mechanical, doc, config, and
  well-specified script work. Opus for anything that changes engine or build semantics, needs a
  byte-parity proof, or designs a test driver. The model is fixed per item on the queue below.
  **Weekend sprints (PM-R8, Ben 2026-09-05 13:44): Fable subagents are authorized, at MEDIUM
  effort only**, dispatched solely through the `fable-worker` agent definition
  (`.claude/agents/fable-worker.md`, mirrored at `~/.claude/agents/`), whose frontmatter pins
  `model: fable` / `effort: medium`. Never `Agent(model: "fable")` on a generic agent — that
  inherits the PM's effort. Use Fable where Opus was queued for size-L engine-semantics work;
  bench-run and test-pass-fixes stay Opus. A new agent type is visible only after a Claude Code
  restart, so the session that created the file (09-05) cannot use it; the next one can. **REVOKED 2026-09-07 09:52 (Ben, chat): *"I am revoking the fable sub-agent rule, as we ate up most of my usage overnight. A good use of tokens, but we need to budget the remaining usage for the rest of the week."* — no `fable-worker` dispatches from now on; every queued row that said `fable-worker` reads opus. Budget = PM-R16.**
- **One worker at a time by default.** Two in parallel only when both are docs-only and touch
  disjoint files. **Weekend sprints (PM-R8 "increased usage"):** up to TWO concurrent workers —
  the bench-loop worker in the main checkout plus ONE lane-R worker with `isolation: "worktree"`
  on files the bench does not touch (not the checklist, handoff, runbook, or next-run file; the
  generated dashboard may collide and the PM rebuilds it at merge). The bench loop itself stays
  strictly serial: one Bench slot.
- **Branch per item, PR per item, CI must be green, PM merges.** Workers never push to `main`.
  Branch name `pm/<item>-<slug>`. Commit messages follow iron rule 6 (engine-only vs rebuild
  stated; no model identifiers).
- **Workers do not:** edit `data/authored/*`, run `deploy-to-foundry.bat`, touch the live
  module, run the bench, or delete files outside their brief. Anything that needs Foundry is a
  **lane B** item and waits for a Foundry window.
- **Every merged item** gets: the TODO item checked `[x]` with the PR, a run-log row here, and a
  dated delta in the handoff (or the changelog, after item 19).
- **The PM runs on nights and weekends only** (Ben, 2026-09-05: *"run on nights and weekends, and
  save compute during the week for my actual work products like Caseware and audit workpaper
  templating"*). Operating windows, America/New_York: **Mon–Thu 21:00 → 07:00 next day**, and
  **Fri 21:00 → Mon 07:00** continuously. **Mon–Fri 07:00–21:00 is Ben's** — no PM session runs,
  nothing is dispatched (cloud lane included), no wakeups. The only PM activity allowed in Ben's
  hours is answering a chat message Ben himself sends. Two scheduled tasks start the sessions:
  `edha-pm-weeknight` (21:00 Mon–Fri) and `edha-pm-daily` (07:00 Sat and Sun); every session
  stops its loop before the next 07:00. **A task that fires late** (the app was closed at 21:00 and
  opened during Ben's hours) checks the clock first and exits without touching anything.
- **One PM session at a time.** Ben opens whichever session is current from his chat history; a
  new session verifies it is alone (the "session of record" line at the top of this file is how a
  session that cannot be messaged is told to stand down).
- **Stop and ask Ben** when: a ruling below is unanswered and the next item depends on it; a
  worker reports a behaviour change it did not expect; gates fail for a reason outside the brief;
  or the usage cap for the window is reached.

## How Ben and the PM talk (agreed 2026-09-04)

- **Chat is the control channel.** Anything Ben types wins over the loop: "pause", "stop",
  "skip 20", "do 19 next", "Foundry is up", "I got a usage warning". Rulings are asked in chat,
  batched, with the question UI.
- **This board is the status channel.** The PM re-reads it every wake, so Ben's edits to this
  file are picked up; marks on the generated dashboard are NOT (they live in the browser).
- **The inbox below is the async channel.** Ben writes free text; the PM clears it into rulings,
  queue changes, or notes on its next wake and says so in chat.
- **Push notifications only when the PM is blocked on Ben** — a ruling, the usage cap, a Foundry
  window, a worker failure it cannot resolve. A merge gets one line in chat, never a push.
- **Only Ben can:** open a Foundry window, run `scripts/deploy-to-foundry.bat`, answer a ruling.

## Inbox from Ben

_(Write anything here — a priority change, a question, "Foundry is up tonight 8–10". The PM
reads it every wake, acts, and clears it.)_

## Mobile board (added 2026-09-04)

**https://claude.ai/code/artifact/a24a597c-4516-425b-9eb2-a30f1ece03f0** — the phone view of this
file: PM state and any running worker (elapsed clock), the trailing-window budget meters and when
the next slot opens, weighted usage per run, the queue, what waits on Ben, the run log, and an
**inbox** Ben can type into from his phone (the PM reads it every wake, exactly like the section
above, and marks each note seen). It is a *projection*: `scripts/pm-state.js` reads this board and
the PM pushes the result into the artifact's `pm/state` document on every state change; the page
source is `docs/pm-board-mobile.html`. If the phone and this file disagree, this file is right.

## Lanes

| Lane | Meaning | When it can run |
|---|---|---|
| **R** | Repo-only. Provable by the gates, no Foundry needed. | Any time the PM session is awake |
| **B** | Needs a live Foundry table after merge (a 🤖 bench section). | Only in a Foundry window Ben has opened, via `bench-run` |
| **H** | Needs a human decision or action first. | After Ben answers the ruling |

## Budget model

Measured on 2026-09-04 (this session, weighted units: cache read ×0.1, cache write ×2, output ×5):

| Unit | Weighted cost | Notes |
|---|---:|---|
| The full fresh-eyes review (PM + 4 Opus survey agents) | ~7.0M | 53% of it was the four Opus agents |
| One Opus survey agent, 35–72 turns | 0.8–1.1M | read-only, no edits |
| PM thinking + writing, per turn | ~17k | 105 turns ≈ 1.7M |
| PM file/shell operations, whole review | ~1.1M | |

Dispatch estimates — **recalibrated after the first measured worker (2026-09-04, item 25):** a
Sonnet size-M worker cost **4.4M over 181 turns**, four times the first guess. A worker's cost is
turns × context: every turn re-reads its own growing context, so long explorations and whole-file
reads are what cost, not the model. The work-item skill now tells workers to read bounded ranges
only and finish in 40–80 turns; re-measure after the next two dispatches.

**Second calibration (2026-09-05, item 16):** two size-S Sonnet workers now differ by 2.4× — item 15
cost 1.3M over 73 turns, item 16 cost **3.1M over 171 turns**. The difference is not the edit (item
16's code change is ~30 lines) but the **proof**: a pack-parity proof means installing
`classic-level`, extracting the pre-fix script, and running two full five-pack builds. So **size is
the wrong predictor — the proof is.** When queueing, read the item's `verify:` line: `parity`
roughly doubles a dispatch. Items 17 and 18 both demand parity-class proofs; budget them as M.

| Dispatch | First guess | Measured / revised |
|---|---:|---:|
| Sonnet, size S (edit + gates + report) | 0.3–0.6M | **1.3M** (item 15, 73 turns) / **3.1M** (item 16, 171 turns) |
| Sonnet, size M | 0.6–1.2M | **4.4M measured** (item 25, 181 turns) |
| Opus, size S | 0.8–1.5M | ~2–4M (revised) |
| Opus, size M | 1.5–3M | ~5–8M (revised) |
| Opus, size L (two PRs, parity proofs) | 3–5M | ~10M+ (revised; split the item instead) |
| PM review + merge per item | 0.2–0.4M | ~0.4M (item 25 review) |

**Caps (CONFIRMED by Ben 2026-09-04, Max 20x — ruling PM-R6; windows re-cut 2026-09-05 — PM-R7):**
at most 2 dispatches per 5-hour window, at most 1 of them Opus (trailing window); the PM wakes on worker
completion, not on a timer, with a 30-minute fallback; hard stop the moment Ben reports a usage
warning. **The old quiet hours (23:00–07:00) are gone — nights are now PM time.** In their place:

**Windows (machine-readable, PM-R7 — parsed by `scripts/pm-state.js`; edit this line, not the prose
above, to change the schedule):** Mon-Thu 21:00-07:00; Fri 21:00-Mon 07:00 America/New_York

- **Windows:** dispatch only inside the operating windows above (weeknights 21:00–07:00, the whole
  weekend Fri 21:00 → Mon 07:00). Cloud routines obey the same windows.
- **PM-R16 (Ben, chat 2026-09-07 ~10:15) is the binding cap from today: 3 Opus dispatches per calendar day, Sonnet and Haiku unlimited, no Fable workers (PM-R8 revoked 09:52).** The numbers below are kept for the record and still logged; they no longer bind. The Bench slot stays serial; a usage warning is still a hard stop.
- **Per-shift ceilings (PM-R7, amended by Ben 2026-09-05 10:40 — "bump up the threshold for # of
  agents you can send in a period on the weekend"; numbers are the PM's default, Ben may set
  others):** a weeknight shift (21:00→07:00) dispatches **at most 2** under the normal 2-per-5h cap.
  **On the weekend (Fri 21:00 → Mon 07:00) the trailing-5h cap is 6 dispatches of any model,**
  and a weekend day-shift (07:00→07:00) dispatches **at most 12** (raised from 4 / 8 at 13:50 on
  Ben's "increased usage for weekend sprints", PM-R8; the numbers are the PM's default). (The ≤2-Opus sub-cap written at
  10:45 was dropped at 11:55: every bench-loop worker is Opus by the skills' own design, so the
  sub-cap would have idled the table for four hours after two dispatches — the exact stall Ben
  objected to. Serialization, not model mix, is what bounds the weekend's spend.) The bench loop is
  sequential by nature (one Bench slot, one repo), so the weekend cap exists to keep the
  bench → fix → deploy → re-bench chain moving, not to run workers in parallel. Weekly maximum 24.
- **Monday 07:00 handoff line reports the week's PM total** from `python scripts/pm-usage.py` so
  Ben can compare it with his weekly meter and re-cut the ceilings.

## Rulings

Answered by Ben on 2026-09-04 (all six, as recommended). Kept here so a worker can quote them.

| Id | Question | Ruling |
|---|---|---|
| **PM-R1** | Handoff split shape | **Yes.** Reference stays at `EDHA_FOUNDRY_HANDOFF.md` (≤800 lines, with TOC); dated deltas move verbatim to `docs/handoff-changelog/2026-MM.md`; `HANDOFF_ARCHIVE.md` folds in. |
| **PM-R2** | Archive moves | **Yes.** `EDHA_EDITABILITY_AUDIT.md` and `Actor pages design review/` → `docs/archive/` with pointer stubs; prune the four clean stale worktrees under `.claude/worktrees/`. |
| **PM-R3** | Radiant-order rows | **Park them** in `source-materials/` if no consumer reads them (the worker checks the primer first). |
| **PM-R4** | Branch policy | **PR per item; the PM merges after green CI.** Workers never push to `main`. |
| **PM-R5** | Cloud lane | **Yes, docs-only items**, one nightly Sonnet routine, PR output, PM reviews next wake. |
| **PM-R6** | Usage tier and caps | **Max 20x; the caps above stand.** |
| **PM-R7** | Nights-and-weekends schedule (Ben's instruction 2026-09-05, amended 10:40: raise the weekend threshold) | **Instruction recorded; the exact cut is a PM default awaiting veto:** windows Mon–Thu 21:00→07:00 and Fri 21:00→Mon 07:00; weekday daytime is Ben's; weeknight ceiling 2 under the 2-per-5h cap; **weekend: 4 per trailing 5h of any model, 8 per day-shift** (Opus sub-cap dropped 11:55 — the bench loop is all-Opus and serial). Ben: say a different hour or number and it changes. |

| **PM-R8** | Weekend sprints: Fable subagents + increased usage (Ben, 2026-09-05 13:44: "I'm authorizing Fable agents and increased usage for weekend sprints. Fable subagents are limited in effort to medium.") | **Recorded.** Fable workers only via `fable-worker` (model fable, effort medium), weekend windows only; weekend caps 6 per trailing 5h / 12 per day-shift; one parallel lane-R worktree worker allowed beside the bench loop. Numbers are PM defaults — Ben may set others. |
| **R-69** (in `EDHA_RULINGS.md`) | Should a CANCELLED picker still burn the talent's once-per-scene use? Found by bench run 25: Final Decree → Cancel refunds the Investiture but leaves `sceneOnce` stamped, so the scene's only use is spent without resolving. | **Answered 2026-09-05 16:30 (chat): stamp only after a successful pick.** Recorded inline in `EDHA_RULINGS.md`; filed as **TODO #36** (lane B, opus, S — fold into the next fix pass). |
| **PM-R9** | Item 32's optional line-ending piece: add `.gitattributes` (`* text=auto eol=lf`, `*.bat text eol=crlf`) in the same PR and `core.autocrlf=false` on Ben's fresh clone? | **Yes (Ben, 2026-09-05 16:30, chat).** In the item 32 PR; the worker renormalises only if ≤ 10 files change, else reports. |
| **R-77** (bench run 36, PR #201) | `edhaDeriveInvestiture`'s persist of `inv.max.override` gates on `actor.isOwner` + a per-client Set, so with two GM clients whichever prepares the actor first writes — the two-GM family item 12 just closed. Owner gate, primary-GM gate, or both? | **ANSWERED 2026-09-06 (Ben, phone): (a) keep the applied default** — owner gate for a non-GM owner, primary-GM gate for the GM case. → §K once the live re-test passes after Ben reloads his `Gamemaster` client (run 37's blocker was the stale client, not the code). Recorded by item 45. |
| **R-76** (bench run 35, PR #198) | No shipped talent carries `edha-focus` `op: "drain"` + `resource: "inv"` (Reaper's Harvest is a GAIN), so item 13's one spend-stamped write has no consumer at the table; driven with a staged rule. Author one, or leave the stamp as latent? | **ANSWERED 2026-09-06 (Ben, phone): (b) leave the branch, note it in the header** — plus Ben's design note, verbatim: *"make a note this is good juice for a future adversary stat block"* → a DESIGN SEED in the bestiary backlog, cross-referenced from the H10 header (item 45); with R-72 (b) the stamp becomes bookkeeping (item 47). |
| **Region traps on a GM-less table** (item 12, PR #197) | Three `RegionBehavior` bodies (Civ fortified foundation, dangerous terrain, Fate snare) deliberately check only "no OTHER active GM" without `isGM`, so a trap springs on the walking player's own client when no GM is connected. Keep that? | **ANSWERED 2026-09-06 (Ben, phone): (a) KEEP** — the three bodies keep springing on the walking player's own client when no GM is connected. Gets an R-number + §K entry, 28a's ungated list, and the `ENGINE_INDEX` "two helpers" note (item 45). |
| **R-73** (fix pass 5, PR #192) | A DISPEL cannot remove a passive that lives on a talent or a trait (the `edha-pick` `source: "effects"` menu reads actor-level effects only, on purpose) — should it stay that way? | **ANSWERED 2026-09-06 (Ben, phone): (b) VETO the narrow default — widen it the safe way**: `edha-pick` `source: "effects"` lists item-owned transferred effects too, offered as a temporary DISABLE (never delete); the delete path stays actor-level only. Plus R-35 (a): the target's Omen ledger entries as a "dispel Omen" button. → item 54. |
| **R-74** (bench run 34, PR #195) | No adversary ability in shipped data pays an engine-driven cost (`data/adversaries.json` has zero `costs` keys across 52 blocks), so 28b's "adversary's own bespoke cost" row has no subject. Author one? | **ANSWERED 2026-09-06 (Ben, phone): (a) author one `costs:` line onto a single adversary ability** — Ben did not pick the subject; default the Stalker's Fade. → item 57 (REBUILD). |
| **R-75** (bench run 34, PR #195) | The `edha-test-react` (H26) reaction family is not combat-gated — Shared Conviction offered on a test while its watcher was in no combat. Not a 28a regression (no `scope` field, never reaches the gate). Right? | **ANSWERED 2026-09-06 (Ben, phone): (a) leave it ungated and document it** — the H26 family (Shared Conviction, Pillar of Order, Voice of Authority) joins 28a's deliberately-ungated list in the handoff and `ENGINE_INDEX` (item 45). No engine change. |
| **R-72** (raised by item 28b, PR #189) | Is an INVOLUNTARY drain a "spend"? H10's Investiture drain is stamped as a spend so today's Order-Edict behaviour is preserved byte-for-byte, but an Edict forbidding you to *activate* Investiture arguably should not fire when a talent rips it out of you. Adjacent to R-8. | **ANSWERED 2026-09-06 (Ben, phone): (b) NO — a drain is not a spend.** The Order Edict fires only on the creature's own activations: H10's Investiture-drain write and `edhaDrainFocus` both carry `edhaBookkeepingTag`; 28b's "a test fails if one ever appears" pin flips to its opposite. R-8 (a: bench artifact, close it) settles the "decide together" clause. → item 47; R-72's own `EDHA_RULINGS.md` entry → item 45. |
| **R-6, sharpened** (bench run 33) | The dangerous-terrain Region Fault Line drops catches the CASTER too (the rectangle starts at the caster's own square: 8 and 10 energy to `Bench — Destruction` on both casts) and the ally in the line takes its own tick on top of the burst. R-5's "only the caster is spared" does not carry over to the Region: right now nobody is spared. | **ANSWERED 2026-09-06 (Ben, phone): (b) spare the CASTER only; everyone else including allies is caught** — matches R-5 / item 29 for the line, so both halves of the talent follow one rule. Lay the rectangle one square out or exempt the caster's token from the tick — the worker picks the one that keeps the footprint honest on the map. → item 48; 🤖 re-test of the run-33 Fault Line row with the caster unharmed. |
| **R-70 / R-71** (in `EDHA_RULINGS.md`, raised by bench run 28) | R-70: a two-resource activation charges only the FIRST resource unless the player ticks the second — the cosmere system's own dialog default (`shouldConsume ?? i === 0`), not an engine fault. R-71: the system's own item-damage card prints the UNFOLDED formula (maths correct). | **ANSWERED 2026-09-06 (Ben, phone): R-70 (b) wrap the dialog so every cost row starts ticked** — ONE wrapper on the system's `showConsumeDialog` passing `shouldConsume: true`, declared in the header as the one sanctioned system-dialog wrapper (an iron-rule-2a exception by Ben's ruling) → item 50. **R-71 (a) fold `system.damage.formula` at BUILD time** → item 59 (REBUILD). |
| **PM-R11** | Overnight 2026-09-05 → 06 | **Ben, 21:30 (chat): "you have my permission to continue overnight with no cap. stop at 7:00am eastern time."** — the day-shift ceiling and the trailing-5h cap are lifted for this night only; the Bench slot stays serial and one lane-R worktree worker runs beside it (checkout safety, not budget); hard stop 07:00 ET. |
| **PM-R10** | Tonight's weekend day-shift ceiling (12 would leave one slot after bench run 4 + item 32) | **Raised to 16 for the 2026-09-05 day-shift (Ben, 16:30, chat).** Trailing-5h cap of 6 unchanged; the 12 default stands for other days unless Ben says otherwise. Also ruled: **close PRs #93 and #103 now** (items 33/34 re-do them; branches stay KEEP) — done 16:35. |

| **2026-09-06 phone batch** | Ben answered **every open ruling** through the phone inbox on the morning of 2026-09-06 (66 relay notes, all `seen` 17:05): R-3, R-6, R-8, R-9 → R-17, R-20 → R-33, R-35 → R-42, R-46, R-47, R-50 → R-57, R-70 → R-78, the GM-less traps, and all fourteen §I defaults accepted (R-43's live dice math confirmed). | **Recorded in `EDHA_RULINGS.md` by item 45; every engine/data consequence is a queue row 47–62.** The rows above keep only the board-level rulings and the ones workers most often quote; the rest are read from `EDHA_RULINGS.md` once 45 lands. |
| **PM-R12** | Ben, phone 2026-09-06 ~08:55: *"I'd like the scheduled pm to be a remote control session by default on startup."* | **Standing rule: every PM session runs with Remote Control on** so Ben can see and message it from his phone. Lever: `"remoteControlAtStartup": true` in `%USERPROFILE%\.claude\settings.json` (the USER file; a project-level copy is ignored by design) or Desktop Settings › Claude Code › "Enable remote control by default"; manual start = `claude --rc` from `C:\dev\Skilltrees`. **Correction (Ben, chat, 17:55): the Desktop toggle was ALREADY ON before this session — nothing was applied today.** The PM's 17:00 attempt to add the key to the user `settings.json` was blocked by the permission classifier, and that file carries no such key; the toggle in force is the Desktop app's own setting. **So the Sunday 07:02 scheduled-task session ran with the toggle on and still did not show on Ben's phone** — the open question is whether scheduler-started sessions honour the toggle at all, not whether it is set. Acceptance check unchanged: the Mon 21:00 weeknight session shows in the phone app by ~21:10; if not, the tasks cannot give Ben a phone view and the fallback is `claude --rc` from `C:\dev\Skilltrees` by hand (record the outcome here). |
| **PM-R13** | Ben, phone 2026-09-06 ~11:20: *"Feel free to remove that combat — the entire scene is for your use at this point."* | **The Playtest Map scene is the bench's.** The zero-combatant combat `BerbNeuXp4iKduef` may be deleted by the next bench run (record it in the run's world diff as authorised). The two PC actor documents (Tem parinaem, Soggy Bottom) keep their hard guard — only their tokens ON that scene fall under the licence. Wording change in `bench-run` hard rule 4 + the runbook = item 45. |
| **TODO 41's OK** | Ben, phone 2026-09-06 ~08:55: *"You have my ok for todo 41."* | **Granted** — the one docId rewrite in `data/authored/deity-knowledge.json`, folded into item 58 (parity proof). |
| **PM-R14** | Ben, chat 2026-09-06 19:08, verbatim: *"I'll provide permission for you to keep issuing agents over the budget. The budget assumed usage during the day that didn't occur due to a bug. I also noted that no fable agents have been issued even with permission. I'd advise using a few on larger items."* | **The day-shift ceiling and the trailing-5h cap are LIFTED for the rest of this shift (to Mon 07:00 ET).** What still bounds concurrency is checkout safety and file conflicts, not budget: every worker in its own worktree, ONE Bench slot, and the second PR to land merges `main` first. **Fable workers (`fable-worker`, effort medium) go on the larger engine-semantics items** — 34a and 53 dispatched 19:15; 24 / 4 / 19a are Fable-class too but conflict with everything in flight (the registry rewrites every handler registration; 19a rewrites the handoff every delta lands in), so they wait for a quiet checkout. |
| **PM-R15** | Item 4 asks: "the concatenated output stays tracked OR becomes a build product — decide with Ben" | **Applied as a default, vetoable: the assembled `module-src/scripts/register-skills.js` STAYS tracked and deployed.** The per-section sources under `module-src/scripts/engine/` are the edit surface; `engine-assemble.js --check` is a gate proving the tracked file equals their concatenation. Ben's F5 reload, `module-src-sync.js`, the test harness, `lint-refs.js` and every "grep the engine" instruction keep working unchanged — the TODO entry itself calls this the safer shape. Veto = the sources become the only tracked copy and the deploy script assembles. |
| **PM-R16** | Ben, chat 2026-09-07 ~10:15, verbatim: *"Budget number: I don't have a hard number for you. Let's say 3 Opus agents and unlimited Sonnet and Haiku agents per day."* (after revoking PM-R8 at 09:52) | **The binding cap from 2026-09-07: 3 Opus dispatches per calendar day (America/New_York), Sonnet and Haiku unlimited, no Fable workers.** The trailing-5h and per-shift numbers above are still logged but no longer bind; the Bench slot stays serial; a usage warning from Ben is still a hard stop. Counted in the run log per day. |
| **2026-09-07 dashboard batch** | Ben went through `EDHA_DASHBOARD.html` on the morning of 09-07 and pasted the Copy-for-Claude block into chat: 48 rulings rows (all "done"), 12 bench ⚑ rows (2 PASS, 9 SKIP, 1 PARTIAL), 1 art row (Mistheron DONE), with notes on 2bM-1, the Civ enemy-cost row, the dissipates re-test, the sight-range row, R-77, R-18, R-83, R-48, R-52, R-56 and F-1. | **Recorded by item 79** (rulings inline, verbatim; R-86 GM-less / two-GM rows not needed; R-87 the wizard's numbers stand; R-56 REOPENED with the PM's scope, WAITING; R-18 → item 80; R-82 (a) → item 81; the bestiary-stats note → item 82; R-56's reversal → item 83; R-83 still WAITING with the plain-language gloss). **The 13 checklist retirements ride bench run 42** (the bench owns the checklist this shift). |
| **R-88** (bench run 42, PR #274) | Volatile Strike shipped R-23's `whenDealer: "any"` (item 58) but still carries `whenDamageType: "impact"`, which the card never mentions — a keen sword hit offers nothing while the card promises any melee hit. | **ANSWERED (a) — Ben, dashboard 2026-09-07 17:11: "a"** → **item 92** (sonnet S, REBUILD + ⟳ Sync Talents). _(Was:)_ recommended default: (a) drop the `whenDamageType` gate (one authored field, `data/authored/leyline-red.json`, REBUILD); (b) keep it and write "for impact damage" on the card (also data-only). Nothing broken; the two halves disagree. |
| **R-89** (bench run 42, PR #274) | The `<!-- NO NAMEABLE HOOK: … -->` marker in an adversary description does not survive a ProseMirror save (measured through the editor's own parse/serialize pair), so the first Foundry-edit → extract of such an ability silently drops it and lint pass 5 fails on an ability that never changed. | **ANSWERED (a) — Ben, dashboard 2026-09-07 17:11: "a"** → **item 93** (sonnet M, adversaries REBUILD + ⟳ Sync Adversaries). _(Was:)_ recommended default: (a) move the declaration into `flags.edha-content.noHook` (data, not prose), read by lint pass 5, rendered nowhere — TOOLING + DATA (adversaries REBUILD); (b) a note in `AUTHORING_WORKFLOW.md` not to save those descriptions from the editor; (c) the extract step re-attaches the marker from the repo copy. |
| **R-90** (bench run 43, PR #280) | An adversary's `edha-triggered-effect` card posts publicly (`edhaRollCard` passes no `whisper`), so the table sees the boss's remaining-HP arithmetic and the GM instruction inside Predator's Due's card. Whisper it to the GMs? | **ANSWERED (a) — Ben, phone inbox 2026-09-07 17:25 (the row's (a) text tapped)** → **item 88** unblocked (opus S, ENGINE-ONLY, after 00:00 ET); recorded in `EDHA_RULINGS.md` by item 95. _(Was:)_ recommended default: (a) whisper to `edhaWhisperIds(owner)` when the owner has no player OWNER, leave a player-owned actor's card public (one helper call per poster, what `edhaPostCueCard` already does) → item 88; (b) whisper only when the note carries a GM instruction; (c) leave public and delete the instruction from the three card texts. |
| **R-91** (bench run 43, PR #280) | The "R-62 audience flips, seven sites" row needs a GM logged OUT when a card fires; the world has exactly two GM users (`Bench`, `Gamemaster`) and both are connected during every run, so it cannot be staged. | **ANSWERED (a) — Ben, phone inbox 2026-09-07 17:25 (the row's (a) text tapped)** → the R-62 audience row is retired under R-86 by **item 95** (sonnet S, DOCS-ONLY, after item 91 lands). _(Was:)_ recommended default: (a) retire the row under R-86 (the flips are pinned in code; the state it tests is one Ben says never occurs); (b) disconnect `Gamemaster` for one ten-minute window so a run drives all seven; (c) keep it open. |
| **R-84** (bench run 40, PR #245 — filed by the bench as "R-82", renumbered) | Unnerving Approach's `emptyNote` branch (no living ally within 10 ft of the target) posts its card, but the SYSTEM has already taken the Investiture (measured 2 → 1) — no refund, no Decline button, because there is no offer to decline. R-17 refunds a declined or ignored offer; it says nothing about an offer that never existed. From the player's side the three cases look identical. | **(waiting, default APPLIED by fix pass 9: (a) refund it)** through the same gate R-17 already computes (`edhaOfferRefundable` → the `edhaRefundCost` path), and the card says the cost was refunded; when not refundable the card says it was spent. (b) keep charging — "you spent the Investiture looking" — is a defensible table rule, but then the card must SAY so. Either way the card stops being silent about the money. Full text: `EDHA_RULINGS.md` R-84. |
| **R-85** (bench run 40, PR #245 — filed by the bench as "R-83", renumbered) | `edha-next-test-mod`'s `expireEndOfRound` stamps `edhaCombatRoundOf(owner)` — the GRANTER's combat. A granter who is not a combatant writes `round: null`, and a null stamp never expires: a "this round" rider granted from outside the tracker sits on the victim for ever (reproduced both ways at the bench; the row passes once both are in the tracker). | **(waiting, default APPLIED by fix pass 9: (a) fall back to the BEARER's combat)** — `edhaCombatRoundOf(owner) ?? edhaCombatRoundOf(target)`, so "this round" always means the round the victim is living in. (b) leave it — out of combat there is no round and an inert stamp is honest; the cost is a mid-combat grant from a non-combatant NPC that never expires. Full text: `EDHA_RULINGS.md` R-85. |
| **R-80** (item 49, PR #221) | With the next-test slot now a LIST, a victim can carry BOTH an advantage entry and a disadvantage entry for the same test — impossible before (one slot). Item 49 folds them by boolean-OR per direction and, when both are present, writes NOTHING: the roll stays exactly as the player configured it (the standard table rule that they cancel; never stomps a manual dialog choice). Ben may prefer disadvantage to win. | **(waiting, default APPLIED in #221: (a) they cancel)** — (b) disadvantage wins is one line in `edhaNextModFoldMode`, pinned, so a veto is a one-line diff. Item 45's worker should give it a numbered entry in `EDHA_RULINGS.md` §C at the next docs pass. |
| **R-81** (item 57, PR #226) | R-46 / R-48 fixed two charge distances by replacing `bySize` with an explicit `distanceFt`. Item 57's worker found the same shape on three more run-19 blocks — the Brandram's Shockwave Slam (`bySize: true` beside a dead `distanceFt: 5`) and Reckless Advance, and the Tussock-Sow's terrain square — all `bySize` at rank 2 while their cards print rank-3 numbers. Sweep them the same way? | **(waiting)** — recommended default: (a) yes, the R-46 treatment on all three (explicit `distanceFt` = the card's own number, stated on the card), in one rebuild → **item 67**; (b) fix the three cards to the rank-2 numbers instead. Item 67 applies (a) unless Ben says (b) before it is dispatched. |
| **R-82** (item 56, PR #242) | R-14 (c) now governs the Life mutation riders (Bone Spurs, Venom Glands, Apex Form) through per-rule graze dials. Item 56's worker found that the generic `edha-damage-bonus` rules with `meleeOnly` (Warlord's Advance and kin — armed-strike bonuses) ALSO fire on a graze application today. Should R-14's "follow the card" rule reach them too? | **(waiting)** — recommended default: (a) yes, the same per-rule `onGraze` dial on `edha-damage-bonus`, each card audited (the `graze` value is already available at that call site); a small `fable-worker` S item once Ben nods. (b) leave them — a bonus "on your attacks" reads as any application. |
| **R-83** (fix pass 8, PR #241) | `ENGINE_INDEX` says every `hea` write outside `applyDamage` must pass the heal-cut gate, and three do NOT: `edha-regen`'s turn-end write, the decay lifesteal heal-back, and `edhaBurstDetonate`'s heal hits. Their cards are honest (item 68 fixed the announcing), but the HP still lands on a withered creature — Mending Aura keeps healing a target that "cannot regain HP". Gating them changes live HP at the table; `edhaApplyBurstResults` must STAY ungated (Raise Dead's stabilising 1 HP rides it, R-10), so the gate belongs in the emitter. | **ANSWERED (a) — Ben, dashboard 2026-09-07 17:11: "a"** → **item 70** unblocked (opus S, ENGINE-ONLY; the first Opus slot after 00:00 ET under PM-R16). _(Was:)_ recommended default: (a) gate all three at the emitter (the mark's card is the promise), the family test's gate-call count raised from 2 with a declaration, 🤖 rows per writer → **item 70**; (b) leave them ungated and say so in `ENGINE_INDEX`. Item 70 waits for Ben's answer — it moves HP. |
| **R-18** (`EDHA_RULINGS.md`, Heroic Quarry row) | Should quarry advantage refuse to stomp an active DISADVANTAGE? Today the quarry site runs after Weakened's and overwrites it — the house convention (pack advantage, the Opportunity adv-test, `edha-next-test-mod` all stomp; only `edha-test-rider` has the opt-in `unlessDisadvantage`). **Not in the 09-06 phone batch.** | **(waiting)** — recommended default: (a) keep the house convention (stomp), no change; (b) would add `unlessDisadvantage` to the quarry site — a one-line engine change if Ben wants it. |
| **R-48** (`EDHA_RULINGS.md`, bestiary) | The Cragdrake Adult's Explosive Leap says "up to 20 ft" and moves 5 (`bySize` at rival rank 2). Same shape as R-46, which Ben answered "full speed, explicit `distanceFt`, stated on the card". **Not in the 09-06 phone batch** (R-46's note said "check R-48 for the same treatment if still open"). | **ANSWERED (a) — Ben, phone 2026-09-06 21:14, verbatim: *"the CARD is canon for an adversary — a statted block should not scale, so give it `distanceFt: 20` and drop `bySize`."*** Item 57 (#226) had already shipped exactly that (`{bySize: false, distanceFt: 20}` on the Cragdrake Adult's Explosive Leap; REBUILD owed to the deploy) — no further change. The same principle ("a statted block should not scale") is what R-81's applied default (item 67) rests on. |

New rulings go in this table with a `(waiting)` mark; the PM asks Ben in one batch, not one at a time.

**PM-D1 — deploy class is what Ben must DO, not what an item feared** (PM's call, 2026-09-05,
item 17; not a Ben ruling — recorded so workers stop asking). Item 17 expected REBUILD; the
measured build-report diff showed every pack byte-identical on all four machine/scenario
combinations, so it shipped **TOOLING-only** and the worker asked whether to label it REBUILD "on
principle". The label answers one question — *does Ben have to rebuild packs and ⟳ Sync?* — and
the answer was no. Label from the measurement, never from the item's prediction; if the two
disagree, that disagreement is a finding and belongs in the delta (it did). A latent behaviour
that *could* change content later is a note, not a deploy class.

## Queue (in order)

Status: `queued` · `briefed` · `running` · `in-review` · `merged` · `blocked(<ruling>)` · `bench-pending`

| # | Item | Lane | Model | Size | Deps | Status | PR |
|---:|---|:-:|:-:|:-:|---|---|---|
| 1 | 25 PM tooling (script + dashboard tab; skills landed in #131) | R | sonnet | M | — | merged | #132 |
| 2 | 15 Pre-commit shim + reinstall | R | sonnet | S | — | merged | #133 |
| 3 | 16 Build fails loudly on a broken overlay | R | sonnet | S | — | merged | #136 |
| 4 | 17 Heroic ids into `data/` | R | sonnet | S | — | merged | #137 |
| 5 | **Weekend bench marathon** — runs 24/25/26 done (32 rows retired, fix passes 1–2 merged; per-tree bench block exhausted). #151 deployed by Ben 16:15 (hash-verified 16:20: 6 in sync). **Run 27 (16:23–17:02): 14 PASS retired, 0 FAIL, 1 BLOCKED (veil needs a dark scene), 1 new 🤖 (two-`consume` activation charged only Investiture)** → merged #157. Run 21's Braced-(b) blocker disproved (`game.combat` is the VIEWED combat). **Next: bench run 5** — the 46 open 🤖 rows (hygiene remainder 19, creation wizard 9, bestiaries 15, player-client 4, pack-sync 4, items 3) after item 36 is deployed. **Run 28 (19:38–20:16) merged #163: 12 rows off** (R-69/#36 green on all four legs, the two-`consume` question settled as R-70, all three 07-28m re-tests, six hygiene rows; the unfolded-formula row relocated to R-71; three bestiary `Unstoppable` rows unblocked to their canvas half). **Run 29 (21:36–22:05) merged #168: 5 rows off, 0 engine defects** — the three `Unstoppable` canvas halves proven (run 28's wall-clipping diagnosis RETRACTED: the hidden Browser pane never advances v13's animated token-move pipeline; `animate:false` moves to the exact pixel — harness, not engine), Set Charge's heal half found unreachable by design (the heal fold lives in `edhaBurstDetonate`, proven via Mending Aura), Venom Glands driven, R-64's two halves settled from data (no drivable shape; do not re-queue). **Run 30 (22:33–23:06) merged #176: 3 rows off, 1 FAIL** — the player-client window finally opened (three clients live): item 37's orphan repair and item 26's 20 ft sight both PASSED from the player client, Magnum Opus folded exactly, Job 6b 1 of 3 shapes proven, Pack Share's subject corrected to Death Mark; **FAIL: Walking Ruin's trail never drops for a player-initiated move** (root cause proven by matched control → **fix pass 3**, running); R-62 / Job 6a / 2bM-1 still BLOCKED on zero GM clients — ask Ben. **Run 31 (23:31–23:52) merged #180: 3 rows off, 1 FAIL** — fix pass 3 PASSED from a player client (3 Regions + 3 Drawings; two GM clients did not double-drop), Job 6b closed from a genuine non-owner, the stale token sight fixed by one write, R-63 half-proven; **FAIL: `Unravel Everything` never detonates the Omens it places in the same activation** (matched pair; root cause hypothesised at the `use`-event dispatcher → **fix pass 4**, running). Not driven: the four July sections (14), the dark-veil scene. **Run 32 (00:15–00:47) merged #186: 6 rows off, 0 FAILs** — all four fix-pass-4 talents PASS from `PlayerBench` (Unravel Everything fills 2 and detonates both in one cast; `edhaAwaitLocal` never timed out), **R-65 CLOSED**, the starting-kit console API, the roster script re-run clean (`orphans: 0`); run 31's `Guardian Stance` mystery explained (a stale adjacency aura on token-less PCs). New finding: all 10 culture items fail system validation at build (severity = run 33's read). Two six-run-stalled July rows re-classified as Ben's (the bulk sync button) and as ruling R-56. **Run 33 (00:50–01:21) merged #190: 1 row off, 1 FAIL, 1 answered** — item 29's Fault Line row PASSED (both insiders on the burst card, the ally's save line, the caster on neither, the outside ally untouched); the culture-item read: the id IS dropped to `"none"` but the wizard is NOT broken (the `cultural:<slug>` expertise is literal data) → log noise + a latent trap, fix = an `init`-hook registration (ENGINE-ONLY); **FAIL: the Stalker's veil has never auto-toggled** — `edhaDarkVeilSweep` reads `actor.effects`, the marker is item-transferred and lives in `allApplicableEffects()` (proven by matched control) → **fix pass 5**; a second, unnamed symptom after the one success recorded PARTIAL. R-6 sharpened (the Region catches the caster). **Run 34 (02:00–02:31) merged #195: 8 rows off, 0 engine defects** — 5 of 8 R-4 rows PASS, each measured in both directions in the same round (per-round ledger, timed expiry, out-of-combat focus watch, two combats, GM focus edit typed AND bar-dragged); three R-4 halves narrowed in place → run 35; **fix pass 5's three rows all PASS** (the Stalker's veil auto-toggled for the first time ever; 2bS-11 retired in full; the culture registration proven through the consumer); two rulings raised (R-74, R-75). **Run 35 (02:36–03:00) merged #198: 5 rows off, 0 engine defects — ALL EIGHT R-4 FACES NOW PASS** (the window half of the negative control, the `costs:`-rule half, the Investiture/Edict face), item 13's migrated spend taxes, the single-target picker driven end to end; two halves narrowed (item 13's heal max-clamp, item 14's `edhaSovTargets` split); R-76 raised. **Run 36 (03:03–03:25) merged #201: 3 rows off, 2 defects** — item 12's three two-GM sites + the negative control PASS with a `userId`-recording observer as the positive control (exactly one write per change, from `Bench`; Ben's client received the broadcast and wrote nothing); item 13's heal clamp landed on exactly 61 not 62; item 14's split put the caster in neither list. **Defects → fix pass 6**: the Investiture-max persist writes outside the two-GM gate (proven; R-77) and White's max HP 64 ↔ 57 (hypothesised). **Run 37 (03:49–04:02) merged #204: 1 row off, 0 defects** — the max-HP row retired with the doubler reproduced as a positive control (bare `prepareData()` → 64, `reset()` → 57; Green carries the same `Hardy — Max HP` effect, em-dash vs hyphen, which discharges fix pass 6's second-root-cause worry — the doubling is per ADD-mode change, not per actor); the Investiture-gate row BLOCKED on Ben's pre-deploy `Gamemaster` client (inference, recorded as such); item 10's probe re-specified (Secret = −2 is finite). Effect-diff false positives from JSON key order explain the standing Guardian Stance hazard. **Run 38 (Sunday 08:35 → PR 08:59) merged #207 by Ben at 12:11 (the PM session was frozen): 5 rows off, R-78 filed (Ben: retire the handler → item 48), the weapon-picker "a Agent" / "a Envoy" article defect → item 48, and run 22's "structural and permanent" rendering blocker DISPROVED — fronting the browser pane flips `document.hidden` and observers run, so every BLOCKED-on-rendering row is to be re-read.** **Run 39 (19:03 → PR 20:05, #228): 9 rows driven, 8 PASS (7 rows + 1 sub-row retired), 0 FAIL, 1 BLOCKED narrowed** — served hash `7d8e0226…` re-verified; **R-77 PASSED and runs 37/38's "a client's engine is unreadable" blocker was WRONG: `Get-Process` shows every Foundry process started 18:59:24–38, after the deploy — compare process start time to deploy time before recording any two-client blocker (now a runbook recipe)**; R-51 (phantom break at 7.071 ft, no cue), R-52 (7.5 / 0 / 5 / 7.071 fire, 10 does not, Roek 20 fires / 25 refused), R-12 (all four faces), R-10 (Death Ward / Raise Dead / Unbreakable Line each land 1 HP through a real Withering mark; plain heal 0), R-36 (incl. the tie), R-54 (five faces, three across a real reload), R-72 (both owner-side halves; POS 3 needs `PlayerBench`). **New defect → item 68 (fix pass 8): an `edha-focus` `resource: hea` rule ANNOUNCES the ungated heal amount** ("heals 5" when it healed 0; blast radius = every `edha-focus` `hea` rule, Field Medicine shipped). World diff clean; the zero-combatant combat deleted under PM-R13. Two harness notes: a real reload wipes the page-global start snapshot (write it to `sessionStorage` next run); bench PCs' pools left at test values (inside the licence). **Run 40 dispatched 20:17** on the pushed engine (`0ea0741a…` = #233): fix pass 7b's 12 rows, then 51 / 49 / 66 / 50 / 52 / 54 / 53's rows, then 34b's seven loot rows and R-72 POS 3 (both want `PlayerBench`); the REBUILD-class rows stay BLOCKED-ON-DEPLOY | B | opus | L | Foundry window ✓ | bench-pending | #142 #143 #145 #147 #151 #157 |
| 6 | **35** Re-land the dashboard-on-the-phone branch (`claude/in-app-dashboard-snapshot-ecwudz`, 3 commits, 5-file conflict with today's `pm-state.js`) — the phone lost its Snapshot/Dashboard on 09-05 14:55; dispatched 17:12 in a worktree, branch `pm/35-phone-snapshot-dashboard` | R | opus | M | — | merged | #159 |
| 7 | **38** The TODO doc is ONE dashboard section (`repo-sec0`, 66 KB) and `tests/pm-state.test.js` shards the real dashboard against a 64 KiB stress cap — item 36's worker had ~390 bytes of headroom and a 1.2 KB note failed the gate. **Every new TODO item now fails `tests/run.js` until this lands.** Fix: split the repo tab into one section per `## N.` item in `build-dashboard.js` (better phone rendering too) and re-pin the stress test against the largest real section; the TODO entries for 38 and 39 are written by THIS worker (they cannot be written before it) | R | sonnet | S | — | merged | #162 |
| 8 | **39** `audit.py`'s NO FILE message + the gate docs — premise corrected 21:20 (the misspelt name already exits 1; the reported exit 0 was a masked code); residual shipped: keys in the message, deity KEYS in both gate lists, exit code pinned by a subprocess test | R | sonnet | S | — | merged | #166 |
| 8b | **37** Bench roster harness: detect + repair ORPHAN tokens on the Playtest Map (code + a pinned node test in the worker's PR; the live before/after is bench run 30's 🤖 row). **Item 40 was folded in here and is CLOSED without a change** — run 29 found the talent is `Adaptive Mutation` and `Bench — Life` always had it. **Its 🤖 row PASSED on bench run 30** (3 repaired, then 0; all three tokens drive; the map has seven orphans, the four non-roster ones correctly left alone) | B | sonnet | S | — | merged | #171 |
| 9 | **33** Re-land handout-forge skill + session-zero one-pager from PR #93 (`fbc8e20`) — took tonight's last slot instead of 39 | R | sonnet | S | — | merged | #164 |
| 10 | 26 Bench PCs get normal vision (R-2) — `prototypeToken.sight.range` 20 ft, cited; its 🤖 row PASSED on run 30 from the player client (1350 px = 20 ft); already-placed tokens keep their own stale sight → new 🤖 row + TODO 42 | R | sonnet | S | R-2 ✓ | merged | #173 |
| 11 | 27 Retire the `GM summon relay` row (R-1) — retired with its reason, relay code marked conditionally dead on purpose (comment-only), R-1 → §K; ⚑ 22 → 21 | R | sonnet | S | R-1 ✓ | merged | #184 |
| 12 | **30** Rulings close-out R-7/R-19/R-34/R-49 → §K; two rows re-worded; the R-34 🤖 row (its indicator half PASSED on run 30; the staging exposed the fix-pass-3 defect) | R | sonnet | S | R-7/19/34/49 ✓ | merged | #172 |
| 13 | **31** Mobile board models operating windows, not quiet hours (`pm-state.js` + page + test) — in a worktree, parallel to fix pass 2 under PM-R8 | R | sonnet | S | PM-R7 | merged | #150 |
| 14 | 20 One gate list, Windows-clean gates — `scripts/gates.js` is the one list (`--list`, `--ci`, `--only`); CI calls it; five doc copies replaced by pointers | R | sonnet | M | — | merged | #167 |
| 15 | 21 Stale-doc sweep — `scripts/README.md` from `git ls-files` + `check-scripts-readme.js` verifier (gate candidate, not wired), archive moves per PM-R2 with stubs and every live link re-pointed, engine ~19.7k lines, pre-commit regex + rulings doc; four bullets moot after the clone | R | sonnet | S | PM-R2 ✓ | merged | #174 |
| 16 | 18 Overlay name-collision guard — fallback scoped to atlas+group, collisions logged, six packs byte-identical; **found the fallback was LIVE**: The Final Study's stale docId → TODO 41 | R | opus | S | #16 ✓ | merged | #170 |
| 17 | 19a Handoff reference rewrite — §1–§10 + the markers section rewritten true-to-today, ≤ 800 lines with a TOC, in place at the bottom (19b lifts it and moves the ~110 deltas); §9's dashboard anchor byte-identical; a ten-question cold-read table as the proof. Dispatched 20:54 as a `fable-worker` (PM-R14), worktree — the handoff is 15,178 lines tonight. **The first worker gathered the local facts, spawned three subagents to mine the deltas, and ended its turn waiting for them (1.1M / 48 turns; the reports arrived at the PM; its unchanged worktree was auto-removed). Re-dispatched 21:26 as a fresh `fable-worker` with the three mining reports handed over as files (`tmp/pm/19a-mine-A/B/C.md`) so nothing is mined twice. **Merged 21:47 after review** — 679 lines from `## Reference — table of contents` to EOF (TOC, the markers section verbatim, §1–§10 true today, §H history for §7.0 / §7.-1 / §9a–§9g); §9 heading byte-identical, Engine tab 40 → 40 items; the ten-question cold read answered with line numbers by an independent Sonnet reader; the worker folded item 4 and bench 40 in as they landed and lifted the rulings ceiling to R-85 itself. 3.9M / 113 turns / 20 min. PM call: no second copy of the ⚑/🤖 section at the top — every pointer is by heading text and 19b lifts the reference within the hour. Found: CLAUDE.md still describes the name-keyed backlog as live and says "45 rulings" / "~19.7k lines" → fold into a docs sweep item | R | fable-worker | L | PM-R1 ✓ | merged | #249 |
| 18 | 19b Handoff changelog move — the ~110 dated deltas verbatim into `docs/handoff-changelog/2026-MM.md` (newest first inside each month) by a re-runnable `scripts/handoff-split.js`, `HANDOFF_ARCHIVE.md` folded into the folder, the reference lifted to be the whole of `EDHA_FOUNDRY_HANDOFF.md`, iron rule 5 + CLAUDE.md map + every skill that says "delta at the TOP of the handoff" re-pointed, `build-dashboard.js` checked (Engine tab 40 → 40). Proof: hash of the removed delta text = hash of the month files' bodies; heading counts; `git log -S` for three titles. Dispatched 21:48 as a `fable-worker` (PM-R14), worktree, the minute #249 merged. **Merged 22:00 after review** — all 142 deltas moved verbatim (18 / 42 / 1 / 81 by month; sha256 of the removed text = sha256 of the month bodies, `8ae508b8…`, re-counted by the PM: 142 on `main`, 143 in the month files with the 19b delta), `HANDOFF_ARCHIVE.md` → `docs/handoff-changelog/ARCHIVE-header-wall.md`, the handoff is the 691-line reference alone, iron rule 5 + the CLAUDE.md map + nine skills + the runbook + `audit.py` re-pointed, Engine tab 40 → 40. "Done when" caveat recorded: `git log --follow` cannot reach a delta's original commit; `git log -S"<title>" -- EDHA_FOUNDRY_HANDOFF.md` does (proven for three months). 1.5M / 86 turns / 12 min. **PM duty: run `node scripts/handoff-split.js` + `build-dashboard.js` once fix pass 9 merges (its delta lands on the old rule)** | R | fable-worker | L | 19a ✓ | merged | #251 |
| 19 | 5 Hook-firing test driver — `fireHook` + stub factories in `tests/harness.js`, three write-asserting hook tests (single-target gate, pre-apply-damage temp HP, timed-status expiry), each proven to fail under a one-line engine mutation; suite 608 → 642; the test-pass-fixes skill now says how to fire a hook | R | opus | L | — | merged | #182 |
| 20 | 23 Banner the unbannered engine lines — 21 banners (31 → 52), section map in `ENGINE_INDEX.md`, stripped source byte-identical (PM re-verified: the repo stripper keeps blank lines, compare with blanks dropped); pushed live with the 23:53 deploy | R | opus | M | — | merged | #179 |
| 21 | 24 Table-driven handler registry — `EDHA_EVENT_TYPES` / `EDHA_HANDLER_TYPES` arrays + ONE registration loop (same order, labels, schemas, executors), exposed on the API; `handler-schemas.js` evaluates the tables through the harness instead of regex-parsing source; a snapshot test pinned BEFORE the refactor; a 🤖 smoke section (every type in the Events-tab picker, one talent per family fires). Dispatched 20:54 as a `fable-worker` (PM-R14), worktree. **Merged 21:24 after review** — 102 call sites → two tables + one loop, the elements ARE the registered objects (pinned by identity), snapshot committed first and byte-identical after, schemas evaluated through the harness (parity 10052 = 10052 / 9302 = 9302 bytes), lint pass 18 moved off its regex, 966 tests; 3.5M / 149 turns / 25 min. 16 🤖 rows (`# BENCH — Handler registry smoke`). **Not yet live — rides the deploy.** Leftovers → TODO 71 | B | fable-worker | L | #23 ✓ | bench-pending | #244 |
| 22 | 11 Path-literal scripts onto `lib/paths.js` — **folded into item 32's PR** (row 32) | R | sonnet | S | — | merged | #156 |
| 23 | 13 `resourceWrite` sites → **`edhaResourceWrite`** (the premise was wrong: none of the 12 was a spend; 11 bookkeeping writes now carry `edhaBookkeepingTag`, one spend keeps its stamp, `edhaDrainFocus` untouched pending R-72); ratchet 12 → 0; two 🤖 rows; **deployed 02:34** | B | opus | M | — | bench-pending | #194 |
| 24 | 14 `userTargets` sites → `edhaUserTargetTokens()` (nine sites + the singular reader; the floor of 1 is the reader itself — "Done when: 0" corrected); ratchet 10 → 1; one 🤖 row; **deployed 02:34** | B | opus | S | — | bench-pending | #193 |
| 25 | 12 `edhaDefBuffGmGate` at the 20 hand-derived primary-GM gate sites — the idiom carried TWO polarities (16 world-writing sites want "single applier", three region traps must spring on a GM-less client), so it is two helpers now; ratchet 20 → 1; one 🤖 two-GM row; **deployed 03:02**, bench run 36 drives it first | B | opus | M | — | bench-pending | #197 |
| 26 | 10 Disposition fail-open backlog — **batch 1 merged #200**: 63 of 74 occurrences (31 sites) fail CLOSED via two pure primitives `edhaSideSame` / `edhaSideHostile`; ratchet 74 → 11; two sites had read two unknowns as OPPOSITE sides; a baked side is a stored fail-open (cast sites now bake `edhaActorSide`); **deployed 03:27**, bench run 37 drives its sideless-probe row first. **Batch 2** = the 11 card-wording / picker-list sites — dispatched 22:33 as a `fable-worker`, worktree; **merged 22:49 after review (#263) — ITEM 10 CLOSED, `dispoFailOpen` 11 → 0 (tombstone)**: `edhaPickAccepts` names its predicate per branch (`ally` / `anchor-ally` → `edhaSideSame`, `enemy` / `anchor-enemy` → `edhaSideHostile`), the empty-note sweep and the movement-window card say when the OWNER's own side did not resolve, the Edict `<select>` and the Beacon list omit an unresolvable token; six family pins (a reversion of three families fails 7); four 🤖 rows (`I10b2-1..4`, expected vacuous per run 38 — a placed token cannot carry a non-finite disposition). 1.2M / 66 turns / 13 min. Found: Power's `!edhaSameDisposition`-as-enemy site → **TODO 77**. **ENGINE-ONLY — not yet live, held for bench 41** | B | fable-worker | L | — | bench-pending | #200, #263 |
| 27 | **29** `kind: line` zones catch allies too (R-5) — `edhaTokensInLine` replaces the enemies-only helper, both riders read the one caught set, Fault Line is the only consumer, R-5 → §K, one 🤖 row; **deployed 00:49**, bench run 33 drives it first | B | opus | S | R-5 ✓ | bench-pending | #185 |
| 28 | **28a** Out-of-combat scope: gate watches on an ACTIVE combat containing the owner (R-4 half a) — dispatched 00:33 in a worktree, `pm/28a-combat-gated-watches`; generic gate helper, adopted-sites table + deliberately-ungated list, mutation both ways, 🤖 rows per symptom face; R-4 stays open until 28b + the bench. **Shipped:** `edhaInActiveCombat` (the started/active combat this creature is a combatant of, never `!!game.combat`), riders, `edhaWatchCombatGate` (gates on `scope: "scene"`, `outOfCombat: true` opts out), ~30 sites, 17 two-direction cases, five 🤖 rows incl. a negative control; **deployed 01:24** | B | opus | M | R-4 ✓ | bench-pending | #188 |
| 29 | **28b** Out-of-combat scope: tag bookkeeping writes (R-4 half b) — POSITIVE stamping (`options.edha.spend` at four spend writers + a pre-use expectation for the system's own untagged consume), one predicate `edhaIsSpend` at the two watches; 18 two-direction cases; three 🤖 rows; **deployed 01:24**; R-4 settles when the bench confirms both halves | B | opus | M | R-4 ✓, 28a ✓ | bench-pending | #189 |
| 30 | **36** Picker cancel must not burn the once-per-scene use (R-69) — one moved statement + 8-case regression + a generic pin; 🤖 row (it lived under `# BENCH — Sovereignty`; run 28 corrected it to Order — Final Decree is an Order talent) **PASSED on bench run 28, all four legs** | B | opus | S | R-69 ✓ | merged | #160 |
| 31 | **34** Fleet weapon migration (34a, REBUILD) + loot caches: player-clickable chest + body search (34b) — re-do PR #103 on current main. **34a dispatched 19:15 as the first `fable-worker` (PM-R14), worktree; merged 19:31 after review** — 11 attack items → `kind: "weapon"` (4 natural ones `alwaysEquipped`), `edhaRuleBearer` on both actor-wide rule loops (mutation: re-gating either loop fails its pins), summon attacks build as weapons, `advItemDoc` passes the flag and `validate.js` type-checks it; parity over all 336 embedded docs: 11 changed, 0 roll differences; `gates.js --ci` 12/12 in the worktree with `EDHA_DATA` pinned. 9 🤖 rows in a new `# BENCH — Fleet weapon migration, 34a` section. **REBUILD + ⟳ Sync owed to Ben.** Found: the 39 later bestiary blocks carry 44 attack items still `kind: action` → **item 65 (34c)**. **34b (loot caches) dispatched 19:51** (`fable-worker`, worktree, ENGINE-ONLY); reported 20:10, in review (PR #233): `edha.createLootCache(name)` mints a flagged adversary-type actor with a linked chest token (`icons/svg/chest.svg` verified on v13.351 — #103's ⚑ settled); a `Token#_onClickLeft2` init-time prototype patch (the phantom-veil idiom) gives a non-GM double-click on a cache or a defeated adversary within 5 ft edge-to-edge a whispered contents card and returns before the sheet render; Take → `loot-take` socket → `edhaLootTakeGM` with a SYNCHRONOUS claim before the first await (the two-relay pin proves exactly one winner — #103's existence-check guard let both through); bodies keep `alwaysEquipped` weapons; 11 cases, eight reversions shown failing; 1.8M / 89 turns / 19 min; 7 🤖 rows; the `Loot Caches` folder literal added to lint's name allowlist (a folder, not a talent). **Merged 20:15 after review** — the PM merged `main` once (three-file conflict: handoff top ×2 blocks, the 34a bench section where 34b's and 34c's sub-blocks both appended, the dashboard; every delta and both sub-blocks kept, §9 line intact). **Item 34 is CLOSED: 34a #220 + 34b #233; 34c = item 65 #232.** Both halves bench-pending | B | **fable-worker** | L | Foundry window for the bench | bench-pending | #233 |
| 32 | 22 Radiant rows + key dialects — 225 Radiant rows parked in `source-materials/radiant-orders.json` (no consumer, PM-R3), one lowercase dialect, `normRow` aliases gone, `validate.js` rejects the old keys by name, six packs byte-identical (DATA-only, no rebuild); two tests that would have gone vacuous fixed | R | opus | M | PM-R3 ✓ | merged | #175 |
| 33 | 4 Engine split into concatenated sources — per-section sources under `module-src/scripts/engine/` cut at the item-23 banners by a re-runnable `engine-split.js`, assembled byte-for-byte by `engine-assemble.js`, a `--check` gate in `gates.js`; **PM-R15 applied: the assembled `register-skills.js` stays the tracked + deployed file** (F5, sync, harness, lint and every grep unchanged). Proof: three equal SHA-256s (before / assembled / `origin/main`) + a source mutation the gate catches. Dispatched 21:26 as a `fable-worker` (PM-R14), worktree, once #244 had merged. **Merged 21:38 after review** — 55 byte-exact sources (every column-0 `/* ===` banner is a seam; largest `53-native-event-system.js`, 3,067 lines), `engine-split.js` re-runnable, `engine-assemble.js --check` = gate `engine-assembly` (11 local gates) + the pre-commit body; sha256 `acac2589…` equal three ways, verified by the PM in the worktree; mutation names line 13186 / `41-life.js:5`; 966 tests unchanged; CLAUDE.md, ENGINE_INDEX (section → file map), three skills, scripts/README re-pointed. 1.6M / 86 turns / 10 min. 55 files (not the brief's 20–40): accepted — one banner, one file is the rule that survives re-runs | R | fable-worker | L | #23 ✓, #24 ✓ | merged | #247 |
| 34 | 9 Map fork consolidation | H | opus | M | bridge/MST rulings batch | blocked(rulings) | |
| 35 | **32** Move the repo off OneDrive (`docs/REPO_MIGRATION_BRIEF.md`) — one lane-R worker PR first (path literals: `foundry-build.js` DATA, `run-playtest-build.bat`, three prose paths, item 11 folded in, `.gitattributes` per PM-R9) — dispatched 16:38 in a worktree, branch `pm/32-onedrive-path-literals`, then Ben's fresh clone (lane H) | R | sonnet | S | #139 #150 #151 ✓ · fold item 11 in | merged(repo side; Ben's fresh clone is the lane-H half) | #156 |
| 36 | **45** Rulings close-out 2026-09-06 — every phone answer recorded inline in `EDHA_RULINGS.md` (no-change answers → §K; change answers ANSWERED-pending-ship naming their TODO item; §I's fourteen → §K as accepted; R-72 gets an entry; the GM-less traps get an R-number; R-78 recorded), the docs-only consequences (checklist rows retired / reworded per R-9, R-16, R-24, R-26, R-30, R-33, R-39, R-41; 28a's ungated list + the `ENGINE_INDEX` note for R-75 and the traps; the Playtest Map scene licence in `bench-run` hard rule 4 + the runbook; R-76's design seed; the `ACTOR_STAT_DERIVATION.md` doc-map line), handoff delta, dashboard. Source: the inbox dump. Dispatched 17:15 in a worktree; **merged 17:44** after review (CONFLICTING on the dashboard after #212 — merged `main`, regenerated, waited for the run on the new sha; one review correction: R-36 → item 47, not 48). 85 ANSWERED blocks; new **R-79** (the GM-less traps, KEEP); R-73 stays in §I vetoed-pending-item-54; **R-18 and R-48 were NOT in the dump — still open** | R | sonnet | M | inbox dump | merged | #213 |
| 37 | **46** File TODO items 43–62 from the 09-06 rulings — entries only (Why / What / Done-when / `PM:` line each, specs verbatim from the inbox dump; 41 folded into 58; 45's own entry too). Dispatched 17:15 in a worktree; **merged 17:23** after review (arrived CONFLICTING on the dashboard after #211 — PM merged `main`, regenerated, waited for the run on the new sha; one review correction: 58's Withering Touch overlay is `deity-death.json`) | R | sonnet | S | inbox dump | merged | #212 |
| 38 | **47** Fix pass 7a — heal / status / resource family: R-10 stabilize bypasses no-heal · R-12 raise clears its own Remain · R-25 Rallying Shout prints only at 0 HP / Unconscious · R-36 THP `source` relabels only on a win · R-51 a phantom's break is no ally drop · R-52 (i) +2.5 ft slack in `edhaAllyDropEligible` · R-72 drains → `edhaBookkeepingTag` (+ R-76 header comment) · R-54 `EDHA_HP_BONUS` 1 → 0 + comment + test re-pins + the checklist row. ENGINE-ONLY (F5); a headless pin + 🤖 row each. Dispatched 17:50 in a worktree; **merged 18:33 after review — seven of eight shipped, each mutation-proven** (suite 742 → 771). **R-25 did NOT ship:** Rallying Shout's reminder is an authored `edha-note` rule on Rousing Presence and `edha-note` has no target-condition dial — a new generic field + an authored value = REBUILD class → **item 63**. R-10 needed no behaviour change (all four drop-to-1 writers already bypassed the heal cut; now pinned as a family). R-52's own prose was wrong in our favour: the boundary is inclusive, so the Crownox Ring's "adjacent ox" now passes under slack alone — **item 62's motivating example no longer fails; the bench row measures it.** Three existing pins deliberately flipped (two R-72 guards, the relay guard), each with a ⚠️ header. **Not deployed — Foundry closed** | B | opus | M | 45 ✓ 46 ✓ | bench-pending | #215 |
| 39 | **48** Fix pass 7b — cards, labels, zones: R-31 "(Illusion)" label on a PC's copy · R-32 "swept N · newly Weakened M" · R-37 three card nits · R-38 Dread Presence refusal whispers the mover (throttled) · R-55 sheet chips SPENT / total · R-78 retire `edha-aoe-template` · R-13 a snare under a creature ARMS, springs on its move · R-6 Fault Line terrain spares the caster · run 38's weapon-picker article. ENGINE-ONLY (F5). Dispatched 18:36 in a worktree; **merged 19:15 after review — all nine shipped, 51 headless cases, each ruling mutation-proven.** Three families, not nine bugs: cards reporting a number without saying whose (R-32, R-37 ×3, R-55), grammar baked into literals (R-37(2), the article), zones catching the wrong creature at the wrong moment (R-13: v13 fires `tokenEnter` for every token already inside a new Region; R-6). **R-6's exit: exempt the caster's token, not shift the rectangle** — the terrain stays exactly the damaged line; the bench measures it. R-32's card reads "swept 5 · newly Weakened 0" (the quoted spec). Found out of scope → **item 64**. **Not yet live — the push waits for bench run 39 to finish on the hash it verified** | B | opus | M | 45 ✓ 46 ✓ | bench-pending | #217 |
| 40 | **49** R-15 the next-test slot becomes a LIST (+ R-57 absorbed: expired entries pruned). Dispatched 19:17 in a worktree, after #217 merged; **merged 19:40 after review** — `flags.nextTestMod` is an array (cap 12 via `edhaListPush`), three writers append, four readers apply every live entry (modes boolean-OR per direction, formulas sum with per-source labels, `count` and expiry per entry, round-stamped entries pruned on read, legacy single object migrated on first read); the claim map is keyed per (actor, grant); a cross-actor clear now relays. 11 cases, five reversions shown failing; 2bI-4 reopened + 4b / 4c. **New ruling R-80** (a victim holding BOTH an advantage and a disadvantage entry — they cancel; default applied). Found: a damage-path rider with a leading minus concatenates as `base + -1d6` → **item 66** | B | opus | S | 46 ✓ | bench-pending | #221 |
| 41 | **50** R-70 consume-dialog wrapper — every cost row starts ticked; the one sanctioned system-dialog wrapper. Dispatched 19:31 (`fable-worker`, worktree); **merged 19:42 after review** — `CosmereItem#showConsumeDialog` wrapped ONCE (libWrapper if active, else an idempotent prototype patch) with a pure `edhaPreTickConsumeOptions` (`shouldConsume ?? true`, so an explicit `false` is honoured); declared in the file header as the iron-rule-2a exception; 6 pins incl. a source-scan that exactly one wrapper exists; 1.3M / 80 turns / 11 min. One 🤖 row (Reknit Form charges both) | B | **fable-worker** | S | 46 ✓ | bench-pending | #222 |
| 42 | **51** R-17 a declined / ignored Puppeteer · Unnerving Approach offer refunds the Investiture (R-69's picker path). Dispatched 19:48 (`fable-worker`, worktree) once 54 had landed; **merged 20:08 after review** — ONE decline path `edhaOfferDecline` (resolve the card once, then `edhaRefundCost`, the R-69 idiom); a Decline button + an `offer` message flag on system-charged offers; `edhaSweepIgnoredOffers` on the round change declines unresolved refundable offers from an earlier round; accept refuses a resolved card; Puppeteer's watch-posted offer non-refundable by construction; 4 pins, M1–M4 shown failing; 15 min. Rows 2bJ-10 / 10b / 10c 🤖 | B | **fable-worker** | S | 46 ✓ · 54 ✓ | bench-pending | #230 |
| 43 | **52** R-27 Battle Fever — the stack is SPENT on the next test, cap Rank, clears at the owner's turn. Dispatched 19:31 (`fable-worker`, worktree); **merged 19:45 after review** — one post-roll consumer `edhaRallyConsume` (re-reads the flag, clears it, posts "spent +N"); the pre-roll rider and the turn-start clear untouched; the cap is enforced twice so it has no one-line reversion (stated honestly in the test header); 1.1M / 60 turns / 10 min; rows 52-1 / 52-2 🤖 | B | **fable-worker** | S | 46 ✓ | bench-pending | #223 |
| 44 | **53** R-50 the FIRST ambush strike benefits — the belief test runs synchronously inside the damage rider; ten carriers. **Dispatched 19:15 as a `fable-worker` (PM-R14), worktree; merged 19:27 after review** — the decision is now synchronous (`edhaAmbushBeliefRoll` pure → `edhaAmbushBeliefTest` sync with an in-flight pending map → `edhaAmbushBeliefCommit` async), the rider gate is `edhaTargetFooledOrTest`, the use hook awaits nothing; 8 cases, four mutations shown failing. **1.5M / 71 turns / 14 min** — the Opus pass of the same size ran 10.7M / 380 turns. Not yet live (push held for bench 39) | B | **fable-worker** | M | 46 ✓ | bench-pending | #219 |
| 45 | **54** R-73 (b) + R-35 dispel widening — item-owned transferred effects as a temporary DISABLE, Omen ledger entries dispellable. Dispatched 19:31 (`fable-worker`, worktree); **merged 19:47 after review** — `edhaAllEffects` read, fail-closed `edhaEffectOwnerItem` (item-owned → DISABLE only, decided from the document; a forged delete button still lands on disable), new `ledgers` rule field defaulting to `omens:omen` (no rebuild), `edhaDispelLedgerMark` drops rows then unmarks; 9 pins, four mutations shown failing; 1.9M / 83 turns / 14 min; two 🤖 rows in Chaos | B | **fable-worker** | S | 46 ✓ | bench-pending | #224 |
| 46 | **55** R-56 adversary sheets AND token sight on the Edha AWA table (engine + build → REBUILD + world bulk sync; one block with an explicit `senses` override). Dispatched 20:21 (`fable-worker`, worktree); reported 20:39, in review (PR #240): the `type !== "character"` guard gone from `edhaDeriveSheetStats` (senses only — HP / Speed stay PC-only), the `preCreateActor` token default and the AWA watcher; the ready sweep resets every actor; `advSensesRangeFt` in the build (the flat 10 gone); **Briar-Gone Grove `senses: 30`** as the bespoke override (a rooted grove-heart perceives through its soil); seven mutations shown failing; read-back: 51 unchanged, the Grove 10 → 30. **Corrected the brief's premise: the pack token sight was already 10 at AWA 0 — the ENGINE half is what moves every world adversary's SHEET 5 → 10; the sync then makes tokens and sheets agree.** Risk stated as medium (live effect only after rebuild + sync). Found: `edha.fixPcTokens()` still retrofits characters only. **Merged 20:44 after review** (2.9M / 124 turns / 17 min; the PM merged `main` once — a doubled `---` separator is the only scar of the two-block handoff conflict). **REBUILD + bulk sync owed** | B | **fable-worker** | M | 46 ✓ | bench-pending | #240 |
| 47 | **56** R-14 melee mutation riders — a per-rule `onGraze` matching each card's wording (ENGINE + AUTHORED → REBUILD). Dispatched 20:21 (`fable-worker`, worktree); reported 20:40, in review (PR #242): **the brief's premise was off — the riders are not `edha-damage-rider` rules (those were hit-only all along, the graze clone strips them) but the `mutation` / `apexForm` flag readers, and the system's `onClickApplyButton` passes `applyDamage` no hit-vs-graze marker at all.** So: a click-lifetime breadcrumb from a wrap of `CosmereChatMessage#onClickApplyButton` (libWrapper or prototype, the rollDamage idiom) read synchronously by `edhaWrapApplyDamage`; dials `keenOnGraze` / `venomOnGraze` (`edha-mutation`) and `vitalOnGraze` (`edha-regen-grant`), baked onto the flags, readers stand down ONLY on an explicit `false`; authored: Bone Spurs on, **Venom Glands OFF (the one behaviour change: "melee HITS inflict")**, Apex Form on; six mutations shown failing; parity: 2 documents / 2 rules. Rows 2bW-18 / 19 🤖. Found: `edha-damage-bonus` `meleeOnly` rules (Warlord's Advance) also fire on a graze today — a separate item if R-14's rule should reach them (→ ruling candidate for Ben: **R-82**, filed on the board). **Merged 20:46 after review** (2.4M / 117 turns / 17 min; the PM merged `main` once). **REBUILD (deity) + ⟳ Sync owed** | B | **fable-worker** | M | 46 ✓ | bench-pending | #242 |
| 48 | **57** Adversary data batch — R-29 Combat Training (Stonebound Captain) · R-40 Fen-Heart 3×3 · R-46 Reckless Advance 25 ft (+ R-48: default (a) applies, Ben silent) · R-47 hide `NO NAMEABLE HOOK` from the card · R-74 one `costs:` line (Stalker's Fade). REBUILD (adversaries). Dispatched 19:32 (`fable-worker`, worktree) once 34a had landed on `main`; reported 19:48, in review (PR #226): seven themed commits; LevelDB read-back diff = exactly 1 actor + 19 items changed, validator 0 issues; lint pass 5 now FAILS a visible marker (mutation both ways); all 16 `NO NAMEABLE HOOK` markers are HTML comments; the Fade's cost moved from the native `consume` to the confirm click (`costs: "inv:1"`); R-48's default (a) applied to Explosive Leap. Corrected the ruling's premise: Combat Training's description was NOT empty (07-16 wording + a visible marker). Found: three more run-19 blocks with `bySize` at rank 2 vs rank-3 card numbers → **R-81 / item 67**; if ProseMirror strips HTML comments on save (the 🤖 row checks), the marker needs a GM-note field → follow-up. **Merged 19:52 after review** (2.0M / 99 turns / 16 min). **REBUILD owed** | R | **fable-worker** | M | 46 ✓ · 34a ✓ | merged | #226 |
| 49 | **58** Talent data batch — R-23 Volatile Strike `whenDealer: "any"` · R-28 Withering Touch "end of your next turn" (authored + `domain.json`) · TODO 41 The Final Study docId re-key (parity). REBUILD. Dispatched 19:31 (worktree); **merged 19:57 after review** — the three edits proven by field-level pack diff (only Volatile Strike's `whenDealer` and Withering Touch's description / note moved; the re-key moves nothing); **item 18's stated seed `MQvIkCSK7fIHjnZE` did not reproduce — the real seed is `yrIgDwup7iBdPq07`, verified three ways** (item 18's measurement was a transcription slip); raw LevelDB bytes are not build-stable (`_stats` timestamps), so parity is content-hash parity. 7.0M / 335 turns / 24 min (four `main` merges). **REBUILD owed** | R | sonnet | S | 46 ✓ · 41's OK ✓ | merged | #227 |
| 50 | **59** R-71 fold `system.damage.formula` at build time (TOOLING + DATA, REBUILD). Dispatched 19:48 (worktree); reported 20:17, in review (PR #234): a pure Node twin of `edhaFoldDieMath` (`scripts/lib/fold-die-math.js`, pinned formula-by-formula against the engine's copy) wired into the build — **and a load-bearing finding: it is a NO-OP on every current formula.** All 51 rolls and every authored overlay are rank / tier-scaled (`(@tier)d(2 * @skills.blue.rank + 2)`), which cannot fold without an actor; parity 0 formula diffs on real data, the fold proven on a synthetic numeric formula only. So **R-71 (a)'s premise was wrong: "Verdict's system card reads 2d8 + 5" needs an ENGINE-side runtime fold in the system's `rollDamage` pipeline → item 69.** Shipped as a harmless guard for any future flat formula; **merged 20:20 after review** (the PM merged `main` once); 64 now unblocked. **REBUILD owed** (the stack is complete: 34a, 57, 58, 65 + 67, 59 → one `deploy-to-foundry.bat` run after bench 40 reports) | R | sonnet | S | 46 ✓ | merged | #234 |
| 51 | **60** R-22 build guard — a `consume` entry with min ≠ max fails lint (TOOLING, mutation-verified). Dispatched 19:31 (worktree); **merged 19:55 after review** — `scripts/lib/consume-guard.js` (pure) + lint pass 23 scanning the authored overlays' `activation.consume` AND the adversaries' consume grammar re-derived (235 entries, a 200 floor pinned so a rotted scan fails), mutation against real data both ways; R-22 → §K. 5.4M / 268 turns / 23 min — the worker re-merged `main` four times as the engine PRs landed, which is where the turns went | R | sonnet | S | 46 ✓ | merged | #225 |
| 52 | **61** R-42 map polygons — Goldenport's coastal / island lobes, Corvaine's edge to the river bank; `lint_map.py` WARNs 4 → 0. Dispatched 22:12 as a `fable-worker`, worktree. **Merged 22:30 after review** — Goldenport re-traced with the toolchain's own tracer plus its coastal strip and two city islands (80 → 131 vertices, 77 old ones verbatim); the Thalendor / Corvaine seam moved to the Palewater's west bank at the ferry (both rings, because `nation_at` is first-match — the PM accepted the Thalendor edit the brief had not named); city WARNs 4 → 0, every other ring byte-identical, `gates --ci` green. The regenerated `thyrcross-nations.json` is a module ASSET — **goes live with the next sync push, held for bench 41's hash**. 1.7M / 90 turns / 12 min. Found: seven cityless Goldenport islets outside any polygon; `lint_map.py` never checks a city that resolves to the WRONG nation | R | fable-worker | S | 46 ✓ | merged | #256 |
| 53 | **43** Phone board "Needs you" view (+ the stale-heartbeat banner from the stall post-mortem). Dispatched 19:48 (worktree); reported 20:31, in review (PR #236): the page opens on a status line + the stale-heartbeat banner + one card per open ruling (Go with the default / Other… / Keep / Veto…) + one card per Ben-only ask (Done) + a ⚑ count; everything else under a persisted `#more` toggle; `parseOpenRulings` / `countCitations` feed `openRulings` into the dash index, `parseBenOnly` reads the board's "Waiting on Ben" line (numbered asks or bullets); pins R-18 / R-48 open and R-41 / R-42 / R-54 closed; verified in a local static preview. 43 min, 237 tool uses (Sonnet M). Note: the worker found no R-80 in the repo — it branched before the board PR that added R-80 / R-81 to the rulings table; the board parser picks them up now. **Merged 20:34 after review** (the PM merged `main` once); **14.4M / 412 turns — the most expensive item of the night, a Sonnet M with a browser-preview loop**; the PM republished the artifact page after merge | R | sonnet | M | 46 ✓ | merged | #236 |
| 54 | **44** `Ask:` lines on non-question ruling headings (phase 2 of 43). Dispatched 22:12 as a `fable-worker`, worktree. **Merged 22:28 after review** — 8 open rulings audited, 4 got an `Ask:` line (R-48, R-81, R-82, R-83), 4 already asked; item 43's parser had no `Ask:` fallback at all, so a two-line fallback + pins landed with it (the "Done when" required it); convention written into the rulings intro. 1.5M / 88 turns / 10 min. Found → TODO 76 | R | fable-worker | S | 43 ✓ | merged | #258 |
| 55 | **62** R-52 (ii) edge-to-edge range measurement for sized tokens + a bench sweep of every `rangeFt` gate — **reframe before dispatch:** item 47 found the ruling's own example (the Crownox Ring's adjacent ox at 7.5 ft) already passes under the inclusive slack, so the motivating failure is gone; keep the item for a Huge owner's "adjacent" and the sweep, but let the bench row measure first. **R-52, Ben 09-07: "I'm fine with whatever fix you can find for this. I think increasing slack would work, or editing the cue." — the applied slack IS the fix; (ii) only if bench 42's measurement row fails.** **Bench 42 MEASURED it:** touching pairs are edge-to-edge 0.0 ft at every size, centre-to-centre 1×1 5.0 · 2×2 7.5 · 3×3 10.0 (diagonal 14.1) · 4×4 12.5 ft — so the +2.5 ft slack fixes Large and nothing bigger. **Reframed:** size-aware slack (rule + half the owner's footprint) or edge-to-edge, worker's choice, pinned on the four measured sizes; Ben is fine with either (R-52) | B | opus | S | 47 ✓ · bench 42 ✓ | queued | |
| 58 | **65** (= 34c) The 39 bestiary statblocks statted after 07-18 (Reedling → The Cull-Alpha) carry **44 attack items still `kind: action`** — same migration as 34a, same proof shape (parity over the embedded docs, 0 roll differences; riders on the weapon harvested by `edhaRuleBearer`); adversaries pack REBUILD; found by item 34a. **Dispatched 19:52 (`fable-worker`, worktree) carrying item 67 (R-81 default a) in the same PR**, once 57 had landed; reported 20:08, in review (PR #232): 36 of the 44 become weapons (35 natural `alwaysEquipped`, the Forge-Hammer as registry `hammer`); 8 stay actions by the 07-18 analogies, each argued in one line (grabs, 2-action maneuvers, a burst, three Focus-costed bolts); parity over 336 embedded docs: 39 changed (36 + 67's 3), 0 roll differences; `gates.js --ci` 12/12. Corrected the entry's own measure: `kind` is never written as `"action"` (it is the default) — the real count is attack-bearing items without `kind: weapon`, 44 → 8. Found: both Sudden Growths place by `rangeByRank` (30 / 60 ft) while the cards say 10 ft → the R-81 family one field over (a note, not yet an item). **Merged 20:13 after review** (2.5M / 124 turns / 15 min); 12 🤖 rows. **REBUILD owed** (34a, 57, 58, 65 stacked; 59 still in flight) | B | fable-worker | M | 34a ✓ (#220) · 57 ✓ | bench-pending | #232 |
| 59 | **66** On the damage path a next-test rider whose formula starts with `-` is concatenated as `base + -1d6` (parser-hostile); the d20 path already handles the leading minus explicitly (`0 - …`). Pre-existing; one shared formula-join helper used by both paths, pinned on a negative formula. ENGINE-ONLY; found by item 49. Dispatched 19:48 (`fable-worker`, worktree); **merged 20:00 after review** — `edhaJoinRiderTerm`, both paths, 7 pins, positive riders byte-identical; 1.0M / 57 turns / 8 min | B | fable-worker | S | 49 ✓ | bench-pending | #229 |
| 60 | **67** R-81's sweep — the Brandram's Shockwave Slam + Reckless Advance and the Tussock-Sow's terrain square: `bySize` → explicit `distanceFt` = the card's number (default (a); one rebuild; can ride item 65's adversaries batch); found by item 57. **Rode item 65's PR** (dispatched 19:52) — Shockwave Slam 10 ft, Brandram Reckless Advance 10 ft, Sudden Growth `sizeFt: 10`; default (a) applied, open for Ben's veto; **merged 20:13 with 65** | R | fable-worker | S | 57 ✓ · R-81 default | merged | #232 |
| 62 | **69** R-71's real fix — an ENGINE-side fold of a talent's `system.damage.formula` when the cosmere-rpg system rolls it itself (the `rollDamage` pipeline the engine already wraps for riders — `edhaWrapRollDamage`): substitute the actor's roll data and fold with `edhaFoldDieMath` before the system's card prints, so Verdict's system card reads `2d8 + 5` like its engine card; pin: a rank-scaled formula folds to plain dice on the wrapped roll, a plain one is byte-identical; 🤖 = the item-59 Verdict row. ENGINE-ONLY (F5); found by item 59. Dispatched 20:21 (`fable-worker`, worktree); reported 20:28, in review (PR #237): 16 lines at the top of `edhaWrapRollDamage` — substitute `getRollData()`, `edhaFoldDieMath`, write `overrideFormula` only if changed; riders join onto the folded base; 9 pins, two reversions shown failing (4 and 5 cases); 7 min. Worker's note for item 56: the graze clone's `@damage.dice` strip now sees `2d8`, not the parenthetical. **Merged 20:31 after review** (0.9M / 51 turns); TODO 69 checked by the PM at merge (the entry landed with #235 after the branch was cut). **Not yet live — pushed with the next `module-src-sync` push after bench 40 reports** | B | fable-worker | S | 59 ✓ | bench-pending | #237 |
| 63 | **70** R-83's fix — gate the three ungated `hea` writers at their emitters (`edha-regen` turn-end, the decay lifesteal heal-back, `edhaBurstDetonate`'s heal hits); `edhaApplyBurstResults` stays ungated (R-10); the drop-to-one family pin's gate-call count raised with a declaration; three 🤖 rows. ENGINE-ONLY; found by fix pass 8. **Ben 09-07: "I'm not sure what this means and will want the pm to give me good examples when we get here in chat" — examples given in chat 10:1x and recorded in the ruling (item 79); still waiting for (a)/(b)** | B | opus | S | R-83 ✓ (a, 09-07 17:11) | queued | |
| 64 | **71** Registry leftovers from item 24 — a `scripts/README.md` row for `fold-die-math.js`, the executor-less `edha-illusion-upkeep`, six handler types with no authored user, the registration console line that still lists `aoe-template`, the loosened `note-target-gate` test slice; one-line dispositions into `ENGINE_INDEX.md`, no deletion without a ruling. Dispatched 22:11 as a `fable-worker`, worktree. **Merged 22:27 after review** — README row (`check-scripts-readme.js` clean, 67 paths / 48 rows), a no-op executor on `edha-illusion-upkeep` (its readers named), the registration console line DERIVED from the tables (no retired type can reappear), the `note-target-gate` slice on the next table row, six dispositions in ENGINE_INDEX (one generator-emitted by the cultures writer, five adversary-carried — none dead, no deletion candidate; proven by a scratch build read back through classic-level); the registry pin now NAMES the nine still-executor-less rows so the set can only shrink. Two mutations shown failing; snapshot untouched. 1.8M / 106 turns / 16 min. **ENGINE-ONLY half not yet live — held until bench 41 reports (hash stability), then pushed.** Found: `foundry-build.js items` single-scope TDZ crash → TODO 74; the nine rows → TODO 75 | R | fable-worker | S | #24 ✓ | merged | #257 |
| 65 | **72** Fix pass 9 — bench run 40's defects: the `edha-prompt-pick` `once` budget never bites (the round stamp is written under a dotted key and read flat — fix in the `edhaCoordOPR*` primitive, key-safe both sides), Ambush Bite's flavor label printed twice, **R-85** (expiry falls back to the bearer's combat) and **R-84** (refund an offer that cannot be made) applied as defaults; a pin per fix shown failing under reversion; the bench's 🤖 rows re-test at run 41. ENGINE-ONLY (F5). Dispatched 21:35 (`test-pass-fixes`, opus, worktree) — ported into the split sources since #247 landed first. **Merged 22:16 after review** — 87 lines of engine source, NO new primitive (`edhaFlagKey` at both ends of the `coordRound` store + a one-time `getProperty` fallback for documents stamped tonight; the doubled flavor is Foundry's `propagateFlavor` on the chat round-trip, repaired in `edhaTidyFormula` because the parentheses must stay for the graze clone; R-85 = one `??`; R-84 through R-17's own gate, the non-refundable card says "no cost was spent" — a stated, correct deviation); 12 pins, each shown failing under its own reversion (969/973/979/983 of 984); the PM merged `main` once more (kept both changelog deltas, re-ran the split for the counts). Four 🤖 re-test rows + `2bJ-10` unblocked. 6.7M / 266 turns / 37 min | B | opus | M | #245 ✓ | bench-pending | #253 |
| 66 | **73** Docs sweep — CLAUDE.md still calls the name-keyed backlog live ("200 talents' worth", the 07-24 ratchet counts), its map rows say "45 numbered rulings" (85) and "~19.7k-line engine" (21,792 / 55 sources); ENGINE_INDEX lists a pre-migration dispatch idiom and "52 banners" (54). One DOCS-ONLY PR, every number with the command that produced it. Dispatched 22:01 as a `fable-worker` (cheaper than Sonnet by measurement), worktree. **Merged 22:10 after review** — seven sentences retired or rewritten, every number beside its command (rulings 82 → the PM then gave R-80–R-83 their entries, so 86; engine 21,792 lines / 55 sources / 54 banners; `talent-migration` marked HISTORICAL); one review correction: its `[x]` fill had globally replaced item 13's `#PRNUM` placeholder with #252 — set to the real #194. 0.7M / 48 turns / 6 min. Found: CLAUDE.md "Current phase" and `talent-migration/SKILL.md` still read as a live migration; `work-item` §0.4 says "19k-line" | R | fable-worker | S | 19a ✓, 19b ✓ | merged | #252 |
| 67 | **74** `foundry-build.js items` (single scope) crashes on a TDZ `let` — `REGISTERED_HANDLER_TYPES` declared below the items writer's call; `all` hides it. Hoist + a pin per single scope. Dispatched 22:31 as a `fable-worker`, worktree. **Merged 22:40 after review** — the item-64 guard block hoisted above `main` (the IIFE runs synchronously to its first `await`; `items` AND `adversaries` alone hit the TDZ, `all` never did); five single-scope builds green, five-pack content parity vs `all` (hashes in the PR), a subprocess pin with a stub `classic-level` on NODE_PATH shown failing under the reversion; 984 → 986 tests. 1.1M / 74 turns / 9 min | R | fable-worker | S | — | merged | #260 |
| 68 | **75** Nine executor-less handler rows (named in `EXECUTOR_LESS_CONFIG_ONLY`) get the no-op executor item 71 gave `edha-illusion-upkeep`; the set shrinks to empty; snapshot unchanged. Dispatched 22:31 as a `fable-worker`, worktree. **Merged 22:43 after review** — nine no-ops with their reader named per row (none dead), `EXECUTOR_LESS_CONFIG_ONLY` empty, the pin now "every handler has a function executor"; snapshot diff 0 lines; the PM merged `main` once more and re-assembled. **ENGINE-ONLY — not yet live, held for bench 41.** 1.1M-class | R | fable-worker | S | #71 ✓ | merged | #261 |
| 69 | **76** Phone cards: `RULING_DEFAULT_RE` yields an empty default for bold-inline defaults (R-80 … R-85); R-80 / R-81 say "(§I)" but live in §C so they render as plain default-ask cards. Dispatched 22:31 as a `fable-worker`, worktree. **Merged 22:51 after review** — the default regex reads through inner bold, a bold upper-case APPLIED marks an entry applied in any section (the worker found R-84 / R-85 had NOT rendered as applied either — the §I stub is skipped by design), R-80 / R-81 got their §I stubs; one PM correction on the branch: R-48's ANSWERED line had been inserted after the R-53 stub instead of inside R-48's block, so the parser still listed it open — moved, and the real-file pin now holds R-48 CLOSED. Open cards after: R-18, R-80*, R-81*, R-82, R-83, R-84*, R-85* (* = applied-veto), every default non-empty. 1.1M / 64 turns / 10 min | R | fable-worker | S | #44 ✓ | merged | #262 |
| 70 | **77** Power's `want === "enemies"` filter reads `!edhaSameDisposition` as "enemy" (47-power.js ~L419) — the batch-1 corollary at an actor-level site the ratchet never counted; name the predicate, sweep every negated actor-level side read, pin. Dispatched 23:21 as a `fable-worker`, worktree. **Merged 23:35 after review** — the Power site plus FIVE siblings the sweep found (Natural Order's veil suppression, Voice of Authority's `rollerIs: enemy`, Shatter Focus's auto-prompt, the Fate snare spring, and the mirror image: Death Ward's `skipIfAlly` read `!edhaDisposHostile` as "willing"); each branch now names its predicate; seven positive-form sites correct as is; a pin file drives all five functions with the real helpers, 993 → 1005 tests; one 🤖 row (77-1). 2.0M / 99 turns / 13 min. ENGINE-ONLY — pushed live 23:35 | B | fable-worker | S | #10 ✓ | bench-pending | #267 |
| 71 | **78** Fix pass 10 — Ambush Bite's doubled rider label survives fix pass 9: the tidy runs in `renderChatMessageHTML` on a detached element before the system re-renders the damage section ~1.5 s later; fix at message creation or the system's own re-render, never a second render hook; audit the whole `edha-damage-rider` family. Dispatched 23:21 (`test-pass-fixes`, opus, worktree). **Merged 23:50 after review** — the doubling is in the roll's serialized data (core's `propagateFlavor` stamps the parenthetical's label into its inner terms; the rehydrated `term` re-derives with it; the cosmere card prints the recompiled getter AFTER `renderChatMessageHTML`, replacing the node the tidy walked), so the repair moved to `preCreateChatMessage` (`edhaUndoFlavorPropagation`, pure, the engine's only binding of that hook); Prognosis's unfolded rider fixed on the same three lines (`edhaFoldRiderFormula`, guarded against `@mod` / `@skill` / `@attribute`); 10 + 4 pins, two mutations (6 and 3 cases fail); 1005 → 1019 tests. **The acceptance string was corrected: `1d10 + 3 + (1d6)[Ambush Bite] + 0` — the parentheses stay (graze clone), bench 42 must not fail the row for them.** 4.5M / 206 turns / 28 min. ENGINE-ONLY — **pushed live 23:50** | B | opus | S | #265 ✓ | bench-pending | #269 |
| 61 | **68** Fix pass 8 — bench run 39's defect: an `edha-focus` `resource: hea` rule announces the UNGATED heal amount (Field Medicine: "heals 5" when the No-Healing gate delivered 0; the halved case misreports too); the card must print what `edhaHealCutGate` actually delivered. Root-cause in the `edha-focus` executor's `hea` branch, pin (gated 0 → card says 0; halved → the halved number; ungated unchanged), 🤖 re-test; `test-pass-fixes`. Dispatched 20:15 (worktree); PR #241 open 20:36, in review: **a family, not one bug** — `edhaCrossHeal` now RETURNS the delivered amount and one `edhaHealLine(who, requested, delivered, phrase)` builds every heal card from it (blocked → names the mark, never a number; halved → the halved number); seven announcers converted (Field Medicine's `edha-focus` arm, Interposing Shield, Shared Burden, Lifeline, two regen sweeps, the group-heal sweep which now counts who actually regained), plus the Investiture arm reports the clamped delta; 317-line pin file; gates 10/10. All seven commits carry the harness's attribution trailer → **squash-merged 20:52** (iron rule 6); 5.3M / 219 turns / 37 min. Its 🤖 rows ride bench 41 after the deploy | B | opus | S | bench 39 ✓ | bench-pending | #241 |
| 57 | **64** `foundry-build.js`'s `aoeRule()` still mints `edha-aoe-template` rules for any talent with `TALENT_TARGETING[…].area` and no `.burst` — today only Lay Foundation, whose authored `edha-zone` rule replaces the generated events, which is why the sweeps find zero and the pack build stays green; delete that authored `events` and the next build mints a rule nothing can execute since R-78. Retire `aoeRule()` (or route it to `edha-burst`) with a build-report parity proof. TOOLING-only; found by item 48. Dispatched 20:21 (`fable-worker`, worktree) after 59 landed; reported 20:34, in review (PR #238): `aoeRule()` gone; every `.area` entry checked (four carry `.burst`; Lay Foundation is the persistent Foundation zone — correctly NOT a burst); **the guard sits in both pack WRITERS** (`assertRegisteredHandlerTypes`, the registered set from the same parse lint pass 9 uses) because a generated rule never appears in `data/`, so pass 9 by construction could not see it; five packs content-identical before / after (the build emits five, not six — the brief was wrong); mutation: re-adding the generator fails the build naming the talent; 13 min. Found: the build report's `events:` count is taken BEFORE the overlay replaces `system.events`, so it counts rules the packs never carry → a small tooling note for the next build-script pass. **Merged 20:38 after review** (the PM merged `main` once) | R | **fable-worker** | S | 48 ✓ · 59 ✓ | merged | #238 |
| 56 | **63** R-25 (c) Rallying Shout's reminder prints only for an ally at 0 HP / Unconscious — needs a generic target-condition dial on `edha-note` (e.g. `whenTarget: "downed"`) PLUS the authored value on Rousing Presence's `RouseRallying000` rule (`data/authored/heroic-envoy.json`); iron rule 2b forbids the name-keyed shortcut and a dial with no consumer is R-74/R-76's complaint. ENGINE + AUTHORED → REBUILD + ↻ Sync; found by item 47. Dispatched 20:21 (`fable-worker`, worktree); **merged 20:41 after review** — one generic field `whenTarget` (blank / `downed`) on `edha-note` read by the pure `edhaNoteTargetGate` on the R-64 victim chain (blank = unchanged; unknown mode fails open); `RouseRallying000` set to `downed`; 8 cases, five mutations shown failing; parity: exactly one pack document changed; 15 min. Row 2bM-6b 🤖 (the retired 2bM-6 stays retired). **REBUILD (heroic) owed** | B | **fable-worker** | S | 46 ✓ | bench-pending | #239 |
| — | 2 History purge → **delete the 61 SAFE branches** (`docs/BRANCH_CLEANUP.md`, Ben's hands only) · 3 LICENSE | H | Ben | — | — | Ben-only | |
| 72 | **Bench run 42** — the REBUILD stack live for the first time (Ben's deploy this morning; served `609c7e45…` = `main` 14f2aa9) plus the night's engine rows (10 batch 2, 77-1, fix pass 10's corrected string, the map picker, the rider-family rows, R-52 (c)(ii)'s measurement); also retires the 13 rows Ben marked on the 09-07 dashboard and the three GM-less / two-GM rows under R-86. Dispatched 10:45 (`bench-run`, opus, worktree `pm/bench-42`). **Merged 12:05 after review (squash, trailer-free)** — 38 rows retired on evidence (all of 34a, 34c/65, R-81 ×3, R-46, R-48, 55, 56, 57, 58, 63, fix pass 10 ×3, the map picker, the article defect), 8 annotated BLOCKED with the blocker re-derived (no sideless token can exist on this build — null/NaN dispositions coerce to −1; 34b's body half needs `PlayerBench`), 1 defect → item 84, R-88 / R-89, R-52 (c)(ii) measured for item 62, Ben's 13 marks recorded; end-of-run world diff EMPTY; 16.9M / 511 turns / 79 min | B | opus | L | deploy ✓ | merged | #274 |
| 73 | **79** Rulings close-out 2026-09-07 — Ben's dashboard marks recorded inline in `EDHA_RULINGS.md` (R-18, R-52, R-56 reopened + scoped, R-82, R-80 / R-81 / R-84 / R-85 accepted, R-83's gloss, R-77 / R-48 confirmations, F-1 settled, new R-86 / R-87), items 80–83 filed, Mistheron art DONE, delta. Dispatched 10:45 (sonnet, worktree `pm/79-rulings-closeout-0907`). **Merged 11:16 after review** — two PM corrections (the two-GM remark was pinned to R-77's and R-56's cards instead of the bench row; the open-rulings test hard-coded exactly one open ruling); found in passing: `parseArtWishlist` had no done-detection at all (added, `**DONE …**`), and a REOPENED ruling does not resurface on the phone's Needs-you card → item 85; 6.5M / 272 turns | R | sonnet | S | — | merged | #273 |
| 74 | **80** R-18 — the Heroic quarry advantage joins the next-test LIST (item 49's writer) instead of stomping the slot, so it cancels against a Weakened disadvantage per SR p.18 / R-80; pin both orders; one 🤖 row. ENGINE-ONLY (F5) | B | opus | S | R-18 ✓ · 79 | queued | |
| 75 | **81** R-82 (a) — the per-rule `onGraze` dial on `edha-damage-bonus` (item 56's shape), every authored card audited; an authored value change is a REBUILD — the PM sets the deploy class from the card list in the PR | B | opus | S | R-82 ✓ · 56 ✓ · 79 | queued | |
| 76 | **82** Adversary statting standard writes senses + speed per block (Ben 09-07: the bestiary skill should stat them so tokens inherit sight and speed); a lint; the values for the 52 existing blocks are invented content → ONE approval batch to Ben, then an adversaries REBUILD | H | sonnet | M | 83 · Ben's batch | blocked(83) | |
| 77 | **83** R-56 REVERSAL — the cosmere senses ladder for everyone (drop the Edha AWA table from `edhaDeriveSheetStats`, `advSensesRangeFt` in the build → adversaries REBUILD, the wizard preview, bench-setup PC sight, `Character_Building_Rules.md` §Senses Range, `ACTOR_STAT_DERIVATION.md`, the `edhaSensesRangeFtFromAwa` pins). Scoped in chat 09-07; Ben asked to be told before changing — **WAITING on his go** | H | opus | M | **R-56 (Ben)** | blocked(R-56) | |
| 78 | **84** Weapon-borne `edha-on-hit` rules never dispatch — `edhaRulesForEvent` (`engine/04-black-ritual.js:206`) still gates on `edhaIsTalent`; swap to `edhaRuleBearer` (34a's widening, applied next door), audit its three other callers, pin (fails on `edhaIsTalent`, passes on `edhaRuleBearer`); blast radius 6 shipped cue rules. ENGINE-ONLY (F5). Dispatched 12:15 (opus, worktree `pm/84-weapon-on-hit`). **Merged 12:22 after review** — one predicate swap, two mutation pins (1019/2 fail → 1021/0), the old test's weapon decoy corrected, the three other callers widened by the same swap and pinned, `preUseItem` keeps `edhaIsTalent` on purpose; found: `edhaListPlaceNotes` has the same gate (0 consumers) → item 86 seed. **ENGINE-ONLY → pushed live 12:23, Ben F5'd 12:25**; 🤖 cue re-test in bench 43; 1.8M / 117 turns / 11 min | B | opus | S | #274 ✓ | bench-pending | #276 |
| 79 | **85** The phone's Needs-you card cannot resurface a REOPENED ruling — `parseOpenRulings` closes any ruling whose body carries an older ANSWERED/VETOED/SETTLED marker (R-56 today); make the LAST marker win (REOPENED after ANSWERED = open), pin on R-56's real text, and fix the Art tab's stray-`*` bold garble found by item 79. TOOLING (dashboard build) + tests. Dispatched 12:15 (sonnet, worktree `pm/85-reopened-rulings`). **Merged 12:31 after review** — last-marker-wins in `parseOpenRulings` (R-56 open again: `[R-56, R-83, R-88, R-89]`), code spans protected from the bold pass in `scripts/lib/md.js` (the canon codex had the same garble on one paragraph — fixed by the rebuild), three pins + mutation both ways; found: `RULING_DEFAULT_RE` misses the bare `*Recommended*,` style so R-56's card says "no default stated" → item 87 seed. TOOLING-only; 5.6M | R | sonnet | S | #273 ✓ | merged | #277 |
| 80 | **Bench run 43** — the 20 remaining 🤖 rows per `docs/BENCH_NEXT_RUN.md` (owned-copy step zero, the player-client block, the hygiene block, the combat pair, three bestiary singles) + item 84's live cue re-test (Surecat's Pounce and Fellstag's Antler Sweep, no `adversaryTalent` flag). Dispatched 12:30 (`bench-run`, opus, worktree `pm/bench-43`) on served `0a677dad…` after Ben's F5. **Merged 13:23 after review** — 10 retired on evidence (item 84's cue on TWO weapons with no flag; 34b's body half from `PlayerBench`; R-64 ×2 overturned by a staged clone; R-63; R-61; CAE burns; Kindle token-light; Reckless Advance 20.0 ft; Pyre spread), 3 left open with the blocker named (Predator's Due audience → item 88 / R-90; Unbreakable Line (b) → item 89; R-62 audience → R-91), world diff EMPTY; 11.1M / 403 turns / 54 min | B | opus | L | #276 ✓ · F5 ✓ | merged | #280 |
| 81 | **86** (seed — entry to be filed by its worker) `edhaListPlaceNotes` (`engine/07-edha-owner-list.js` ~L230) harvests `placeNote` behind `edhaIsTalent` and would drop a weapon-borne placeNote the way item 84's dispatcher did; 0 shipped consumers; same one-line swap to `edhaRuleBearer` + pin, plus a sweep of every remaining `edhaIsTalent(` call with a verdict each. ENGINE-ONLY. Found by item 84. Dispatched 13:22 (sonnet, worktree `pm/86-place-notes-rule-bearer`). **Merged 13:40 after review** — one predicate swap, four pins (mutation: 1026/1 → 1027/0), all 37 remaining `edhaIsTalent(` calls verdicted (25 talent-use automation, 9 other surface, 0 further harvest loops); the PM resolved a changelog conflict on the branch. **ENGINE-ONLY → pushed live 13:40**; no bench row (0 consumers); 3.3M | B | sonnet | S | #276 ✓ | merged | #283 |
| 82 | **87** (seed — entry to be filed by its worker) `RULING_DEFAULT_RE` in `scripts/build-dashboard.js` captures only the colon form (`*Recommended default: …*`), so rulings written in the bare `*Recommended*, and it …` style (R-56 and a handful of others — lines ~16 / 879 / 1432 of `EDHA_RULINGS.md`) render "no default stated" on the phone card; widen the regex or normalise the doc, pin on R-56's real text. TOOLING. Found by item 85. Dispatched 12:45 (sonnet, worktree); **merged 12:46 after review** — additive `bareRecommendedDefault()` fallback anchored to the `**(x)**` label before a bare `*Recommended*`, three pins, every previously-captured default byte-identical; 2.8M / 151 turns / 12 min | R | sonnet | S | #277 ✓ | merged | #279 |
| 83 | **88** Every `edha-triggered-effect` card is PUBLIC by construction — `edhaRollCard` (`engine/33-triggered-effect-resolution.js:143`) and its seven call sites pass no `whisper` key, so an adversary's kill-heal arithmetic and its GM-only instruction (Predator's Due ×3) print to the table; 17 adversary rules; run 16's `edhaWhisperIds` diagnosis corrected. Fix = whisper to `edhaWhisperIds(owner)` when the owner has no player OWNER (R-90 default (a)); pin. ENGINE-ONLY (F5). Filed by bench 43 | B | opus | S | R-90 ✓ (a, phone 09-07 17:25) | queued | |
| 84 | **89** `Unbreakable Line` ships no `use` rule on either block (Crownox Ring, The Reckoning) — the card's own "test White (DC = half the damage) via the contest core" has never existed; plus `activation.type` `none` vs `utility` on the same ability. Author the contest-core `edha-def-test` rule on both + align activation. DATA (`data/adversaries.json` + overlay) → REBUILD + ⟳ Sync Adversaries; 🤖 row (b) half. Filed by bench 43. Dispatched 13:35 (sonnet, worktree `pm/89-unbreakable-line-use-rule`). **Merged 13:54 after review** — `use → edha-def-test {skill: white, vs: prompt-dc, requireTarget}` on both blocks (the Callthief's Counterpoint shape; `prompt-dc` because H1's `dc` is a flat number and "half the damage" varies), The Reckoning's stray `kind: trait` → `cost: Special` / `consume: 3 Focus`, both build to `activation.type: skill_test` (the worker overrode the brief's advisory "utility", correctly); pack built and read back from LevelDB; gates + `--ci` green; 4.1M. **REBUILD + ⟳ Sync Adversaries owed** | B | sonnet | S | #280 ✓ | bench-pending | #285 |
| 85 | **90** Retire the five harness-only rows — I10b2-1..4 and 77-1's unset-side clause: runs 38, 42 and 43 each re-derived that no sideless token can exist on this build (null / undefined / NaN dispositions coerce to −1 at create and update), so the observable cannot occur live; `tests/disposition-failclosed.test.js` is the standing record. Checklist edit + delta; the PM's call on the bench's recommendation. DOCS-ONLY. Dispatched 13:28 (sonnet, worktree); **merged 13:32 after review** — four checkbox rows + the 77-1 bare-bullet row retired with the harness citation (`tests/disposition-failclosed.test.js`, 23 cases); dashboard 🤖 9 → 5; 2.4M / 155 turns / 8 min | R | sonnet | S | #280 ✓ | merged | #282 |
| 86 | **92** R-88 (a) — Volatile Strike drops its `whenDamageType: "impact"` gate so the `edha-on-hit` rider fires on ANY melee hit, as the card says (one authored field + the rule's description string in `data/authored/leyline-red.json`; parity: exactly one doc differs); 🤖 row = bench 42's keen-hit negative case, flipped. **REBUILD (leyline) + ⟳ Sync Talents.** Filed 17:2x from Ben's dashboard paste | B | sonnet | S | R-88 ✓ (a) · merged 17:29, REBUILD owed (leyline + ⟳ Sync Talents) | bench-pending | #288 |
| 87 | **93** R-89 (a) — the `NO NAMEABLE HOOK` declaration leaves the description (17 comments in `data/adversaries.json`) for a `noHook` key → `flags.edha-content.noHook` on the built doc; lint pass 5 reads the key and now ERRORS on the prose form; validators type-check it; extract path audited; docs sweep (CLAUDE.md, leyline-tree-authoring, R-47's clause); parity (17 docs, comment out + flag in, 0 other) + two lint mutations pinned; 🤖 row = editor save round-trip keeps the flag. **Adversaries REBUILD + ⟳ Sync Adversaries.** Filed 17:2x | B | sonnet | M | R-89 ✓ (a) | queued | |
| 88 | **91** Rulings close-out 2026-09-07 evening — R-83 / R-88 / R-89 ANSWERED (a) with Ben's verbatim text and their item numbers (70 / 92 / 93), the four ⚑ wizard-v2 rows retired on his PASS marks (Coin row v3 "looks good to me!", weapon picker, preview centered, map-picker dead spots), R-56 left WAITING, dashboard rebuilt; item 79's shape. **DOCS-ONLY.** Filed 17:2x | R | sonnet | S | — | queued | |
| 89 | **94** The mobile board re-renders the whole page on every store snapshot — `tick()` calls `renderNeedsYou` every 30 s and `pm/state` pushes call `render()`, so Ben's DOM-only "Sent ✓" pills vanish and the page jumps to the top (Ben, chat 17:3x). Fix: "sent" derives from the inbox store (Sent ✓ / Recorded by PM ✓), targeted re-renders with scroll preserved, `tick` touches clocks only, a hint that PC-dashboard marks arrive on the next push; three headless proofs. **TOOLING-only + PM republish of the artifact.** Filed 17:40 | R | sonnet | M | — | queued | |
| 90 | **95** Rulings close-out part 2 — R-90 (a) and R-91 (a) from the phone inbox (17:25) recorded in `EDHA_RULINGS.md`; the R-62 audience row retired under R-86; dashboard rebuilt. Same files as item 91 → branches from the `main` that has 91. **DOCS-ONLY.** Filed 17:40 | R | sonnet | S | 91 | queued | |

## Foundry windows

**✅ OPEN 2026-09-07 from the morning — Ben ran `deploy-to-foundry.bat` and relaunched: LIVE = `main` at `14f2aa9`, served engine `609c7e45…` = repo, 6 in sync, 0 stale (verified 09:55 ET by hash and `module-src-sync.js status`). The whole REBUILD stack is at the table for the first time. Bench run 42 dispatched 10:45 (worktree); Ben's `Gamemaster` client is connected, so the bench is the second GM client (R-86: that is a bench condition, never a play defect).**

**Bench run 42 done 12:05 — no deploy owed (DOCS-ONLY), but ⟳ Sync TALENTS is owed by Ben:** the adversary sync had run, the talent sync had not, so his own PCs' owned copies are still pre-items-56/58/63 (the bench synced only its 16 Bench PCs, per-actor). Item 84's fix will be ENGINE-ONLY (F5) when it lands.

**✅ ENGINE PUSHED 12:23 — LIVE = `main` at `1c52cfd` (#276), served hash `0a677dad…` equal both sides, 6 in sync, 0 hand-edited (`module-src-sync.js push`, 1 copied; safety copy under `%TEMP%\edha-engine-backups\2026-09-07T16-23-06`). Ben pressed F5 at 12:25. Bench run 43 dispatched 12:30 on that engine.**

**Bench run 43 done 13:23 (#280) — no deploy owed; the bench queue is DRAINED.** The 10 open 🤖 rows are all gated: Predator's Due audience (item 88 ← R-90), Unbreakable Line (b) (item 89, REBUILD), the R-62 audience row (R-91), the five harness-only rows (item 90), and the rest wait on fixes not yet shipped. Item 86's engine change goes live with one `module-src-sync.js push` + F5 when it lands. Ben still owes ⟳ Sync Talents for his own PCs.

**✅ ENGINE PUSHED 13:40 — LIVE = `main` after #283, served hash `a23e70d1bad4…` equal both sides, 6 in sync, 0 hand-edited (item 86's `edhaListPlaceNotes` widening). Ben owes an F5 (and still the ⟳ Sync Talents click).**

**✅ DEPLOYED ~17:03 ET by Ben — `deploy-to-foundry.bat` rebuilt all five packs (pack dirs stamped 17:03, after item 89's 13:54 merge): Unbreakable Line's `use` rule is at the table; Ben pressed F5 (item 86 live) and clicked ⟳ Sync Adversaries + Players. Engine 6 in sync (served `a23e70d1…` = `main`), Foundry up (302 at 17:12). NOTHING was owed to Ben's machine as of 17:12. ⛔ Since 17:29 a leyline REBUILD + ⟳ Sync Talents is OWED (item 92, #288 — Volatile Strike's gate); item 93 will add the adversaries pack + ⟳ Sync Adversaries when it lands — ONE `deploy-to-foundry.bat` run covers both, tomorrow morning at the earliest, Ben only.** Item 89's 🤖 (b) row is un-gated → bench run 44 (Opus, after 00:00 ET). The next REBUILD stack is items **92** (leyline pack + ⟳ Sync Talents) and **93** (adversaries pack + ⟳ Sync Adversaries) when they land tonight — one bat run tomorrow morning covers both. _(Replaces: ⛔ A PACK REBUILD IS OWED (item 89, #285, 13:54) … the 🤖 row's (b) half re-drives after it.)_

**✅ OPEN from 2026-09-06 19:00 ET — LIVE = `main` at `dd1e1c3` (#216), engine hash `7d8e0226…` equal both sides, 6 in sync; fix pass 7a (#215) is at the table.** Ben ran `deploy-to-foundry.bat` at 18:59 (chat: "deploy ran @18:59 and foundry is open, adversaries synced") and relaunched Foundry, so his `Gamemaster` client is fresh (the R-77 blocker is gone) and the adversaries pack is rebuilt + synced. **Bench run 39 dispatched 19:03** (worktree) for the 27 open 🤖 rows — fix pass 7a's re-tests first. **✅ ENGINE PUSHED 20:16 — LIVE = `main` at `2d9631d` (#233), hash `0ea0741a…` equal both sides, 6 in sync** (`module-src-sync.js push`, 1 copied; safety copy under `%TEMP%\edha-engine-backups`). Every engine-only merge of the evening is at the table: fix pass 7b, 53, 49, 50, 52, 54, 66, 51, 34b. Ben's `Gamemaster` client runs the old engine until he presses F5. **Bench run 40 dispatched 20:17** for their 🤖 rows. **✅ ENGINE PUSHED 22:17 — LIVE = `main` at `53dd0bc` (#253), hash `f2fb3e2d…` equal both sides, 6 in sync** (`module-src-sync.js push`, 1 copied; safety copy under `%TEMP%\edha-engine-backups`; Foundry was up and Ben had not yet run the bat, so the engine-only merges went live on their own class): 69, 68 (fix pass 8), 24, the 55 / 56 engine halves, fix pass 9. **Bench run 41 dispatched 22:18** for their 🤖 rows (registry smoke, fix pass 9's four, 2bJ-10, the Verdict fold, the heal cards); the REBUILD rows stay BLOCKED-ON-DEPLOY. **Bench 41 reported 23:1x — 25 rows retired, 1 FAIL (fix pass 10).** **✅ ENGINE + ASSET PUSHED 23:21 — LIVE = `main` at `6e657c3`, engine hash `ee29b118…` and `thyrcross-nations.json` `c81a388b…` equal both sides, 6 in sync** (items 71, 75, 10 batch 2 and 61's map asset went live once bench 41 was done). **✅ ENGINE PUSHED 23:35 — LIVE = `main` at `1cf0fc7` (#267), hash `78520e49…` equal both sides, 6 in sync** (item 77). **✅ ENGINE PUSHED 23:50 — LIVE = `main` at `9f3f529` (#269), hash `609c7e45…` equal both sides, 6 in sync** (fix pass 10). Every engine-only merge of the night is at the table; nothing engine-side is owed. **⏳ OWED: `deploy-to-foundry.bat` (packs REBUILD + ⟳ Sync — and the bat copies the engine) for 34a #220, 57 #226, 58 #227, 65 + 67 #232, 59 #234, 63 #239, 55 #240, 56 #242, plus the engine-only merges since the 20:16 push: 69 #237, 68 #241, 24 #244** — one run covers them all; **Ben was push-notified ONCE at 21:33** (bench 40 had reported); until he confirms, their 🤖 rows are BLOCKED-ON-DEPLOY, not defects, and bench run 41 waits. _(Earlier:)_ **❌ CLOSED at 16:56** (refused; last seen up at 08:30); #215 sat on `main` un-deployed from 18:33 to 18:59. Tonight's engine-only passes deploy by `module-src-sync.js push` when Foundry is next up (F5 on Ben's side); their 🤖 rows wait for that window. _(Earlier:)_ **✅ LIVE = `main` at #204, hash `57a8c950…` (PM push 2026-09-06 03:47, 6 in sync, equal both sides; adds fix pass 6 to `39cc7612…` of 03:27; verified by run 37). Nothing owed to `deploy-to-foundry.bat` — no pack rebuild all weekend.** ⚠️ Ben's `Gamemaster` client has been connected since before 23:28 and runs a pre-fix engine until it reloads. _(Earlier:)_ `1496af37…` at 01:24 (28a + 28b). _(Earlier:)_ `0051bde1…` at 00:49 (item 29, verified by run 33). _(Earlier:)_ `06229ecc…` at 00:14 (fix pass 4, verified by run 32); `ffb1f3f5…` at 23:53 (fix pass 3 + item 23). _(Earlier tonight:)_ fix pass 3 pushed 23:28 as `c498d9eb…`, verified by bench run 31. _(19:10 fact:)_ **✅ #160 LIVE (verified 2026-09-05 19:10 from the new clone: served and repo `register-skills.js` both `6fc09da0…` CRLF-normalised, `module-src-sync.js status` 6 in sync, `localhost:30000` answers 302).** _(18:35 note, now history:)_ **⏳ DEPLOY OWED (2026-09-05 18:35, PR #160, ENGINE-ONLY):** item 36's one moved statement is on `main` but not live — `module-src-sync.js push` + hash verify from the NEW clone (`C:\dev\Skilltrees`) is the next session's first act if Foundry is up; the 🤖 row under `# BENCH — Sovereignty` waits on it. Ben closed Foundry for the OneDrive move (~18:30). _(Previous fact:)_ **✅ DEPLOYED (2026-09-05 ~16:15, Ben, `deploy-to-foundry.bat`, Foundry closed): #151's engine half and the Reeve-Owl data fix are LIVE.** PM verification 16:20: `module-src-sync.js status` → **6 in sync, 0 stale, 0 hand-edited**; repo engine blob `a59b0c41…` at `main` = `7b4bcc8`; Foundry relaunched, `localhost:30000` answers. The run was the **live confirmation of #139's step 2** in the sense that it completed, but note how it got there: Ben's checkout was still pre-#139 when he double-clicked, so the OLD 7-step script ran and hung on the same worktree-prune `(y/n)` prompt (this time a half-removed `.git/worktrees/hw` record from a ~13:58 worker). The PM removed the record, had Ben close the window rather than answer `y` (the pull would have rewritten the running script under `cmd`, which resumes by byte offset), pulled `main` in the checkout, and Ben re-ran the new 8-step script clean. **Lesson for the runbook: a deploy-script fix only protects the run AFTER the one that pulls it — when the script itself changes, pull first, then run.** Bench run 4 verifies the served hash before driving anything.

**OPEN — Saturday 2026-09-05 from 09:13** (Ben: "foundry has been updated and is open"). **Deploy
fact (2026-09-05 11:50, PM push after fix pass 1):** `module-src-sync.js push` → 1 copied; live and repo
`register-skills.js` both hash `9575fba2…`; status 6 in sync. _(09:13 fact: 6 in sync at `9027cd17…`.)_ The
2026-08-10 hygiene campaign (R-60..R-67, live dice math) is now LIVE at the table for the first
time — its `# BENCH — hygiene campaign 2026-08-10` section (checklist line ~3328) is the first thing
the bench should run. The checklist carries **106 🤖 rows** in total. The dispatch itself waits for
the 12:05 slot (two dispatches already in the trailing five hours) and for Foundry still being up
then — **superseded 10:40: Ben raised the weekend cap and asked for bench progress while he is away;
the first bench run was dispatched at once.** The weekend runs as a **bench marathon** (the
`bench-marathon` skill's loop, orchestrated by the PM): bench-run → fails to one `test-pass-fixes`
worker → PM deploys engine-only fixes with `module-src-sync.js push` and verifies by hash → next
bench run re-tests the restored rows first. Pack-rebuild fixes stay BLOCKED-ON-DEPLOY for Ben.
Lane-B items merge to `main` but stay `bench-pending` until a window; the PM then dispatches
one `bench-run` worker (Opus) for the accumulated 🤖 sections, and fails go to one
`test-pass-fixes` worker.

_(Previous fact, for the record: on 2026-09-04 the live engine was the 2026-07-28 version and
`deploy-to-foundry.bat` had not been run; Ben ran it on 09-05 — it hung on git's worktree-prune
prompt on the way, which he is fixing in his own session in the `focused-booth-7259bf` worktree.)_

## Run log

| Date | Item | Model | Duration | Weighted usage | Outcome | PR |
|---|---|---|---|---:|---|---|
| 2026-09-04 | Review (Fable + 4× Opus survey) | fable/opus | ~45 min | 7.0M | Report published; items 15–25 filed | #130 |
| 2026-09-04 | Board + rulings + the two skills (PM, no worker) | fable | ~30 min | — | project-manager + work-item skills written; six rulings answered | #131 |
| 2026-09-04 18:41 | #25 PM tooling (script + Project tab) | sonnet | 15.5 min, 181 turns | 4.4M | merged after review; 4 trailers stripped; 2 out-of-scope finds → item 21 | #132 |
| 2026-09-04 19:02 | #15 Pre-commit shim + reinstall | sonnet | 7 min, 73 turns | 1.3M | merged after review; clean first pass, no trailers | #133 |
| 2026-09-04 19:45 | PM handoff | fable | — | — | loop stopped: window cap reached (2/2), quiet hours next; `edha-pm-daily` created, first run 2026-09-05 ~07:02; next dispatch is #16 (sonnet, S) | #134 |
| 2026-09-04 19:30 | Mobile PM board (Ben's request; PM built it, no worker) | fable | ~40 min | — | `scripts/pm-state.js` + `tests/pm-state.test.js` + `docs/pm-board-mobile.html`; artifact published, `pm/state` seeded; skill gained the push/inbox procedure. **Merged 2026-09-05** as PR #135 (`main` at `ac170d0`) — the earlier "Not merged / needs Ben" note in this row was corrected by the 09-05 session | #135 |
| 2026-09-05 07:05 | #16 Build fails loudly on a broken overlay | sonnet | 10.9 min, 171 turns | 3.1M | merged after review; clean first pass, no trailers, no bounce. PM re-verified the mutation itself (reintroducing `catch { continue; }` → "Missing expected exception"), re-ran all 8 gates locally green, CI green in 28s | #136 |
| 2026-09-05 08:35 | Phone inbox: R-1, R-2, R-4 answered (PM bookkeeping, no worker) | fable | ~20 min | — | Ben answered three standing rulings from the mobile board. Recorded inline in `EDHA_RULINGS.md` (NOT moved to §K — the doc's own rule is that a ruling is settled only once the thing it decides has changed); filed the consequences as TODO items **26** (R-2 bench vision), **27** (R-1 retire the summon-relay row), **28a/28b** (R-4 out-of-combat scope, split before dispatch). R-4 is lane B and cannot be called done without a bench pass | #138 |
| 2026-09-05 07:30 | #17 Heroic ids into `data/` | sonnet | 11.6 min, 174 turns | 3.3M | merged after review, no bounce. **The item's premise did not survive measurement**: the map is fully dormant, so shipping content is unchanged everywhere — see PM-D1 for the deploy-class call. PM re-derived the collision count through `buildTrees()` (79/82 confirmed) and found the worker's 3 "non-colliding" names are punctuation variants (`Erudition*`, U+2019 apostrophe, hyphen) of talents that DO exist → all 82 are dormant; PM corrected that paragraph in the delta itself rather than spending a bounce. Snapshot has punctuation drift vs `data/` — noted for any re-dump | #137 |
| 2026-09-05 09:13 | New PM session of record (Ben's chat); phone inbox: R-5, R-7, R-19, R-34, R-49 answered; schedule re-cut to nights and weekends (PM bookkeeping, no worker) | fable | ~35 min | — | Ben confirmed both things the board waited on: the engine is deployed (sync status 6/6) and Foundry is open. Five rulings recorded inline in `EDHA_RULINGS.md`; consequences filed as TODO **29** (R-5, lane B: line zones hit allies) and **30** (docs close-out of the four confirmations; R-34 read as "the trail Regions are the indicator" — flagged for Ben's correction). **PM-R7**: operating windows moved to weeknights 21:00–07:00 + weekends, weekday daytime is Ben's; scheduled tasks re-cut (`edha-pm-daily` → Sat/Sun 07:00, new `edha-pm-weeknight` → Mon–Fri 21:00, both with a late-fire clock guard). The 07:02 daily session could not be messaged (unattended); the board's top line tells it to stand down. Trailing-5h cap is full until 12:05 → next dispatch then: the bench run if Foundry is still up, else item 26 | (this PR) |
| 2026-09-05 10:52 | Bench run 1 = bench run 24 (hygiene campaign + engine-wide, 37 🤖 in scope) — `bench-run` | opus | 39 min, 275 turns | 7.4M | merged after review; one commit trailer stripped by the PM. Deploy hash-verified by the worker. 14 rows touched: **10 PASS retired, 2 FAIL** (Apex Form double-fire; `edhaDialogPick` Cancel returns its action string), **1 new defect** filed as a 🤖 row (R-60 sweep writes ~40 keys to every actor, tripped the socket limiter), 23 not reached (all left 🤖). World restored to snapshot: 0 diffs, Bench logged out. Worker corrected the PM's sizing (Heroic = 1 🤖, not 54). Dispatched at 10:52 after Ben's 10:40 message; weekend cap raised (PM-R7 amendment) | #142 |
| 2026-09-05 11:33 | Fix pass 1 — run 24's three defects — `test-pass-fixes` | opus | 18.5 min, 161 turns | 3.4M | merged after review, no bounce, no trailers. All three root causes verified in source; `edhaDialogPick` fixed once at the primitive (a `{edhaPick}` box that survives DialogV2's `?? action`), `edhaSceneReset` gained a per-actor claim across combats and an `edhaFlagKeyPresent` gate; 3 pinned tests (550 total, mutation-verified); 3 🤖 re-test rows; ENGINE_INDEX updated. Worker corrected the bench's blast radius (edhaPromptDC's two buttons were not live defects; the Weave cancel was worse than reported). **ENGINE-ONLY — deployed by the PM at 11:50 via `module-src-sync.js push`, hash-verified equal** | #143 |
| 2026-09-05 11:55 | PM lesson (no worker) | fable | — | — | #142 was merged before CI had finished on its amended (trailer-stripped) commit: `gh pr checks --watch` returned the OLD run's green. Content was byte-identical and the main run went green, but the rule is now explicit: **after any force-push, wait for the run on the NEW sha** (`gh run list --branch <b>` shows it). Also: PM session cost so far 6.7M / 214 turns — most of it the two long-report reviews | |
| 2026-09-05 12:08 | Bench run 2 = bench run 25 (re-test fix pass 1, then hygiene part 2) — `bench-run` | opus | 45 min, 396 turns | 10.6M | merged after review, no bounce, no trailers. Deploy hash-verified (installed AND served, after CRLF normalisation — a raw-bytes hash reads a good deploy as bad on Windows; runbook lesson). **All three fix-pass-1 rows PASS** with negatives (one card/one injury; Cancel refunds 4→1→4 with no flag; 4 of 74 actors written, none of Ben's PCs). 11 retired incl. both remaining R-60 rows (**R-60 closed**), R-64, four R-65 families; 4 partial with the open half written on the row; 2 BLOCKED (need zero GM clients — Ben's Gamemaster client was connected). 0 FAIL → no fix pass this cycle. New low-severity 🤖 row (system's own damage card prints the unfolded formula; maths correct). **New ruling R-69** for Ben. World restored: 3 pre-existing Covenant effects the sweep ate were recreated with original ids; final id-diff clean; roster idempotency 0 ⚠ / 0 created; Bench logged out | #145 |
| 2026-09-05 12:57 | Bench run 3 = bench run 26 (engine-wide 2 + leyline scatter 12 + hygiene remainder) — `bench-run` | opus | 52 min, 358 turns | 9.5M | merged after review, no bounce, no trailers. Deploy hash-verified (installed + served-normalised). 11 PASS retired (both engine-wide rows, Flashpoint, Living Image, The Seeming, three Green rows, Devoted Conduit, White burst riders, Covenant icon, Probability Cascade); **1 FAIL** Spreading Roots — `edhaGrowTerrain` deep-clones DataModel shape instances so `region.update` diffs to nothing while the Drawing grows (measured by re-running the path live); **1 data anomaly** Reeve-Owl / Sovereign of Solitude builds with `system.events === {}` against four authored rules, stale pack ruled out by six correct siblings; 2 PARTIAL; veil half BLOCKED (Playtest Map has darkness 0 + global light — needs a bench-created scene). Green's stale "needs an Opportunity" blocker struck. **Per-tree `# BENCH —` block exhausted.** World restored: 3 Covenant effects recreated with original ids, final diff empty; roster 0 ⚠; logged out | #147 |
| 2026-09-05 13:44 | Ben: "I'm authorizing Fable agents and increased usage for weekend sprints. Fable subagents are limited in effort to medium." → **PM-R8** (PM bookkeeping, no worker) | fable | — | — | `fable-worker` agent definition written (user-level, mirrored into the repo); board + skill amended; weekend caps 6/5h, 12/day; one parallel lane-R worktree worker allowed. The Fable agent type is visible only after a restart — this session keeps dispatching Opus/Sonnet | (this PR) |
| 2026-09-05 14:00 | **PM handoff** (Ben: "Stop after this turn and I'll continue the project manager weekend sprint in a new session") | fable | session ~4h50m, 9.7M weighted so far | — | Loop stopped. **IN FLIGHT at 14:00, both dispatched ~13:55 under PM-R8:** (a) **fix pass 2** — Opus, `test-pass-fixes`, branch `pm/fix-pass-2-run26` (not yet pushed at 13:57; the worker holds the MAIN checkout); (b) **item 31** — Sonnet, `work-item`, in worktree `.claude/worktrees/agent-a100f1efd45d7e319`, branch `pm/31-mobile-board-windows`. **Next session:** `gh pr list` — if their PRs exist, review per the checklist (rebuild `EDHA_DASHBOARD.html` at merge, both touch it); if a branch never appeared, the worker died with the old session → set the row back to `queued` and re-dispatch. Fix pass 2's engine-only half needs the PM's `module-src-sync.js push` + hash verify before bench run 4; its Reeve-Owl half is probably **REBUILD** (Ben's `deploy-to-foundry.bat`) → its row stays BLOCKED-ON-DEPLOY. **Bench run 4** = `# Adversary ability wiring` (12 🤖) per `docs/BENCH_NEXT_RUN.md`; per-tree block is exhausted. After item 31 merges, republish the mobile page to the existing artifact URL. Trailing-5h dispatches at 14:00: 10:52, 11:33, 12:08, 12:57, 13:55, 13:56 = **6 of 6** → next slot 15:52; day-shift 9 of 12. **`fable-worker` became visible in the 09:13 session WITHOUT a restart** (the earlier "restart needed" note was wrong) — usable now. Waiting on Ben, not blocking: **R-69**. Foundry still up at 13:57; Bench logged out by run 26. PM-side lesson: a PM worktree must live at a SHORT path (`C:/tmp/…`) — the scratchpad path + this repo's long filenames exceed MAX_PATH | (this PR) |
| 2026-09-05 14:30 | **New PM session of record — status wake** (Ben: status report first, then the queue; cloud session) | fable | ~40 min | — (cloud; no transcripts for `pm-usage.py`) | Both 13:55 workers **survived the 14:00 handoff** and opened their PRs at 14:13 (#150) and 14:20 (#151) — a stopped PM loop does not kill background workers. #151 CI green + mergeable (the worker merged main and rebuilt the dashboard itself); #150 conflicts on `EDHA_DASHBOARD.html` only, and GitHub runs no `pull_request` workflow on a conflicting PR, hence 0 check runs; #139 (Ben's deploy-script fix, CI green 13:25) conflicts on the handoff + dashboard. Phone inbox: the PM-R8 note marked seen (already recorded). Wrote `docs/REPO_MIGRATION_BRIEF.md` + TODO **32** (OneDrive → SSD) at Ben's request; the phone board still shows the 14:00 handoff picture until the next push. Trailing-5h cap 6 of 6 until 15:52; day-shift 9 of 12 | (this branch) |
| 2026-09-05 14:44 | Fix pass 2 (#151) — review + merge (PM, cloud) | fable | — | — | merged after review; diff read with the generated dashboard excluded (engine 72 lines: one source reader `edhaRegionShapes` behind all three Region shape writers; data: one enum value; lint pass 9b derives every closed `choices()` set from the engine — 88 handler types, 137 fields; 13 pinned tests, 563 total). The worker had already merged main and rebuilt the dashboard itself; no trailers, no bounce. **DEPLOY OWED to Ben** (engine + adversaries REBUILD) — a cloud PM cannot push the engine | #151 |
| 2026-09-05 14:52 | Item 31 (#150) — merge (PM, cloud) | fable | — | — | merged after review. The PR sat with 0 check runs because GitHub runs no `pull_request` workflow on a conflicting PR; the PM merged main in a worktree, regenerated the dashboard, CI green in 28 s on the new head. Stale "OLD quiet hours" board note retired, TODO 31 checked, mobile page republished to the existing artifact | #150 |
| 2026-09-05 14:56 | Ben's deploy-script fix (#139) — merge (PM, cloud) | fable | — | — | merged after CI green. Needed TWO main merges: the first resolved the handoff (#139's delta placed in date order beneath run 24) and the dashboard; #150 landing in between re-conflicted the dashboard → second merge, rebuild, CI. Live evidence for the "generated dashboard collides on every parallel PR" finding. Ben's next deploy is the live confirmation of step 2 | #139 |
| 2026-09-05 15:00 | **PM handoff** (Ben: "let me know when there is no agents or work going on… I'll start another real PM session at my PC") | fable | ~30 min PM work | — (cloud) | Loop stopped. **Nothing running**: no worker dispatched this session, no wakeup scheduled, no routine armed, all three PRs merged, all worktrees removed. Open PRs left: **#103 and #93** (July, untouched since 07-28 — Ben's call: close or rebase). **Deploy owed**: #151 → `deploy-to-foundry.bat` with Foundry closed. **Next local session**: verify the deploy by hash, bench run 4 (`# Adversary ability wiring`, 12 🤖) plus the three #151 re-tests, then item 32's path-literal PR (lane R, sonnet, S) as the first repo dispatch. Trailing-5h dispatches at 15:00: 10:52, 11:33, 12:08, 12:57, 13:55, 13:56 = 6 of 6 until 15:52; day-shift 9 of 12 | (this PR) |
| 2026-09-05 15:35 | Branch audit + items 33/34/35 (Ben: "add both as fresh to-dos", "make me a list of branches that can be deleted") — PM, cloud, no worker | fable | ~25 min | — (cloud) | **66 remote branches classified from git, not memory** → `docs/BRANCH_CLEANUP.md` (+ phone copy): 61 SAFE, 2 Ben's call, 3 KEEP. Main's history was **restarted 2026-07-28** (root `aed1a76`); 57 branches are orphaned by it and are what keeps the Stormlight PDF alive — item 2 rewritten accordingly. **Found while auditing:** `claude/in-app-dashboard-snapshot-ecwudz` (the Snapshot/Dashboard-on-the-phone work, 743 lines) was never merged and the PM's 14:55 page republish from main dropped those sections from Ben's phone → **item 35, first dispatch next session**. Items 33 (handout-forge from #93) and 34 (fleet weapons + loot caches from #103) filed at Ben's request; both source branches are KEEP until they land | (this PR) |
| 2026-09-05 16:10 | **New PM session of record — desktop** (Ben: "back on the desktop… I tried running the deploy to foundry.bat and it failed, again. do the fix… start working on what you can"). Deploy unblock + resume (PM, no worker) | fable | ~30 min | — | The deploy hung on git's worktree-prune `(y/n)` prompt — the pre-#139 script, because the bat pulls #139 *during* the run it cannot benefit from. Root cause on disk: `.git/worktrees/hw` half-removed (no `gitdir`/`HEAD`, empty `logs`/`refs`) by some ~13:58 process; maintenance's `worktree prune` hit the OneDrive lock on `logs`. PM removed the record, told Ben to close the window (not `y`: `cmd` resumes a rewritten batch file by byte offset), pulled `main` → `7b4bcc8`, pre-ran `module-src-sync.js status` (stale, not hand-edited), Ben re-ran → clean. Resume: tree clean, no open PM PRs, phone inbox empty, `#93`/`#103` still open from July | — |
| 2026-09-05 16:23 | Bench run 4 = bench run 27 (`# Adversary ability wiring` 13 🤖 + the three #151 re-tests) — `bench-run` | opus | 39 min, 301 turns | 6.9M | merged 17:05 after review; one `Co-Authored-By` trailer stripped by the PM (iron rule 6). Served hash verified = `a59b0c41…`. **14 PASS retired** (all three #151 re-tests incl. Reeve-Owl's 4 rules on a fresh import; the whole adversary-wiring block incl. Braced (b), which run 21 had wrongly BLOCKED — `game.combat` is the client's VIEWED combat), **0 FAIL**, 1 BLOCKED (Stalker veil: Playtest Map has darkness 0 + global light), 1 new 🤖 (Reknit Form's two-`consume` activation charged Inv, not Focus — not diagnosed). Harness finding: three Playtest-Map tokens are orphans (`actorId` resolves to nothing) → TODO **37**. World restore id-diff empty; residue: bench actors at full HP, Bench — White's temp HP gone, Bench — Green 2 Inv down | #157 |
| 2026-09-05 16:30 | Rulings batch (chat, 4 questions, all answered as recommended) — PM bookkeeping, no worker | fable | — | — | **R-69** = stamp after a successful pick → TODO **36**; **PM-R9** `.gitattributes` yes, in the item 32 PR; **PM-R10** tonight's day-shift ceiling 16; **close #93 and #103 now** → closed 16:35 with pointers to items 33/34 | (this PR) |
| 2026-09-05 16:38 | Items **32 + 11** (path literals onto `lib/paths.js`, `run-playtest-build.bat` relative, three prose paths, `.gitattributes`) — `work-item` | sonnet | 18.4 min, 263 turns | 5.0M | Dispatched in a worktree (PM-R8's one parallel lane-R slot) on branch `pm/32-onedrive-path-literals`; proof = scratch clone at `%TEMP%\edha-32-clone` printing the resolved DATA + pass-21 mutation → **merged 16:59** after review, no bounce, no trailers. Six scripts onto `lib/paths.js` (pass 21 shrink array empty, mutation-verified), `run-playtest-build.bat` → `%~dp0`, three prose paths, `.gitattributes` (renormalize touched exactly one file, the deity-revision-guide SKILL.md, CRLF→LF content-identical). Proof: the scratch clone printed its own `data` dir and built `edha-leyline` (136 items) into a scratch modroot. Cost confirms the calibration: a parity-class proof made an S+S item cost like an M. **Item 11 closed; item 32 is now Ben's (fresh clone, `core.autocrlf=false`).** PM note: `git worktree remove` on the worker's worktree failed with `Permission denied` on the dir AND its `.git/worktrees` record — the same read-only-directory mechanism as the deploy hang, leaving exactly the half-removed shape (`ORIG_HEAD`, `logs`, `refs`) `hw` had at 13:58; `rm -rf` + `git worktree prune` cleared it | #156 |
| 2026-09-05 16:55 | Board PR (session of record, deploy fact, rulings R-69/PM-R9/PM-R10, TODO 36) — PM, no worker | fable | — | — | merged after CI green on the trailer-stripped sha (the PM's own commit had carried a `Co-Authored-By` trailer; iron rule 6 wins over the harness default — stripped, force-pushed, waited for the NEW sha's run). Merge order #155 → #156 → #157, regenerating the dashboard at each step and keeping both handoff deltas newest-first at #157 | #155 |
| 2026-09-05 17:12 | Item **35** (re-land Snapshot + Dashboard on the mobile board over #150/#153) — `work-item` | opus | 13.6 min, 179 turns | 3.3M | merged 17:40 after review, no bounce, no trailers. A real merge of the old branch over #150/#153 (`caps.windows` and `item: null` intact, mutation-proven); 570 tests; the phone page's 582 HTML lines verified from the DOM. **PM republished the page to the existing URL at 17:50 and pushed `pm/state` + `dash/index` + `c0..c3` in one batch** — the phone has its Snapshot and Dashboard back. Out-of-scope finds: dispatch chips render a bare `#` for a run-log row without an item number (same family as #153); the PM skill still says "quiet hours" in one place. `claude/in-app-dashboard-snapshot-ecwudz` → SAFE in `docs/BRANCH_CLEANUP.md` terms | #159 |
| 2026-09-05 18:14 | Item **36** (a cancelled picker leaves no once-per-scene stamp — R-69) — `work-item` | opus | 16.6 min, 155 turns | 2.9M | merged 18:40 after review, no bounce, no trailers. Engine diff = ONE statement moved (`edhaDecreeUse`'s stamp below the `if (!proh)` refund guard); all 9 stamp sites and 5 picker callers inspected, exactly 1 affected (per-site table in the PR); 8-case headless regression + a generic pin (no function may reach a refund-and-return with a stamp behind it) — mutation 573/5 → 578/0. 🤖 row under `# BENCH — Sovereignty`. **ENGINE-ONLY, DEPLOY OWED** (not pushed — Ben is moving the repo). Out-of-scope finds → items **38** (TODO doc vs the 64 KiB stress cap — blocks filing) and **39** (`audit.py` exits 0 on a nonexistent tree). Main checkout; trailing-5h at dispatch 5 of 6; day-shift 13 of 16 | #160 |
| 2026-09-05 18:35 | **PM handoff — STOPPED on Ben's instruction** ("Once the current subs are done let me know and stop your next action" — he is cloning to `C:\dev\Skilltrees`) | fable | session ~2h25m; PM 9.1M / 286 turns; workers 18.1M (4 dispatches) | — | Loop stopped. Nothing running, nothing scheduled, all six PRs of the session merged, PM worktree removed, old checkout clean and pushed. **Deploy owed: #160 (ENGINE-ONLY)**. **Repo location is changing**: the next session starts in the new folder (Ben copies `.claude/settings.local.json`, `CLAUDE.local.md`, the memory folder, re-points the two scheduled tasks; `Worldbuilding\CLAUDE.md` is no longer an ancestor — copy to `C:\dev\CLAUDE.md`; `npm install --no-save classic-level@2.0.0`). Bench queue: 46 open 🤖 + the R-69 row. Day-shift 13 of 16 | (this PR) |
| 2026-09-05 19:07 | **New PM session of record — first in `C:\dev\Skilltrees`** (Ben: status report; "foundry is up, keep doing your thing"; wants to know whether the SSD move helped after the next round) — PM, no worker | fable | ~25 min | 0.8M (status wake) | Resume clean in the new clone (see the top line). #160 verified LIVE by hash → the deploy-owed note cleared. Status reported to Ben: bench 485 → 109 → 71 open rows (07-26 baseline → Sat 07:00 → now), 🤖 85 → 47 this weekend, ⚑ 22 unchanged; 14 dispatches since 09-04 with zero bounces; PM ≈ 41M vs workers ≈ 76M weighted since Thursday (a third of spend is management). **Lessons still only in this log, not in a skill:** "after a force-push wait for CI on the NEW sha" (PM skill) and "pull the deploy script before running it" (runbook); the PM skill still says "quiet hours" once. Old OneDrive folder still at its original name (Ben's); no `CLAUDE.local.md` in the new clone | (this PR) |
| 2026-09-05 19:35 | Bench run 5 = bench run 28 (R-69 row + Reknit Form + 07-28m re-tests + hygiene remainder + bestiary tail + veil scene) — `bench-run` | opus | 56 min, 430 turns | 12.4M | **merged ~20:30 after review**, no bounce, no trailers. Worker re-verified the served engine by hash from both sides (LF-only serve for the first time since `.gitattributes`). **12 rows off the checklist** (11 retired on evidence + 1 relocated to ruling R-71), **0 engine defects**, ⚑ untouched at 22, world restore id-diff EMPTY (the cleanest of the marathon). Two checklist rows had wrong contents and were corrected in place. New rulings R-70, R-71; out-of-scope find → TODO **40** (`Bench — Life` lacks `Mutation`). Worker resolved the predicted dashboard + handoff collisions with #162 itself. **Most expensive bench run yet** (~1.0M per retired row vs ~0.7M average) — the rebase and the long report are the difference. Dispatched in the MAIN checkout; served hash `6fc09da0…` verified by the PM before dispatch. Trailing-5h at dispatch: 16:23, 16:38, 17:12, 18:14 + this = 5 of 6; day-shift 14 of 16 | |
| 2026-09-05 19:36 | Item **38** (TODO doc → one dashboard section per item; re-pin the 64 KiB stress cap) — `work-item` | sonnet | 12.6 min, 175 turns | 3.2M | **merged 19:35 after review**, no bounce, no trailers. `parseRepoHygiene()` → one section per `## N.` item; the stress cap now derives from the largest REAL section (65 443 B → 28 491 B largest; 84 → 121 sections; 4 chunks unchanged); mutation proven both ways. The PR arrived CONFLICTING on the generated dashboard (branched 29 s before #161 merged) with 0 check runs — the PM merged main in the worker's worktree, regenerated, and waited for CI on the new sha. **Worktree removal was instant and clean** (the same operation failed with Permission denied on OneDrive at 16:38). Dispatched in a worktree (PM-R8's parallel lane-R slot) on `pm/38-todo-per-item-sections`; touches `build-dashboard.js`, `tests/pm-state.test.js`, `TODO_REPO_HYGIENE.md` (writes entries 38 and 39), the generated dashboard (collision with the bench PR expected; PM rebuilds at merge). Trailing-5h 6 of 6 until 21:23; day-shift 15 of 16 | |
| 2026-09-05 20:25 | **SSD verdict for Ben** (both round-1 workers asked to report stalls) — PM bookkeeping, no worker | fable | — | — | **The move off OneDrive helped exactly where it was supposed to.** Item 38: 12.6 min, zero git/filesystem stalls, worktree removed instantly (vs 16:38 on OneDrive: `Permission denied` + a half-removed record needing `rm -rf` + prune). Bench run 28: 56 min, every git op instant, a two-conflict rebase + force-with-lease + pre-commit hook with no retry, no MAX_PATH failure. The served engine is LF-only for the first time (`.gitattributes` from #156). Also fixed on arrival: the fresh clone had NO git identity (repo-local `user.name`/`user.email` set to the values the last six commits used) — it would have broken every worker's commit. Still true and unrelated to the move: `python` not `python3`; long doc bodies go to `$TEMP/edha-*`, not the scratchpad path. What did NOT change: worker turn counts and cost (the work is the same size), and the generated-dashboard collision on every parallel PR. Session cost so far: PM 3.1M / 132 turns; workers 15.6M (2 dispatches) | (this PR) |
| 2026-09-05 21:22 | Item **33** (re-land handout-forge skill + session-zero one-pager from `fbc8e20`) — `work-item` | sonnet | 7.2 min, 115 turns | 1.9M | **merged 21:29 after review**, no trailers, zero stalls. Exactly two files taken from the orphaned branch (proved with `git ls-tree`/`show --stat`), CLAUDE.md row + trigger sentence written fresh, one stale citation fixed (the primer is generated, never hand-written), `docs/BRANCH_CLEANUP.md` KEEP 3 → 2. **One PM review correction, committed on the branch:** the one-pager stated the "two years / every nation feels it" timeline that Ben's ruling (recorded in `EDHA_PLAYER_PRIMER.md`'s GM note) bans from player text; the worker listed it as drift per its brief, and with no bounce channel to a finished worker and no cap headroom for a re-dispatch, the PM swapped the sentence for the primer's own approved paragraph (one sentence, no invented content). Took the slot over item 39, whose premise failed measurement (see its row) | #164 |
| 2026-09-05 21:32 | **PM-R11 — Ben lifts tonight's caps** (chat 21:30) — PM bookkeeping, no worker | fable | session so far ~2h25m; PM 4.9M / 192 turns; workers 17.5M (3 dispatches) | — | The planned 21:40 stop is cancelled; the loop runs to 07:00 ET. Skill lessons written that were owed since the status wake: the PM skill now says to wait for CI on the NEW sha after any push and how to handle a CONFLICTING PR's zero check runs, and no longer says "quiet hours"; the runbook carries the pull-the-deploy-script-first rule. Item 39's TODO entry corrected (premise failed measurement). Next: bench run 29 (Opus, main checkout) + item 39 (Sonnet, worktree) at once | (this PR) |
| 2026-09-05 21:36 | Item **39** (audit.py NO FILE lists the valid keys; deity keys in both gate lists; exit code pinned) — `work-item` | sonnet | 8.1 min, 131 turns | 2.1M | **merged 21:42 after review**, no bounce, no trailers, zero stalls. One-line message change derived from the same `DATA.glob` list; subprocess-based pin mutation-verified (drop `any_fail` → test fails). Reviewed in the worker's worktree while the bench held the checkout | #166 |
| 2026-09-05 21:43 | Item **20** (ONE gate runner: `scripts/gates.js`; CI calls it with `--ci`; five doc copies → pointers) — `work-item` | sonnet | 11.9 min, 149 turns | 2.6M | **merged 21:55 after review**, no bounce, no trailers. 10/10 local, 12/12 with `--ci` (scratch modroot under `%TEMP%`, never the live module), negative proven (one gate made to fail → FAIL reported, the other nine still ran, exit 1), CI green on the head sha with the runner. Root cause named: `python3` is a Windows App Execution Alias stub (exit 49), not a missing binary. PM's memory note on gates rewritten. `git worktree remove` said Permission denied on the DIRECTORY (its `node_modules` from the `--ci` install), the record was gone; `rm -rf` cleared it | #167 |
| 2026-09-05 21:36 | Bench run 6 = bench run 29 (Unstoppable canvas halves, R-65 remainder, pass 5.2) — `bench-run` | opus | 29 min, 270 turns | 6.0M | **merged 22:03 after review**, no trailers. Deploy hash-verified from both sides. **5 rows off** (🤖 35 → 31), **0 engine defects, no fix pass needed**. Run 28's 2×2-footprint diagnosis RETRACTED by experiment (1×1 fails identically; `animate:false` moves; `document.hidden`, rAF never fires) — the harness pumps the ticker now. Two rows' stated subjects were wrong and corrected: Venom Glands (`Adaptive Mutation`, always on `Bench — Life` → **TODO 40 closed without a change**), Set Charge ally-heal (unreachable by design; proven via Mending Aura). R-64 settled from the authored data. World restore id-diff EMPTY. Worker merged main mid-run (#167 landed) and ran the NEW gate runner: PASS. Cheapest bench run of the marathon per row retired is still run 27; this one was ~1.2M/row because the retraction experiment was the work | #168 |
| 2026-09-05 22:06 | Board (round 2 close; item 40 closed; item 37 dispatched) — PM, no worker | fable | — | — | Session so far: PM 7.2M / ~230 turns; workers 24.3M (6 dispatches: 38, bench 28, 33, 39, 20, bench 29) + item 18 in flight. **SSD verdict holds across six workers: zero git or filesystem stalls**; the one wrinkle is `git worktree remove` refusing a worktree that holds a `node_modules` (rm -rf clears it) | (this PR) |
| 2026-09-05 21:57 | Item **18** (overlay name fallback scoped to the talent's own atlas+group; collisions logged; `authoredOverlayFor`) — `work-item` | opus | 15.5 min, 174 turns | 2.9M | **merged 22:12 after review**, no bounce, no trailers. Six packs byte-identical before/after (hashes match PR #136's too). Mutation-verified pin (restore last-file-wins → 2 tests fail). **Found the fallback LIVE, not dormant**: The Final Study (Knowledge) resolves by name on a stale docId → TODO **41**. The 12 colliding names measured exactly as filed. Worker rebased over #168/#169 itself | #170 |
| 2026-09-05 22:07 | Item **37** (bench roster script detects + repairs orphan tokens; pure `benchOrphanPlan` + pinned test) — `work-item` | sonnet | 11.8 min, 138 turns | 2.7M | **merged 22:19 after review.** One PM correction committed on the branch: the replace path deleted the orphan BEFORE resolving a replacement (which, on that path, never exists) — reordered so nothing is deleted without its replacement built. Live proof = a 🤖 row, which run 30 then PASSED | #171 |
| 2026-09-05 22:13 | Item **30** (rulings close-out R-7/R-19/R-34/R-49 → §K; two checklist rows re-worded; the R-34 🤖 row) — `work-item` | sonnet | 12.7 min, 202 turns | 3.5M | **merged 22:26 after review**, no trailers. The worker caught the PM's brief putting Walking Ruin in the Death tree (it is Destruction) and placed the row correctly. It also caught itself branching from a stale local main and rebuilt cleanly before pushing | #172 |
| 2026-09-05 22:21 | Item **26** (bench PCs' `prototypeToken.sight.range` 20 ft, R-2; one value + a citing comment) — `work-item` | sonnet | 9.1 min, 107 turns | 1.7M | **merged 22:30 after review.** Root cause traced to the engine's `preCreateActor` stamping sight from AWA 0 before the script raises AWA — the `updateActor` re-sync watcher did not take effect for console-created actors → TODO **42** (lane B, measure it). Adversary dial untouched. The PR arrived CONFLICTING (branched before #172); PM merged main in the checkout, both deltas kept, dashboard regenerated | #173 |
| 2026-09-05 22:27 | Item **21** (stale-doc sweep: README table + verifier, PM-R2 archive moves with stubs, engine line count, pre-commit regex) — `work-item` | sonnet | 17.2 min, 208 turns | 4.4M | **merged 22:45 after review.** Touched more files than briefed, all forced by the re-point (lint-refs.js, the ratchet allowlist, the classification JSON: comment/string paths only — read and confirmed). Four bullets moot after the clone, recorded not acted. `check-scripts-readme.js` is a gate candidate — PM's call: wire it into `gates.js` with the next tooling item, not tonight. Arrived CONFLICTING; resolved in the worker's worktree | #174 |
| 2026-09-05 22:33 | Bench run 7 = bench run 30 (orphan-repair setup, the player-client window, R-65 Magnum Opus / Pack Share, R-63) — `bench-run` | opus | 33 min, 327 turns | 8.4M | **merged 23:08 after review**, no trailers. Deploy hash-verified. Three clients live (Ben's Gamemaster, Bench, PlayerBench). **3 rows off (🤖 34 → 32), 1 FAIL with a proven root cause** (matched control: player move → 0 Regions; activeGM move → 3): `preUpdateToken` stashes on the initiating client, the activeGM applier reads null; the broadcast-`options` pattern already ships at ~14046 → **fix pass 3**. Also: the map has SEVEN orphans (four non-roster, correctly left alone); existing tokens keep their own stale sight (new 🤖 row); Pack Share's subject corrected to Death Mark. **Honest flag from the worker:** its restore guard fired a no-op `toggleStatusEffect(braced,false)` at four of Ben's Frostbinders — verified nothing lost (they carry `braced` with no effect), the guard rule corrected in the runbook. World id-diff empty; both clients logged out | #176 |
| 2026-09-05 22:50 | Item **22** (225 Radiant rows parked; one lowercase atlas dialect; `normRow` aliases gone; `validate.js` rejects retired keys) — `work-item` | opus | 20 min, 162 turns | 2.7M | **merged 23:10 after review.** No consumer of the Radiant rows (grep across every reader, primer/codex `--check` unchanged). Six packs byte-identical (every LevelDB key, `_stats` timestamps stripped); mutation proven (`"Talent Name"` re-introduced → validate.js names it). Scope grew as forced by the rename (primer builder, lint-refs, handbook §10, handout-forge skill) — read and accepted. Two tests that would have gone VACUOUS after the rename (`notStrictEqual(obj.Key, …)`) fixed — a pattern worth a lint some day. lint-refs universe 608 → 477 names; all 11 engine literals still resolve. Arrived CONFLICTING (branched before #176); resolved in the worker's worktree | #175 |
| 2026-09-05 23:09 | Fix pass 3 — run 30's Walking Ruin player-move defect — `test-pass-fixes` | opus | 18 min, 172 turns | 3.7M | **merged 23:28 after review and DEPLOYED 23:28** (`module-src-sync.js push`, hash `c498d9eb…` equal both sides). Root cause re-derived from Foundry's own client source (pre-hooks fire on the initiating client only; `options` are broadcast); ONE bug by exhaustion across all 15 `pre*` hooks × 23 `_edha*` writes; fixed at a shared primitive (`edhaPrevTokenPos` / `edhaPrevTokenCenter`, three consumers); 8-case two-client test, mutation-verified; family gates added. **Verified PASS by bench run 31 from a real player client.** Dispatched in the MAIN checkout on `pm/fix-pass-3-run30`; brief carries the bench's proven root cause and the `options.edhaPrevPos` pattern, and asks the one-bug-or-family question (sweep every `pre*` hook that stashes on the document for an activeGM applier). ENGINE-ONLY expected → PM deploys by `module-src-sync.js push` + hash before bench run 31 | |
| 2026-09-05 23:12 | Board (round 3 close; TODOs 41 + 42 filed; run-log clock corrected) — PM, no worker, from a short-path worktree | fable | — | — | Session so far: PM 12.0M / ~330 turns; workers 44.4M (14 dispatches incl. 3 bench runs). **Lesson recorded: the #169 run-log times drifted ~40 min ahead of the wall clock** because the PM added estimated durations instead of reading `date` / PR `mergedAt` — corrected above from GitHub's timestamps; from now on every run-log time comes from `date` or `mergedAt`. Eight merges between 21:42 and 23:10 with zero bounces and zero filesystem stalls; every parallel PR arrived CONFLICTING on the generated dashboard + handoff exactly as predicted, and each took the PM ~3 min to resolve | (this PR) |
| 2026-09-05 23:32 | Item **23** (21 engine banners, comment-only; section map in `ENGINE_INDEX.md`) — `work-item` | opus | 14 min, 158 turns | 2.7M | **merged 23:44 after review**, no trailers. Stripped-source equality re-run by the PM (13,373 code lines both sides, identical sha with blank lines dropped); a pure 353-line insertion. Also found and bannered the file's unbannered SHARED CORE (lines 1–1487). Gotcha recorded: a banner containing the literal close-comment token closes the block. Pushed live with the 23:53 deploy | #179 |
| 2026-09-05 23:31 | Bench run 8 = bench run 31 (fix-pass-3 re-test, the player-client window, R-63) — `bench-run` | opus | 22.5 min, 219 turns | 4.4M | **merged 23:52 after review**, no trailers. Served hash `c498d9eb…` verified cache-busted. **Fix pass 3 PASS from `PlayerBench`'s own client** with the double-GM negative held; Job 6b CLOSED; stale token sight fixed (one write); R-63 half. **1 FAIL: `Unravel Everything`** (fill 2 → sweep sees an empty ledger; pre-loaded ledger → detonates) → fix pass 4. Declared: the roster script was NOT re-run this run (fixtures asserted directly); a `Guardian Stance` AE appeared that the snapshot could not attribute — left alone, snapshot gap now a runbook lesson. Actor id-diff empty; both clients logged out. 🤖 32 → 30 | #180 |
| 2026-09-05 23:53 | Engine deploy — PM, no worker | fable | — | — | `module-src-sync.js push` → 1 copied; status 6 in sync; repo = live = `ffb1f3f5…` (fix pass 3 + item 23's comments). Foundry still up (302) | — |
| 2026-09-05 23:55 | Fix pass 4 — run 31's `Unravel Everything` same-activation ledger read — `test-pass-fixes` | opus | 19 min, 185 turns | 5.5M | **merged 00:13 after review and DEPLOYED 00:14** (hash `06229ecc…`). **The bench's dispatcher hypothesis was REFUTED in source** (the system's `fireEvent` sequences ordered rules); the real cause one level DOWN: `game.socket.emit` is never acknowledged, so `edhaWriteStatusMark`'s non-owner branch returned a round trip early and the ledger reconcile dropped entries the local client had not received. Family: all 50 multi-rule activations swept, **4 affected — 3 never reported** (Spreading Omen worse than the original). Fix at a new primitive `edhaAwaitLocal` (poll until the relayed write is observable, fail open with a warning), adopted at two relays; a lying `catch {}` now returns false. 6-case race test incl. a negative control that reproduces the diagnosis. **All four verified PASS from a player client on run 32.** Dispatched in the MAIN checkout on `pm/fix-pass-4-run31`; brief says the bench's dispatcher hypothesis is unproven and must be proven or refuted in source, asks the family question across every authored activation with ordered rules, requires a generic fix (allowlist may not grow), a race-pinning test, and STOP if the right fix is authored-side (REBUILD + ruling) | |
| 2026-09-06 00:00 | Board (round 4 close) — PM, no worker, short-path worktree | fable | — | — | Session so far: PM 15.7M / ~400 turns; workers 58.6M (18 dispatches: 11 items, 4 bench runs, 1 fix pass, + 2 in flight). Tonight's shape: a bench run every ~50 min, each finding at most one defect, each defect fixed and re-verified within the hour; ten lane-R items landed beside them with zero bounces | (this PR) |
| 2026-09-05 23:48 | Item **5** (hook-firing test driver: `fireHook`, stub factories, three write-asserting hook tests) — `work-item` | opus | 18 min, 155 turns | 3.0M | **merged 00:04 after review**, no trailers. Three one-line engine mutations each failed exactly the new assertions; suite 608 → 642; one existing test refactored onto `fireHook`. Out of scope: a wrong-owner comment on the single-target gate header; three more ad-hoc hook loops to migrate; `edhaWrapApplyDamage` is a method wrap, not a hook | #182 |
| 2026-09-06 00:07 | Item **27** (retire the summon-relay row, R-1) — `work-item` | sonnet | 12.5 min, 207 turns | 3.8M | **merged 00:18 after review**, no trailers. ⚑ 22 → 21; two engine comments (stripped source identical); R-1 → §K. Pushed live at 00:49 | #184 |
| 2026-09-06 00:14 | Engine deploy — PM, no worker | fable | — | — | fix pass 4: `module-src-sync.js push` → 6 in sync; repo = live = `06229ecc…` | — |
| 2026-09-06 00:15 | Bench run 9 = bench run 32 (fix-pass-4 re-tests ×4 from the player client, roster script, July sections) — `bench-run` | opus | 34 min, 351 turns | 8.0M | **merged 00:48 after review**, no trailers. Served hash `06229ecc…` verified with `decodedBodySize`. **6 rows off, 0 FAILs** — all four fix-pass-4 talents from `PlayerBench` with `isOwner:false` asserted first; R-65 closed; starting kit; roster script idempotent. New finding: the 10 culture items fail system validation at build (`foundry-build.js` ~816 writes `system.id = slugify(name)`; the system allows only its six Roshar cultures) — severity deliberately left open for run 33's one-call read. Guardian Stance explained. Dark-veil scene deferred a seventh time (declared). Actor id-diff empty; both clients logged out. 🤖 30 → 29 | #186 |
| 2026-09-06 00:20 | Item **29** (`kind: line` zones catch every character but the caster, R-5) — `work-item` | opus | 12 min, 126 turns | 2.1M | **merged 00:30 after review** (deploy held for the bench), no trailers. One helper, one consumer (Fault Line), both riders read the one caught set; 6-case test mutation-verified; lint pass 20's `dispoFailOpen` ratchet LOWERED 76 → 74 (two disposition-defaulting reads gone); the schema label lost its "per foe" wording; R-5 → §K. `edhaFoeSkillVsColor` turned out disposition-blind — good news for R-6 and #28 | #185 |
| 2026-09-06 00:49 | Engine deploy — PM, no worker | fable | — | — | `module-src-sync.js push` → 6 in sync; repo = live = `0051bde1…` (items 29 + 27 on top of fix pass 4) | — |
| 2026-09-06 00:50 | Bench run 10 = bench run 33 (Fault Line ally row, culture severity read, dark-veil scene, wizard/items rows) — `bench-run` | opus | 31 min, 250 turns | 5.7M | **merged 01:23 after review** (the PM merged main in for #189 and regenerated the dashboard). Served `0051bde1…` verified. Fault Line PASS on evidence; culture severity answered (dropped, not breaking); **the seven-run dark-veil blocker beaten** with a bench-owned dark scene, which exposed the real defect (`actor.effects` vs item-transferred effects). Roster script re-run for real. Actor id-diff empty; logged out. 🤖 29 → 28 (+5 from 28a landing mid-run = 33) Dispatched in the MAIN checkout on `pm/bench-run-33` against `0051bde1…` | |
| 2026-09-06 00:33 | Item **28a** (combat-gated scene/turn watches, R-4 half a) — `work-item` | opus | 25.5 min, 223 turns | 4.8M | **merged 00:58 after review** (deploy held for the bench; live 01:24). `edhaInActiveCombat` + `edhaWatchCombatGate`, ~30 sites, four families deliberately ungated, 17 two-direction cases mutation-verified three ways, neither ratchet moved. Out of scope: the `ready`-hook expiry sweep still reads the VIEWED combat (28a-ii); two prompt gates share an idiom Dispatched in a worktree on `pm/28a-combat-gated-watches`; brief: ONE generic gate helper on `edhaCombatantOf` (`combat.active` AND the owner is a combatant — never `!!game.combat`), adopted-sites table, deliberately-ungated list, mutation both ways, 🤖 rows per symptom face, R-4 stays open, STOP-and-split if it outgrows ~2 h | |
| 2026-09-06 00:55 | Board (round 5 close) — PM, no worker, short-path worktree | fable | — | — | Session so far: PM 19.4M / ~470 turns; workers 74.5M (24 dispatches: 14 items, 5 bench runs, 2 fix passes, + 2 in flight). Every fix this weekend has been verified from a player client the same night it merged | (this PR) |
| 2026-09-06 01:00 | Item **28b** (spend stamp + `edhaIsSpend`, R-4 half b) — `work-item` | opus | 21.5 min, 166 turns | 3.4M | **merged 01:21 after review**, no trailers. Direction chosen and argued: POSITIVE stamping (the writes the ruling complains about are ones the engine never issues, so absence of a tag can never be evidence); the system's own activation deduction (no options) covered by a pre-use expectation; every uncertainty leans toward "is a spend". Four writers stamped, two watches adopted, four HP→0 watchers deliberately not (a GM zeroing an adversary is a kill), pinned. 18 two-direction cases. New ruling **R-72** (involuntary drain). Consequence Ben should know: a GM hand-recording an adversary's cost on the sheet no longer trips the PCs' enemy focus watchers — a `costs:` rule still does; the 🤖 row makes that contrast the test | #189 |
| 2026-09-06 01:24 | Engine deploy — PM, no worker | fable | — | — | `module-src-sync.js push` → 6 in sync; repo = live = `1496af37…` (28a + 28b on top of everything earlier). Foundry still up | — |
| 2026-09-06 01:25 | Fix pass 5 — run 33's Stalker veil (`actor.effects` vs item-transferred), the culture `init` registration, the unnamed second symptom — `test-pass-fixes` | opus | 33 min, 268 turns | 7.5M | **merged 01:57 after review and DEPLOYED 01:58** (`f268e990…`). Root cause verified against Foundry's `actor.mjs` (`allApplicableEffects()`; `appliedEffects` would NOT have worked — markers are stored disabled). `edhaAllEffects` at three sites; **50 `.effects` reads swept: 2 latent siblings fixed, the rest safe with reasons**. Second symptom root-caused from source (a succeeded sweep is a no-op) and later confirmed by run 34. Ten Edha cultures registered at `init` (the DataModel freezes `choices` at `defineSchema()`); lint pass 22 gates the engine table against `data/cultures.json` both ways; **no rebuild owed** — the built pack becomes valid as it stands. New ruling R-73. **All three rows verified PASS on run 34.** Dispatched in the MAIN checkout on `pm/fix-pass-5-run33`; brief: ONE helper for applied effects + a family sweep of every `actor.effects` read that a trait could transfer; the culture half derived from data, never a hard-coded list; the second symptom diagnosed from source or ranked as hypotheses, never guess-fixed; REBUILD pieces isolated in their own commit as Ben's | |
| 2026-09-06 01:30 | Board (round 6 close) — PM, no worker, short-path worktree | fable | — | — | Session so far: PM 22.0M / ~540 turns; workers 83.5M (27 dispatches: 16 items, 6 bench runs, 2 fix passes, + fix pass 5 in flight). Ten hours in: every fix verified from a player client the same night; the bench found three defects that fifteen green GM-side rows had hidden — the player-client window was the single most valuable habit of the weekend | (this PR) |
| 2026-09-06 01:58 | Engine deploy — PM, no worker | fable | — | — | fix pass 5: `module-src-sync.js push` → 6 in sync; repo = live = `f268e990…` | — |
| 2026-09-06 02:00 | Bench run 11 = bench run 34 (R-4's eight rows, fix pass 5's three, wizard/items) — `bench-run` | opus | 34 min, 317 turns | 8.5M | **merged 02:33 after review** (the worker merged main for #193/#194 itself). Full page reload so `init` re-ran; served `f268e990…` verified. **8 rows off, 0 engine defects**: 5 of 8 R-4 rows PASS both directions with positive controls in the same round; 3 narrowed halves stay; fix pass 5's three rows PASS (the veil's first-ever auto-toggle on the shipped trait AE; run 33's second symptom closed as the first-ranked hypothesis; 2bS-11 retired in full; cultures 16 keys, zero errors). R-74 and R-75 raised. Note for Ben: an active zero-combatant combat lives in his world. Actor id-diff empty; logged out. 🤖 33 → 28 (+3 from items 13/14 landing mid-run = 31) | #195 |
| 2026-09-06 02:01 | Item **14** (`game.user.targets` → `edhaUserTargetTokens()`; nine sites; ratchet 10 → 1) — `work-item` | opus | 11 min, 105 turns | 1.6M | **merged 02:10 after review** (deploy held; live 02:34). Pure refactor pinned by the ratchet from inside the suite + an 8-case reader contract test; the floor of 1 is the reader's own body; two stale `ENGINE_INDEX.md` exemptions (predictions, not findings) deleted; one 🤖 spot-check row | #193 |
| 2026-09-06 02:12 | Item **13** (`resourceWrite` sites → `edhaResourceWrite`; ratchet 12 → 0) — `work-item` | opus | 16 min, 126 turns | 2.4M | **merged 02:27 after review** (deploy held; live 02:34). **The item's premise was wrong** — none of the twelve was a spend; the missing primitive was a NON-spend writer carrying 28b's bookkeeping tag (its first consumers). `edhaDrainFocus` moved with options untouched and PINNED that way because R-72 is open. Lint-refs' stale `userTargets` helper string fixed in passing. Two 🤖 rows. Out of scope: pass 20's regex matches only quoted literal keys | #194 |
| 2026-09-06 02:34 | Engine deploy — PM, no worker | fable | — | — | `module-src-sync.js push` → 6 in sync; repo = live = `4d882ea0…` (items 13 + 14) | — |
| 2026-09-06 02:36 | Bench run 12 = bench run 35 (items 13/14's rows, the three R-4 halves, wizard/items) — `bench-run` | opus | 27 min, 206 turns | 4.5M | **merged 03:01 after review.** Served `4d882ea0…` verified. **5 rows off, 0 engine defects; the last three R-4 faces PASS → R-4 settled (moved to §K by the 03:10 board PR).** Item 13's spend stamp proven (Edict prompt fired); the single-target picker (deferred 3×) driven. Two halves narrowed honestly (a wall-blocked heal pulse; a split not reached). R-76 raised (no talent drains `inv`). Item 12 note: `activeGM` resolved to `Bench`, so watches ran locally — the run-36 plan says count on both screens. Actor id-diff clean bar one stated `_stats` artifact; logged out. 🤖 31 → 26 (+1 from item 12 = 27) Dispatched in the MAIN checkout on `pm/bench-run-35` against `4d882ea0…`; told it is the last bench of the night and to put the report in the PR body AND its final message (run 34's agent ended on "waiting for CI" — the PR body carried the report) | |
| 2026-09-06 02:30 | Item **12** (`edhaDefBuffGmGate` at the 20 hand-derived primary-GM gate sites) — `work-item` | opus | 16 min, 138 turns | 2.5M | **merged 02:45 after review** (deploy held; live 03:02). **Why the key never shrank:** two polarities — `edhaNoOtherActiveGM()` is the primitive, `edhaDefBuffGmGate()` = `isGM && no other`; 16 world-writing sites migrated byte-equivalently, 3 region traps stay on the primitive on purpose; ratchet 20 → 1; 6-case test, flipped-polarity mutation caught by three pins; one two-GM 🤖 row; a ruling candidate (GM-less traps) recorded, not decided Dispatched in a worktree on `pm/12-primary-gm-gate`; world-writing sites first; a two-GM double-write 🤖 row; STOP-and-split at ~90 min | |
| 2026-09-06 02:40 | Board (round 7 close) — PM, no worker, short-path worktree | fable | — | — | Session so far: PM 25.8M / ~640 turns; workers 101M (32 dispatches: 18 items, 7 bench runs, 3 fix passes, + 2 in flight). Nine ratchets/finds tonight corrected an item's premise on measurement (17, 39, 40, 13, 14, the culture severity, the Unstoppable walls, the dispatcher hypothesis, the roster gap) — the workers' habit of measuring before fixing is what kept the night clean | (this PR) |
| 2026-09-06 02:47 | Item **10 batch 1** (disposition fail-open → fail-closed helpers, the world-write / damage / status sites) — `work-item` | opus | 20 min, 145 turns | 3.0M | **merged 03:07 after review** (arrived CONFLICTING on the dashboard after #199; PM merged main in the worktree, regenerated; live 03:27). **Ratchet 74 → 11**: all 63 occurrences that gate a write, damage, status, ledger stamp or dice math; two polarities again (`?? 1` = friendly, `?? 0` = neutral); two sites worse than they read (the trample sweep compared two unknowns with DIFFERENT defaults → opposite sides; an "unknown fails closed" comment over a `?? 0` pair); `!same ≠ hostile` named at the splash/burst filters; baked sides now resolve or refuse. 7 cases mutation-verified; one 🤖 sideless-probe row. Convention settled and documented: an unresolvable side matches NEITHER predicate Dispatched in a worktree on `pm/10-dispo-fail-open-batch1`; classify all 74 (migrate / batch 2 / stays), ratchet lowered in-commit, fail-closed mutation pins, one 🤖 batch row; hard stop at ~90 min | |
| 2026-09-06 03:02 | Engine deploy — PM, no worker | fable | — | — | `module-src-sync.js push` → 6 in sync; repo = live = `95c98d65…` (item 12) | — |
| 2026-09-06 03:03 | Bench run 13 = bench run 36 (item 12's two-GM row, the two narrowed halves, wizard/items) — `bench-run` | opus | 23.5 min, 188 turns | 4.2M | **merged 03:26 after review.** Served `95c98d65…` verified. **3 rows off, 2 defects** (both with an instrumented positive control): the Investiture-max persist fires from whichever GM client prepares the actor first (measured from BOTH clients) → R-77 + fix pass 6; White's max HP 64 ↔ 57 hypothesised (an AE application dropped on the partial-prepare path). `activeGM` = `Bench` is structural (user-id sort). World diffs empty; both clients logged out. Declared: the roster script was asserted, not re-pasted. 🤖 27 → 26 (+1 from item 10 = 27) Dispatched in the MAIN checkout on `pm/bench-run-36` against `95c98d65…`; the last bench of the night | |
| 2026-09-06 03:10 | Board (round 8 close; **R-4 → §K**; R-76 + the GM-less-traps candidate in the rulings table) — PM, no worker, short-path worktree | fable | — | — | Session so far: PM 27.9M / ~700 turns; workers 111M (35 dispatches: 20 items, 8 bench runs, 3 fix passes, + 2 in flight). R-4 was answered by Ben on 2026-09-05 08:35 from his phone and is settled 19 hours later with both halves shipped, deployed, and every one of its eight faces measured in both directions at the table | (this PR) |
| 2026-09-06 03:27 | Engine deploy — PM, no worker | fable | — | — | `module-src-sync.js push` → 6 in sync; repo = live = `39cc7612…` (item 10 batch 1) | — |
| 2026-09-06 03:28 | Fix pass 6 — run 36's Investiture-max persist outside the two-GM gate (proven) + White's max HP 64 ↔ 57 (hypothesised) — `test-pass-fixes` | opus | 19 min, 124 turns | 2.3M | **merged 03:46 after review and DEPLOYED 03:47** (`57a8c950…`). Persist gated on `!isGM || edhaNoOtherActiveGM()` (R-77's default, ratchet stays at its floor). **The HP flip's direction was inverted in the report:** the `ready` hook's bare `a.prepareData()` re-applied every ADD-mode effect (`DataModel#reset()` is what re-inits from `_source`; cosmere-rpg applies AEs in `prepareDerivedData`) — 57 was right, 64 inflated, engine-wide on every client from world load; fixed with `a.reset()`; three mutation-verified tests + a source assert that a bare `prepareData(` cannot regrow. Harness lesson: a console restore must `reset()`. Process note: `git checkout --` during mutation testing reverted uncommitted edits — mutations run against committed state. **Verified by run 37.** Dispatched in the MAIN checkout on `pm/fix-pass-6-run36`; brief applies R-77's recommended default (owner gate for a non-GM owner + primary-GM gate for the GM case) pending Ben's veto, and says: prove the HP flip from source or rank hypotheses — never guess-fix; ~25-minute budget | |
| 2026-09-06 03:35 | Board (round 9 close) — PM, no worker, short-path worktree | fable | — | — | Session so far: PM 30.2M / ~760 turns; workers 118M (38 dispatches: 21 items, 9 bench runs, 3 fix passes, + fix pass 6 in flight). The engine idiom ratchets moved tonight: `userTargets` 10 → 1, `resourceWrite` 12 → 0, `primaryGmGate` 20 → 1, `dispoFailOpen` 74 → 11 — four backlogs that had been frozen since August | (this PR) |
| 2026-09-06 03:47 | Engine deploy — PM, no worker | fable | — | — | fix pass 6: `module-src-sync.js push` → 6 in sync; repo = live = `57a8c950…` | — |
| 2026-09-06 03:49 | Bench run 14 = bench run 37 (item 10's sideless probe, fix pass 6's re-tests, AoE burst, wizard) — `bench-run` | opus | 15 min, 145 turns | 2.4M | **merged 04:03 after review.** Served `57a8c950…` verified on a full reload. **1 row off, 0 defects, 0 rulings**: max HP 57 on a fresh load, the doubler reproduced as a positive control, Green's "same numbers" explained (same effect, different dash). Investiture-gate row BLOCKED on Ben's pre-deploy `Gamemaster` client — a new runbook rule. Item 10's probe recipe corrected (Secret is finite). Three probe actors created and deleted; all diffs empty; logged out; the temp setup-script copy removed. 🤖 27 → 26 | #204 |
| 2026-09-06 04:15 | **PM handoff — STOPPED before Ben's 07:00 hard stop (PM-R11)** | fable | session 9h08m; PM 32.4M / 695 turns; workers 140.6M (34 dispatches) | — | Loop stopped. **Nothing running, nothing scheduled, no open PRs, no worktrees; main clean at #204; live engine = main.** 44 PRs merged (#161–#204) with zero bounces; 20 items closed (38, 33, 39, 20, 18, 37, 30, 26, 21, 22, 23, 5, 27, 29, 28a, 28b, 14, 13, 12, 10-batch-1), 10 bench runs (🤖 47 → 26, ⚑ 22 → 21, re-test block empty), 4 fix passes for 5 table-found defects (a pre-hook client split, an un-acked relay read-back, a trait-transferred marker invisible to `actor.effects`, a persist outside the two-GM gate, a bare `prepareData()` doubling every ADD-mode effect on load) — every one verified at the table the same night. Four engine ratchets moved (`userTargets` 10 → 1, `resourceWrite` 12 → 0, `primaryGmGate` 20 → 1, `dispoFailOpen` 74 → 11). R-4 settled. **Week's total (since 09-04, both project dirs): PM 72.5M, workers 216.9M, ≈ 289M weighted.** The SSD verdict held all night: zero git or filesystem stalls across 34 workers; the only wrinkle is `git worktree remove` refusing a worktree that holds a `node_modules`, cleared by `rm -rf`. Sunday's order is in the top line | (this PR) |
| 2026-09-06 07:02 | **New PM session of record — `edha-pm-daily` (Sunday day-shift opens)** — PM, no worker | fable | ~20 min | — | Clock guard passed (Sun = weekend window). Session moved from the retired OneDrive checkout into `C:\dev\Skilltrees` (`change_directory`; toplevel verified). Resume clean: `main` #204 `4d2eb51`, no open PRs, no worktrees, board inbox and phone inbox both empty, `module-src-sync.js status` **6 in sync** (live `57a8c950…` = `main` — nothing owed to the deploy script), Foundry answers 302 on :30000 so **lane B is open**. **Budget read of the shift:** new day-shift **0/12**; PM-R11's no-cap authorization expired at the 07:00 stop, so the ordinary weekend caps are back. Clock re-verified from three sources at **08:28 EDT** (the session's opening read of 07:02 was stale by the time the board was written) → trailing five hours start 03:28, catching only **fix pass 6 (03:28) and bench run 37 (03:49)** of the night's seven dispatches: **2 of 6, four slots free, dispatch now.** The earlier 7-of-6 reading held the shift to 07:30 and was wrong only in the clock, not the method. Also noted: the live repo's copy of the PM skill is one revision ahead of the user-level copy the Skill tool loaded (it carries the "wait for CI on the NEW sha" rule from 09-05) — the live copy is the one being followed | (this PR) |
| 2026-09-06 07:02 | **PM session FAILED — wrong model, then frozen** (edha-pm-daily) | opus (should have been fable) | 159 turns; 07:02 fire, dead 09:01→16:05 | 4.1M PM + 6.2M subagent = **10.3M** | **Session fired as `claude-opus-5` because no scheduled task pins a model** (the MCP create/update tools expose only taskId, title, prompt, description, cronExpression, fireAt, enabled, notifyOnCompletion — no `model`), so it inherits the app default at fire time; the "must remain Fable" note was an assumption with no mechanism. Session then froze twice — **82.7 min** from 07:02:34 (it only advanced when Ben touched the app at 08:25) and **7h04m** from 09:01:18, during which Ben's "stop" went undelivered until 16:07. Ben lost the working day, ran a cloud Fable session instead (#208, #209) and merged **#207 himself at 12:11**. Delivered before the freeze: #206 (board) and bench run 38 → #207 (5 rows retired, R-78, the run-22 harness blocker disproved). **Fixes applied 16:20:** a **MODEL GUARD** is now step 0 of BOTH `edha-pm-daily` and `edha-pm-weeknight` (exit without spending unless the model id starts with `claude-fable`, push-notify Ben), plus a rule to re-read `date` before every dispatch and budget calculation (today's stale 07:02 read caused a wrong trailing-window hold). Board's session-of-record line rewritten to a **stand-down** so the next PM takes over instead of deferring | #210 |
| 2026-09-06 08:35 | Bench run 15 = bench run 38 (the wizard block; the re-test block was empty) — `bench-run`, dispatched by the failed 07:02 session | opus | ~24 min (PR opened 08:59) | 6.2M | **merged 12:11 by Ben, via the cloud relay session** (the PM was frozen). 5 rows off (2 wizard + 3 others); R-78 filed (Ben 09-06: retire → item 48); one 🤖 defect (the weapon picker's "a Agent" / "a Envoy" → item 48); run 22's rendering blocker DISPROVED. ⚠️ Its four commits carry model trailers (iron rule 6) — merged, left in place | #207 |
| 2026-09-06 15:06 | `docs/ACTOR_STAT_DERIVATION.md` — the R-54 derivation map (the cloud relay session, not a PM dispatch; DOCS-ONLY) | fable (cloud) | — | — | **merged by Ben 15:06.** Finding: the +1 max HP has NO canon source (`EDHA_HP_BONUS = 1`, generalised from the June pregens on 06-11b); Ben chose (c) remove it → item 47 | #208 |
| 2026-09-06 16:21 | Board stand-down line + MODEL GUARD on both scheduled tasks + `pm-live.json` / `pm-state.json` into the repo (the dead session's last act, DOCS-ONLY) | opus | — | (in the 10.3M above) | **merged by Ben 16:21** | #210 |
| 2026-09-06 16:40 | Sunday PM session sync — W14 / W15 / W20 pre-walked menus, `SCRATCH=tmp/pm`, scheduled-task form hygiene, the board's relay correction (the cloud relay, DOCS-ONLY) | fable (cloud) | — | — | **merged 16:40 by the relay on Ben's word** ("go ahead with any needed PRs and merge to main") | #209 |
| 2026-09-06 16:56 | **New PM session of record — Fable, started by Ben by hand** — PM, no worker | fable | step 0 ≈ 20 min | — | Model guard passed (`claude-fable-5-1`); Sunday = weekend window; clock read live (16:56, re-read 17:02). Resume clean: `main` `7e34961`, no open PRs, no worktrees, engine 6 in sync (`57a8c950…`), **Foundry CLOSED.** #209 / #210 verified on disk: `docs/pm-*.json` tracked, `tmp/` gitignored, both task prompts open with the MODEL GUARD. **Phone inbox: 66 notes → all `seen`** (dump in `tmp/pm/inbox-2026-09-06/`); the board's rulings table, queue rows 43–62 and this run log carry the consequences. The `remoteControlAtStartup` write was **blocked by the permission classifier** → PM-R12, Ben's hands. **Budget read at 17:05:** day-shift **1 of 12** (bench run 38 at 08:35); trailing-5h **0 of 6**. Dispatch: items 45 + 46 in parallel worktrees (both docs-only, disjoint files — 45 never touches `TODO_REPO_HYGIENE.md`, 46 touches nothing else) | #211 |
| 2026-09-06 17:15 | Item **46** (file TODO items 43–62 from the 09-06 rulings) — `work-item`, worktree | sonnet | 10 min, 117 turns | 2.3M | **merged 17:23 after review**, no trailers. Twenty entries in house format, specs faithful to the inbox dump; `grep -c '^## [0-9]'` → 62. Arrived CONFLICTING (dashboard vs #211): PM merged `main` in the worktree, regenerated, pushed, and waited for the run on the NEW sha (`gh pr checks --watch` returned the old run's green first — the 09-05 trap, caught). One review correction committed by the PM (58's overlay path). Local gates 10/10 in the worktree | #212 |
| 2026-09-06 17:15 | Item **45** (rulings close-out 2026-09-06 — `EDHA_RULINGS.md`, checklist, handoff delta, `ENGINE_INDEX`, bench-run scene licence) — `work-item`, worktree | sonnet | 34 min, 359 turns | **13.7M** | **merged 17:44 after review**, no trailers. 85 ANSWERED blocks, 17 no-change rulings + the 14 §I defaults → §K with stubs, new R-79, R-72 written, every change ruling names its item 47–62; checklist rows retired/reworded per R-9/16/24/26/30/33/39/41 and annotated where the fix ships later; doc-map line for `ACTOR_STAT_DERIVATION.md` (in `CLAUDE.md`, where the map actually lives). Arrived CONFLICTING (dashboard vs #212) — same resolution as #212; one review correction (R-36 → item 47). Local gates 10/10 in the worktree, CI green on the corrected sha. **Cost note: 13.7M for a docs item** — the section surgery on a 1,100-line rulings doc is turn-heavy (359 turns; the worker caught and fixed two of its own mid-edit mistakes); budget the next rulings-doc pass as M-plus, and prefer append-only formats for it. **Left open by the dump: R-18, R-48** (not answered; in the rulings table as waiting) | #213 |
| 2026-09-06 17:50 | Item **47** (fix pass 7a — R-10, R-12, R-25, R-36, R-51, R-52(i), R-72 + R-76, R-54) — `work-item`, worktree | opus | — | — | dispatched. Clock re-read 17:44 before the budget: day-shift **4 of 12** (bench 38, 45, 46, 47), trailing-5h **3 of 6**. The PM's board bookkeeping rides as its own small PR (`pm/board-2026-09-06b`) so the engine worker starts from a clean `main` and the main checkout stays the PM's | — |
| 2026-09-06 17:50 | Item **47** (fix pass 7a) — `work-item`, worktree | opus | 44 min, 379 turns | **10.2M** | **merged 18:33 after review**, no trailers, one themed commit per ruling. Seven of eight rulings shipped with a reverted-and-re-run mutation proof each (R-54, R-36, R-52(i), R-51, R-72 + R-76, R-12 via a new `edhaLedgerDropCreature`, R-10 as a family pin); R-25 stopped and reported — not engine-only → **item 63**. Local gates 10/10 in the worktree; CI green on the merged-`main` head (the branch arrived CONFLICTING on the dashboard after #214 and the worker resolved it itself). 7 🤖 rows, 0 ⚑, headers clean. **Engine now STALE vs `main` — `module-src-sync.js push` when Foundry is next up (no bat needed).** Cost note: an Opus M engine pass with eight mutation proofs ran 379 turns; the proofs are the turns | #215 |
| 2026-09-06 18:36 | Item **48** (fix pass 7b — R-31, R-32, R-37, R-38, R-55, R-78, R-13, R-6, the weapon-picker article) — `work-item`, worktree | opus | — | — | dispatched. Clock re-read 18:31: day-shift **5 of 12** (bench 38, 45, 46, 47, 48), trailing-5h **4 of 6** (17:15 ×2, 17:50, 18:36). Next: 49 (R-15 list slot) after 48 merges; the board bookkeeping for 47 + 63 rides on the next board PR | — |
| 2026-09-06 18:59 | **Ben: `deploy-to-foundry.bat` run, Foundry relaunched, adversaries synced** (chat, 19:00) | Ben | — | — | PM hash-verified 19:00: served engine `7d8e0226…` = `main` `dd1e1c3` (6 in sync) → **fix pass 7a LIVE**; `localhost:30000` answers 302; Ben's `Gamemaster` client is fresh. The Foundry lane is open | — |
| 2026-09-06 19:03 | Bench run 16 = **bench run 39** (fix pass 7a re-tests, R-77, the bench-pending items' 27 open 🤖 rows) — `bench-run`, worktree | opus | — | — | dispatched beside item 48 (PM-R8: one Bench slot + one worktree worker; both edit the checklist, so the second PR to land merges `main` first — accepted for the sake of the open window). Clock 19:00: day-shift **6 of 12**, trailing-5h **5 of 6** (17:15 ×2, 17:50, 18:36, 19:03) — the next slot opens 22:15 unless 45/46's 17:15 pair ages out first (22:15) | — |
| 2026-09-06 19:08 | **PM-R14 — Ben lifts the caps for the shift and asks for Fable workers on larger items** (chat) | Ben | — | — | Recorded in the rulings table. Concurrency is now bounded by worktrees + file conflicts, not budget; the Bench slot stays serial |  — |
| 2026-09-06 19:15 | Item **34a** (fleet weapon migration — the 11 adversary attack items → `kind: "weapon"`, `edhaRuleBearer` on both harvest loops; adversaries pack REBUILD) — `work-item` via **`fable-worker`**, worktree | fable (medium) | — | — | dispatched — the first Fable worker of the project. Runs beside 48, bench 39 and 53; disjoint engine regions; the handoff top and the checklist will conflict and the later PR merges `main` first | — |
| 2026-09-06 19:15 | Item **53** (R-50: the FIRST ambush strike benefits — sync belief test inside `edha-damage-rider`, one shared pure helper, ten carriers) — `work-item` via **`fable-worker`**, worktree | fable (medium) | — | — | dispatched. Four workers in flight: 48, bench 39, 34a, 53 | — |
| 2026-09-06 18:36 | Item **48** (fix pass 7b — nine rulings + the weapon-picker article) — `work-item`, worktree | opus | 39 min, 380 turns | **10.7M** | **merged 19:15 after review** (gh `mergedAt` 23:14:46Z; the PM first wrote "19:30" from an estimate, corrected 19:20 — read `date`, never guess), no trailers, one themed commit per ruling (split after the fact from one verified change — each stage `node --check`'d and tree-consistent; the full gate set ran on the completed change, CI green on the merged-`main` head). 51 headless cases in nine new test files, every ruling shown failing under a one-line reversion; 12 🤖 rows, 0 ⚑, headers clean; `edha-aoe-template` retired (R-78) with its `ENGINE_INDEX` row struck. Lint pass 7 caught the worker mid-pass (a literal `"Snare"` fallback → `"trap"`) — the ratchet works. Found out of scope → item 64 (build script still mints the retired type). **Deploy held** until bench run 39 reports, so the run's evidence stays on one hash; then one `module-src-sync.js push` covers 7b | #217 |
| 2026-09-06 19:17 | Item **49** (R-15: the next-test slot becomes a list; R-57 absorbed; R-20's round expiry per entry) — `work-item`, worktree | opus | — | — | dispatched (PM-R14: no cap). Four in flight: bench 39, 34a, 53, 49. Board PR #218 (this bookkeeping) merged 19:19 | — |
| 2026-09-06 19:15 | Item **53** (R-50 ambush first strike) — `work-item` via `fable-worker`, worktree | fable (medium) | 14 min, 71 turns | **1.5M** | **merged 19:27 after review**, no trailers, three commits. Sync/async split of the ambush block with one pure roll helper and a pending map so the use hook and the rider path agree in the same tick; 8 pins, M1–M4 mutations each shown failing; the Mistheron path pinned unchanged; one 🤖 row (Stillback's Ambush Bite + two negatives). PM re-checked that `edhaRandomFace` / `edhaFlagKey` exist (they do — L11153, L4007). **The first Fable worker: 1.5M for an M engine item vs 10.7M for the Opus M beside it** — the remaining engine items go to `fable-worker` | #219 |
| 2026-09-06 19:30 | Wave 2 under PM-R14 — items **50, 52, 54** (`fable-worker`, engine S each: the consume-dialog wrapper, Battle Fever's spent stack, the dispel widening) and **58, 60** (sonnet: the talent data batch, the min≠max lint guard) — all worktrees | fable ×3, sonnet ×2 | — | — | dispatched. In flight: bench 39, 34a, 49, 50, 52, 54, 58, 60. Held for conflicts: 51 (shares the picker family with 54), 57 (shares `adversaries.json` with 34a), 59 + 64 (both touch `foundry-build.js` — 59 first), 55 / 56 / 63 (rebuild-class engine, next wave), 24 / 4 / 19a (need a quiet checkout) | — |
| 2026-09-06 19:15 | Item **34a** (fleet weapon migration) — `work-item` via `fable-worker`, worktree | fable (medium) | 16 min, 141 turns | **3.1M** | **merged 19:31 after review** (gh `mergedAt` 23:31:09Z — the PM first wrote 19:45 from an estimate; corrected at once: `date` goes BEFORE the board write, never after), no trailers, four themed commits + two `main` merges the worker resolved itself (every delta kept, dashboard regenerated, gates green each time). Parity proof over 336 embedded docs (11 changed, 0 roll differences), `gates.js --ci` 12/12 with `EDHA_DATA` pinned to the worktree, the `edhaRuleBearer` mutation both ways. **REBUILD + ⟳ Sync owed** (adversaries pack). Out of scope → item 65 (the 44 later attack items). 34b (loot caches) is the next Fable-class engine item | #220 |
| 2026-09-06 19:32 | Item **57** (adversary data batch — R-29, R-40, R-46 + R-48's default, R-47, R-74; REBUILD) — `work-item` via `fable-worker`, worktree | fable (medium) | — | — | dispatched after 34a landed (both edit `data/adversaries.json`). In flight: bench 39, 49, 50, 52, 54, 57, 58, 60 | — |
| 2026-09-06 19:17 | Item **49** (R-15 next-test list) — `work-item`, worktree | opus | 22 min, 172 turns | **3.5M** | **merged 19:40 after review** (gh `mergedAt` 23:39:55Z), no trailers, three commits + one `main` merge the worker resolved. 11 pinned cases driving the real readers; five reversions shown failing; the shape keeps the pipeline's field names (a deliberate reading of the ruling's `{source, kind, value, expiry}`, argued in the PR). Two knock-ons the shape forced and the worker surfaced: the cross-path claim is keyed per (actor, grant); a cross-actor clear relays instead of silently failing. R-80 raised (cancelling pair), item 66 filed (leading-minus damage rider). Local gates 10/10; CI green on the merged head | #221 |
| 2026-09-06 19:41 | Items **50** and **52** in review (both `fable-worker`, 11 and 10 min, 53 and 39 tool uses) — reports arrived together with 49's; both gates 10/10 in their worktrees, CI green, no trailers, 0 ⚑; the PM merged `main` (#221) into each (handoff top + dashboard conflicts, both deltas kept, 848 / 846 tests green through the pre-commit hook). Serial landing: 50 first, then 52 re-merges once more | — |
| 2026-09-06 19:31 | Item **50** (R-70 consume-dialog wrapper) — `work-item` via `fable-worker`, worktree | fable (medium) | 11 min, 80 turns | **1.3M** | **merged 19:42 after review** (gh `mergedAt` 23:42:10Z), no trailers, three commits + one `main` merge by the worker + one by the PM (#221 landed in between; handoff top + dashboard, both deltas kept, 848 tests through the hook). The seam argued in the PR (`use()` calls the dialog with no options; `preUseItem` cannot reach them; a DOM tick would bind to template ids). Worker's process note adopted: a PR read CONFLICTING right after a push may be GitHub's stale recompute — re-read 30 s later before merging `main` again | #222 |
| 2026-09-06 19:31 | Item **54** (R-73 (b) + R-35 dispel widening) — `work-item` via `fable-worker`, worktree | fable (medium) | 14 min, 83 turns | **1.9M** | reported 19:42; review: engine diff read (fail-closed `edhaEffectOwnerItem`, disable-only for item-owned effects decided from the DOCUMENT not the button, `ledgers` field defaulting to `omens:omen` so no rebuild), 9 pins with four mutations shown failing, gates 10/10 in the worktree, 0 ⚑; waits its turn behind 52 for the serial `main` merge | #224 |
| 2026-09-06 19:31 | Item **52** (R-27 Battle Fever spent stack) — `work-item` via `fable-worker`, worktree | fable (medium) | 10 min, 60 turns | **1.1M** | **merged 19:45 after review** (gh `mergedAt` 23:44:55Z), no trailers; the PM merged `main` into it twice (#221, then #222 — handoff top + dashboard each time; the first push hit a reset connection and was retried). Three pins + the honest note that the rank cap has no single-line reversion | #223 |
| 2026-09-06 19:47 | Item **54** — **merged 19:47 after review** (gh `mergedAt` below), after the PM's `main` merge (#223 in between; handoff top + dashboard; 861 tests through the hook) | fable (medium) | 14 min, 83 turns | 1.9M | five engine PRs landed serially in 8 minutes (#221 → #222 → #223 → #224), each re-merging `main` once; the handoff top now stacks eight same-day deltas | #224 |
| 2026-09-06 19:48 | Wave 3 under PM-R14 — **51** (offer decline refund) and **66** (negative rider join) via `fable-worker`; **59** (build-time formula fold, REBUILD) and **43** (the phone "Needs you" view) via sonnet — all worktrees | fable ×2, sonnet ×2 | — | — | dispatched. In flight: bench 39, 57, 58, 60, 51, 66, 59, 43. Still held: 64 (behind 59), 55 / 56 / 63 (rebuild-class engine — after bench 39 lands, its checklist edits are the biggest conflict source), 65 = 34c and 34b (after 57), 62 (its bench row first), 44 (behind 43), 24 / 4 / 19a (quiet checkout) | — |
| 2026-09-06 19:32 | Item **57** (adversary data batch) — `work-item` via `fable-worker`, worktree | fable (medium) | 16 min, 99 turns | **2.0M** | **merged 19:52 after review** (gh `mergedAt` 23:51:37Z), no trailers, seven themed commits; the PM merged `main` once (handoff top + dashboard). LevelDB read-back diff = 1 actor + 19 items; validator 0 issues; lint pass 5 mutation both ways. Corrected a stale premise in R-29 (the description was not empty). R-81 raised (three more `bySize` blocks), item 67 filed and folded into 65's dispatch. **REBUILD owed** — now three rebuild-class merges are stacked (34a, 57; 58 / 59 / 65 in flight) → one deploy ask when they land | #226 |
| 2026-09-06 19:51 | Items **34b** (loot caches, `fable-worker`, L) and **65 + 67** (34c + the R-81 sweep, `fable-worker`, M, REBUILD) — worktrees | fable ×2 | — | — | dispatched 19:51 / 19:52. In flight: bench 39, 58, 60, 51, 66, 59, 43, 34b, 65 | — |
| 2026-09-06 19:31 | Item **60** (R-22 min≠max lint guard) — `work-item`, worktree | sonnet | 23 min, 268 turns | **5.4M** | **merged 19:55 after review** (gh `mergedAt` 23:54:41Z), no trailers; traced both producers of a consume entry before deciding the scan; the PM merged `main` once more (#226). **Cost note: the two Sonnet S workers of this wave (60: 5.4M, 58: 7.0M) each cost more than every Fable S/M worker (1.1–3.1M) — the Fable workers finish in a third of the turns, so they re-merge `main` less; Sonnet stays for docs-only filing, Fable for anything with a proof** | #225 |
| 2026-09-06 19:31 | Item **58** (talent data batch + TODO 41) — `work-item`, worktree | sonnet | 24 min, 335 turns | **7.0M** | **merged 19:57 after review** (gh `mergedAt` 23:57:13Z), no trailers; the PM merged `main` once more (#225). Item 18's Final Study seed was wrong on the record; the worker re-derived it three ways and shipped the real one. **REBUILD owed** (three stacked: 34a, 57, 58) | #227 |
| 2026-09-06 19:03 | Bench run 16 = **bench run 39** — `bench-run`, worktree | opus | 63 min, 460 turns | **12.0M** | **merged 20:10 (squash, to drop an attribution trailer the branch's commit carried — iron rule 6)** after review: gates 10/10 in the worktree, CI green on the merged head, 0 ⚑ added, headers clean. 9 rows driven, 8 PASS, 0 FAIL, 1 BLOCKED narrowed (R-72 POS 3 → `PlayerBench`); R-77 PASSED and the two-client "unreadable engine" blocker of runs 37/38 is disproved by process start times (runbook recipe); new defect → item 68 (fix pass 8); world diff clean, the zero-combatant combat gone (PM-R13). Harness notes: a real reload wipes the page-global start snapshot → `sessionStorage` next run; bench PCs' pools left at test values | #228 |
| 2026-09-06 19:48 | Item **51** (R-17 offer decline refund) — `work-item` via `fable-worker`, worktree | fable (medium) | 15 min, 93 turns | **2.0M** | **merged 20:08 after review** (gh `mergedAt` 00:07:42Z), no trailers; one decline path, the round-change sweep for ignored offers, four mutations shown failing; the PM merged `main` once (#231). Worker's out-of-scope note: `edhaRefundCost` credits without waiting once a card is >5 s old (the existing late-cancel rule) — a ruling only if the bench ever sees it | #230 |
| 2026-09-06 19:52 | Item **65 + 67** (34c + the R-81 sweep) — `work-item` via `fable-worker`, worktree | fable (medium) | 15 min, 124 turns | **2.5M** | **merged 20:13 after review**, no trailers; 36 of 44 migrated, 8 argued as actions; parity 39 changed / 0 roll differences; `gates.js --ci` 12/12; the PM merged `main` once. The entry's own "done when" grep was a no-op (`kind` is never written as `action`) — corrected on the board; item 65's TODO measure should be attack-bearing items without `kind: weapon`, 44 → 8. **REBUILD owed** | #232 |
| 2026-09-06 19:51 | Item **34b** (loot caches) — `work-item` via `fable-worker`, worktree | fable (medium) | 19 min, 89 turns | **1.8M** | **merged 20:15 after review** (gh `mergedAt` 00:15:27Z), no trailers, four themed commits; the claim-before-await guard proven by a two-relay pin (#103's existence check let both through); the PM merged `main` once (three-file conflict, both bench sub-blocks kept). **Item 34 closed.** Risk stated honestly as medium: the double-click intercept and the cross-client take are live-table behaviour → 7 🤖 rows | #233 |
| 2026-09-06 20:16 | Engine deploy — PM, no worker | fable | — | — | `module-src-sync.js push` → 1 copied, 6 in sync; served `0ea0741a…` = `main` `2d9631d` (verified both sides, Foundry 302). Nine engine-only PRs go live at once. Ben's `Gamemaster` client needs F5 | — |
| 2026-09-06 20:15 | **Fix pass 8** (item 68 — the `edha-focus` heal card announces the ungated amount) — `test-pass-fixes`, worktree | opus | — | — | dispatched | — |
| 2026-09-06 20:17 | Bench run 17 = **bench run 40** (fix pass 7b's 12 rows, 51 / 49 / 66 / 50 / 52 / 54 / 53's rows, 34b's loot rows, R-72 POS 3) — `bench-run`, worktree | opus | — | — | dispatched on `0ea0741a…`. In flight: bench 40, 68, 59, 43 | — |
| 2026-09-06 19:48 | Item **59** (R-71 build-time formula fold) — `work-item`, worktree | sonnet | 29 min, 297 turns | **7.6M** | **merged 20:20 after review**; the PM merged `main` once. Honest, load-bearing finding: the build-time fold is a NO-OP on every current formula (all rank / tier-scaled); shipped as a guard, R-71's real fix filed as item 69 (runtime fold). Third Sonnet S item of the night above 5M — the model choice for anything with a proof is settled: Fable | #234 |
| 2026-09-06 20:21 | Wave 4 under PM-R14 — **55** (R-56 senses, REBUILD), **56** (R-14 graze dial, REBUILD), **63** (R-25 `edha-note` dial, REBUILD), **64** (retire `aoeRule()`, TOOLING), **69** (R-71 runtime fold, ENGINE) — all `fable-worker`, worktrees | fable ×5 | — | — | dispatched 20:21. In flight: bench 40, 68, 43, 55, 56, 63, 64, 69 (eight). Deploy plan: ONE `deploy-to-foundry.bat` ask after bench 40 reports and 55 / 56 / 63 land (Foundry must be closed for it), then bench 41 on every REBUILD row. Still held: 62 (its bench row first), 44 (behind 43), 24 / 4 / 19a (quiet checkout) | — |
| 2026-09-06 20:21 | Item **69** (R-71 runtime fold) — `work-item` via `fable-worker`, worktree | fable (medium) | 7 min, 51 turns | **0.9M** | **merged 20:31 after review** (gh `mergedAt` 00:31:23Z), no trailers; the PM merged `main` once and checked TODO 69 in the same commit. The cheapest engine item of the night; R-71 now has both halves (build guard #234, runtime fold #237). Engine push held for bench 40 | #237 |
| 2026-09-06 19:48 | Item **43** (phone "Needs you" view + stale-heartbeat banner) — `work-item`, worktree | sonnet | 43 min, 412 turns | **14.4M** | **merged 20:34 after review** (gh `mergedAt` 00:34:06Z), no trailers; the PM merged `main` once and republished `docs/pm-board-mobile.html` to the artifact with `--inject`. Cost note: a Sonnet M that drove a browser preview loop — the night's most expensive dispatch, above every Opus engine pass but bench 39; UI items go to `fable-worker` too from here | #236 |
| 2026-09-06 20:21 | Item **64** (retire `aoeRule()` + the unregistered-type guard in the pack writers) — `work-item` via `fable-worker`, worktree | fable (medium) | 13 min, 143 turns | **2.3M** | **merged 20:38 after review** (gh `mergedAt` 00:38:21Z), no trailers; five packs content-identical, the guard's mutation names the talent; corrected the brief (five packs, not six). Found: the build report's `events:` count is pre-overlay | #238 |
| 2026-09-06 20:21 | Item **63** (R-25 `edha-note` `whenTarget` dial) — `work-item` via `fable-worker`, worktree | fable (medium) | 15 min, 103 turns | **1.7M** | **merged 20:41 after review** (gh `mergedAt` 00:41:35Z), no trailers, two `main` merges by the worker + one by the PM; one pack document changed. **REBUILD (heroic) owed** | #239 |
| 2026-09-06 20:21 | Item **55** (R-56 one senses rule) — `work-item` via `fable-worker`, worktree | fable (medium) | 17 min, 124 turns | **2.9M** | **merged 20:44 after review** (gh `mergedAt` 00:43:58Z), no trailers; seven mutations; the brief's premise corrected (pack tokens were already 10 — the engine half moves the sheets). **REBUILD + world bulk sync owed** | #240 |
| 2026-09-06 20:21 | Item **56** (R-14 graze dials) — `work-item` via `fable-worker`, worktree | fable (medium) | 17 min, 117 turns | **2.4M** | **merged 20:46 after review**, no trailers; the brief's premise corrected by the worker (the riders are the mutation / apexForm flag readers, and the system passes no hit-vs-graze marker — a click-lifetime breadcrumb from a wrap of `onClickApplyButton` now carries it); Venom Glands is the one behaviour change; R-82 raised. **REBUILD (deity) owed**. **The REBUILD stack is now complete for one deploy: 34a, 57, 58, 65 + 67, 59, 63, 55, 56** | #242 |
| 2026-09-06 20:15 | **Fix pass 8** (item 68 — heal cards announce what landed) — `test-pass-fixes`, worktree | opus | 37 min, 219 turns | **5.3M** | **squash-merged 20:52 after review** (gh `mergedAt` 00:52:05Z) — every one of its seven commits carried a `Co-Authored-By` trailer, so the squash keeps `main` clean; the PM merged `main` once more (#243). The worker's completion notice never arrived (the PR was found open at 20:49 and reviewed from the branch). Family fix: `edhaCrossHeal` returns the delivered amount, `edhaHealLine` builds every card from it; seven announcers + the Investiture arm; 317-line pin file. **Both Opus test-pass-fixes / bench workers of the evening carried trailers; the Fable and Sonnet workers did not** — noted for the work-item skill | #241 |
| 2026-09-06 20:54 | Wave 5 under PM-R14 — the two big ones: **24** (table-driven handler registry, engine L) and **19a** (handoff reference rewrite, docs L) — both `fable-worker`, worktrees | fable ×2 | — | — | dispatched now that the checkout is quiet (only bench 40 in flight). Item 4 (the engine split) waits on 24; 19b on 19a. The deploy ask (the REBUILD stack + the un-pushed engine merges 69 / 55 / 56 / 68 — the bat copies the engine too) goes out when bench 40 reports | — |
| 2026-09-06 20:54 | Item **24** (table-driven handler registry) — `work-item` via `fable-worker`, worktree | fable (medium) | 25 min, 149 turns | **3.5M** | **merged 21:24 after review** (gh `mergedAt` 01:24:05Z), no trailers, no bounce. 102 `api.register*Type` calls → two tables + one loop, same objects (pinned by identity); the snapshot was committed FIRST (cf15416) and is byte-identical after; `handler-schemas.js` evaluates the registry through the harness (parity 10052 = 10052 bytes, 9302 = 9302); lint pass 18 moved off its regex (found in flight); the mutation names the dropped row; 963 → 966 tests; 16 🤖 rows under `# BENCH — Handler registry smoke`. ENGINE-ONLY — **not yet live**, rides the deploy. Out-of-scope finds → TODO **71** | #244 |
| 2026-09-06 21:14 | Phone inbox: Ben answers **R-48** (read 21:46, marked seen) | — | — | — | *"the CARD is canon for an adversary — a statted block should not scale, so give it `distanceFt: 20` and drop `bySize`"* = default (a), already shipped by item 57 (#226) as `{bySize: false, distanceFt: 20}` — no further change; the same principle backs R-81's applied default (item 67). Recorded in `EDHA_RULINGS.md` R-48 | — |
| 2026-09-06 21:26 | Wave 6 under PM-R14: **4** (engine split, TOOLING-only, PM-R15) and **19a** re-dispatched with the mined reports — both `fable-worker`, worktrees | fable ×2 | — | — | 4 unblocked by #244. 19a's first worker (20:54) ended its turn waiting on its own mining subagents (1.1M sunk; the three reports were saved to `tmp/pm/19a-mine-*.md` and handed to the new worker). In flight: bench 40, 4, 19a | — |
| 2026-09-06 21:29 | **Bench run 40 reported** (PR #245, DOCS-ONLY) — review | opus | 72 min so far, 572 turns | **17.4M** | 27 rows retired on evidence (fix pass 7b's six, 49/66's five, 51's two, 50, 52's two, 54's two, 53, R-72 POS 3 from a real second client, six of seven 34b loot rows incl. the 7 ms take race); `2bJ-10` PARTIAL; 34b's body half BLOCKED-ON-DEPLOY; world diff empty but one pre-existing `Guardian Stance` effect on `Bench — Chaos`. **One real defect, root-caused:** an `edha-prompt-pick` `once` budget never bites — the round stamp is written under a DOTTED key (`setFlag` expands `item.uuid`) and read flat; one cosmetic (Ambush Bite's flavor printed twice). **Two rulings filed as R-82 / R-83 — both numbers already taken tonight → renumbered R-84 / R-85 by the PM in the PR.** **Merged 21:41 after review** (gh mergedAt 2026-09-07T01:41:11Z), no trailers; the PM renumbered R-82 / R-83 to R-84 / R-85 in the branch and merged main once more. Its two defect rows feed fix pass 9; 34b's body half and every REBUILD row wait on the deploy. 81 min, 572 turns, 17.4M | #245 |
| 2026-09-06 21:33 | **Deploy ask push-notified to Ben — ONCE** ("Bench 40 done… close Foundry, run deploy-to-foundry.bat, relaunch, tell me") | — | — | — | Covers the REBUILD stack (34a, 57, 58, 65 + 67, 59, 63, 55, 56) and, because the bat copies the engine, 69 / 68 / 24 (+ fix pass 9 once merged, else a `module-src-sync` push). Bench run 41 waits on Ben's confirmation | — |
| 2026-09-06 21:35 | **Fix pass 9** (TODO **72** — bench 40's defects: the dotted-key `once` budget, Ambush Bite's double flavor, R-85 + R-84 applied as defaults) — `test-pass-fixes`, worktree | opus | — | — | dispatched from the PR #245 report (branch read directly); told to port into the split sources if item 4 lands first (it did, 21:38) | — |
| 2026-09-06 21:26 | Item **4** (the engine split) — `work-item` via `fable-worker`, worktree | fable (medium) | 10 min, 86 turns | **1.6M** | **merged 21:38 after review** (gh `mergedAt` 01:37:59Z), no trailers, no bounce; the PM re-ran the hash proof in the worktree (`acac2589…` three ways, `--check` OK, 55 sources) and the full gate set (11/11). TOOLING-only — the deployed engine is byte-identical, nothing owed to Foundry. Found: ENGINE_INDEX's "52 banners" is now 54; `fold-die-math.js` README row (already TODO 71); item 13's `PR #PRNUM` placeholder on `main` | #247 |
| 2026-09-06 21:26 | Item **19a** (handoff reference rewrite, second dispatch) — `work-item` via `fable-worker`, worktree | fable (medium) | 20 min, 113 turns | **3.9M** (+1.1M for the first attempt) | **merged 21:47 after review** (gh `mergedAt` 01:46:56Z), no trailers, no bounce; the worker merged `main` twice itself (item 4, then bench 40 + the board) and made the R-85 correction before the PM asked. 679-line reference, §9 anchor byte-identical, dashboard 40 → 40, one Sonnet cold reader. Found: four stale CLAUDE.md / ENGINE_INDEX lines (the "200 name-keyed" paragraph, "45 rulings", "~19.7k lines", a pre-migration dispatch idiom) → a docs-sweep item | #249 |
| 2026-09-06 21:48 | Item **19b** (move the ~110 deltas to `docs/handoff-changelog/2026-MM.md` by a re-runnable split script, fold `HANDOFF_ARCHIVE.md`, lift the reference, re-point every reader incl. iron rule 5 and the skills) — `work-item` via `fable-worker`, worktree | fable (medium) | — | — | dispatched the minute 19a merged; proof = hash of the removed delta text vs the month files' bodies, heading counts, `git log -S` for three titles; told to re-run its split when fix pass 9's delta lands | — |
| 2026-09-06 21:48 | Item **19b** (handoff changelog move) — `work-item` via `fable-worker`, worktree | fable (medium) | 12 min, 86 turns | **1.5M** | **merged 22:00 after review** (gh `mergedAt` 02:00:38Z), no trailers, no bounce; hash-verified verbatim move by a kept idempotent script, counts re-checked by the PM, reference text byte-identical but for its pointer paragraph, local gates re-run green. **Item 19 is closed (both halves).** The handoff is 691 lines; `docs/handoff-changelog/` holds the deltas | #251 |
| 2026-09-06 22:01 | Item **73** (docs sweep — CLAUDE.md + ENGINE_INDEX still describe the pre-migration engine) — `work-item` via `fable-worker`, worktree | fable (medium) | — | — | dispatched the minute 19b merged; the board said sonnet, but a measured Fable S costs 1–2M against Sonnet's 5–14M for the same size (run log, this evening), so PM-R14's "Fable on the larger items" is read as "Fable where it is cheaper" — noted here, not a new ruling. The first delta written under the new rule (top of `docs/handoff-changelog/2026-09.md`) | — |
| 2026-09-06 22:01 | Item **73** (docs sweep) — `work-item` via `fable-worker`, worktree | fable (medium) | 6 min, 48 turns | **0.7M** | **merged 22:10 after review** (gh `mergedAt` 02:10:27Z), no trailers; one PM correction on the branch (item 13's placeholder → its real PR #194 — the worker's `[x]` fill had replaced every `#PRNUM`, the same trap item 4's worker hit and undid); CI re-run on the corrected head. The cheapest item of the night | #252 |
| 2026-09-06 22:11 | **PM bookkeeping:** R-80, R-81, R-82, R-83 get their numbered entries in `EDHA_RULINGS.md` (they lived only in this board's table since the workers raised them; item 73's count found the gap) — inserted before R-84 so the numbering has no holes (86 entries, R-1 … R-85); CLAUDE.md's rulings row bumped to match | fable | — | — | rides on the next board PR | — |
| 2026-09-06 22:11 | Item **71** (registry leftovers from item 24: README row, the executor-less `edha-illusion-upkeep`, six handler types with no authored user, the stale console line, the loosened test slice) — `work-item` via `fable-worker`, worktree | fable (medium) | — | — | dispatched while fix pass 9 is still on the engine sources; told to resolve in the sources and re-assemble. Deleting a type needs a ruling — the worker files candidates, never deletes | — |
| 2026-09-06 21:35 | **Fix pass 9** (TODO 72 — bench run 40's defects) — `test-pass-fixes`, worktree | opus | 37 min, 266 turns | **6.7M** | **merged 22:16 after review** (gh `mergedAt` 02:16:42Z), no trailers this time, no bounce; two of the four root causes went deeper than the bench's report (the third dotted-flag-key ledger; Foundry's own flavor propagation), both re-derived from the installed Foundry source and pinned. ENGINE-ONLY → pushed live by the PM at 22:18 (below). R-84 / R-85 applied as defaults, vetoable, notes in `EDHA_RULINGS.md` §C + §I | #253 |
| 2026-09-06 22:12 | Items **61** (R-42 map polygons — Goldenport's lobes, Corvaine's river edge; `lint_map.py` WARN 4 → 0) and **44** (`Ask:` lines on open rulings whose heading is not a question) — `work-item` via `fable-worker`, worktrees | fable ×2 | — | — | dispatched while fix pass 9 was finishing; 61's regenerated `thyrcross-nations.json` is a module ASSET (sync push / the bat), 44 is DOCS-ONLY. Three Fable workers in flight with 71 | — |
| 2026-09-06 22:17 | **Engine pushed live** (`module-src-sync.js push`) — Foundry was up (302) and still serving the 20:16 engine, i.e. Ben had not run the bat; every engine-only merge since (69, 68, 24, 55 / 56 engine halves, fix pass 9) went live on its own class | fable | — | — | served hash `f2fb3e2d…` = `main` at `53dd0bc`, 6 in sync, 0 hand-edited, safety copy kept. The pack REBUILD stack is still Ben's bat; his `Gamemaster` client is two pushes behind until F5 | — |
| 2026-09-06 22:18 | **Bench run 41** (registry smoke ×16, fix pass 9's four + `2bJ-10`, the Verdict fold, the fix-pass-8 heal cards, the 55 / 56 engine halves; REBUILD rows stay blocked) — `bench-run`, worktree | opus | — | — | dispatched on `f2fb3e2d…`; told the next free ruling number is R-86 and that deltas go to the changelog now. Four workers in flight: bench 41, 71, 61, 44 | — |
| 2026-09-06 22:11 | Item **71** (registry leftovers) — `work-item` via `fable-worker`, worktree | fable (medium) | 16 min, 106 turns | **1.8M** | **merged 22:27 after review** (gh `mergedAt` 02:27:00Z), no trailers, no bounce; the worker merged `main` (fix pass 9) into the sources and re-assembled itself. Its ENGINE-ONLY half (the derived console line + one no-op executor) is **not yet live** — held for bench 41's hash. Found: the `foundry-build.js items` single-scope TDZ crash → **TODO 74**; nine executor-less rows → **TODO 75** | #257 |
| 2026-09-06 22:12 | Item **44** (`Ask:` lines on open rulings) — `work-item` via `fable-worker`, worktree | fable (medium) | 10 min, 88 turns | **1.5M** | **merged 22:28 after review** (gh `mergedAt` 02:28:41Z), no trailers; the PM merged `main` once more (two changelog deltas kept, split re-run). Item 43's parser had NO `Ask:` fallback, so the worker added the two-line fallback + pins (in scope by the "Done when"); 8 open rulings audited — 4 got an `Ask:` (R-48, R-81, R-82, R-83), 4 already asked. Found: `RULING_DEFAULT_RE` yields an EMPTY default for the bold-inline form (R-80 … R-85), and R-80 / R-81 say "(§I)" but live in §C → **TODO 76** | #258 |
| 2026-09-06 22:12 | Item **61** (R-42 map polygons) — `work-item` via `fable-worker`, worktree | fable (medium) | 12 min, 90 turns | **1.7M** | **merged 22:30 after review** (gh `mergedAt` 02:30:43Z), no trailers; the PM merged `main` once more (changelog + its README + dashboard + codex regenerated), re-ran `lint_map.py` on the branch (1 WARN left, the pre-existing audit-S3 one). The asset is **not yet live** — next sync push after bench 41. Found: seven cityless islets, a lint blind spot for wrong-nation resolution, the tree-audit footer on a passing run | #256 |
| 2026-09-06 22:31 | Wave 8: items **74** (build single-scope TDZ), **75** (nine executor no-ops), **76** (ruling-card defaults + R-80 / R-81 §I stubs) — `work-item` via `fable-worker`, worktrees | fable ×3 | — | — | the three follow-ups filed tonight; all S, disjoint files (build script / engine source 53 / dashboard parser + rulings). Bench 41 still driving | — |
| 2026-09-06 22:33 | Item **10 batch 2** (the last 11 `dispoFailOpen` reads — picker candidates, the empty-note sweep, the movement-window card, the prohibition dialog, the cleanse beacon list — onto `edhaSideSame` / `edhaSideHostile`, ratchet 11 → 0) — `work-item` via `fable-worker`, worktree | fable (medium) | — | — | the queue's last engine item; lane B (each flip changes what a human sees for an unset-disposition token → 🤖 rows). Five workers in flight: bench 41, 74, 75, 76, 10 | — |
| 2026-09-06 22:31 | Item **74** (single-scope build TDZ crash) — `work-item` via `fable-worker`, worktree | fable (medium) | 9 min, 74 turns | **1.1M** | **merged 22:40 after review** (gh `mergedAt` below), no trailers, no bounce; the worker found `adversaries` alone was broken the same way (the item named only `items`) and pinned both; content parity of all five packs vs `all` proven; TOOLING-only, nothing owed to Foundry | #260 |
| 2026-09-06 22:31 | Item **75** (nine executor no-ops) — `work-item` via `fable-worker`, worktree | fable (medium) | 9 min, 78 turns | **1.1M** | **merged 22:43 after review** (gh `mergedAt` 02:43:53Z), no trailers; the PM merged `main` (item 74) and re-assembled. ENGINE-ONLY, held for bench 41's hash | #261 |
| 2026-09-06 22:33 | Item **10 batch 2** (the last 11 fail-open disposition reads) — `work-item` via `fable-worker`, worktree | fable (medium) | 13 min, 66 turns | **1.2M** | **merged 22:49 after review** (gh `mergedAt` 02:49:14Z), no trailers; the worker merged `main` twice itself (items 74, 75) and re-assembled. **Item 10 closed; the ratchet is a tombstone at 0.** Found: the Power-side corollary → **TODO 77**. ENGINE-ONLY, held for bench 41's hash | #263 |
| 2026-09-06 22:31 | Item **76** (ruling-card defaults + R-80 / R-81 §I stubs) — `work-item` via `fable-worker`, worktree | fable (medium) | 10 min, 64 turns | **1.1M** | **merged 22:51 after review** (gh `mergedAt` 02:51:13Z), no trailers; one PM correction (R-48's misplaced ANSWERED line — the PM's own 21:46 insert — moved into its block, pin flipped to closed); CI on the corrected head was slow to appear and ran green after the final `main` merge. The phone's Needs-you cards now show 7 open rulings with real questions, real defaults, and applied-veto styling on R-80 / R-81 / R-84 / R-85 | #262 |
| 2026-09-06 22:18 | **Bench run 41** (the engine-only merges on the 22:17 push) — `bench-run`, worktree | opus | 61 min, 496 turns | **14.3M** | **merged 23:21 after review** (gh `mergedAt` 03:21:00Z), no trailers, no header markers, the worker merged `main` itself and re-ran the split. **25 rows retired on evidence — open 🤖 77 → 52:** item 24's whole registry-smoke section (the real Events-tab picker holds 87 `Edha:` handlers / 15 events in TABLE order), fix pass 9's `once` budget (one flat key, third use refused), 2bJ-10 whole, R-84 (refund + "cost refunded"), R-85 (bearer's round), item 68's four heal cards (both marks produced for real), item 69 (Verdict's system card `2d8 + 5`), item 55's engine half (48 adversaries read Senses 10 with no rebuild; the AWA watcher driven on an adversary). **1 FAIL, root-caused:** Ambush Bite's label is still doubled on the card — `edhaTidyFormula` runs in `renderChatMessageHTML` on a detached element before the system re-renders the damage section ~1.5 s later → **fix pass 10 (TODO 78)**. Three wrong reference names corrected; no new rulings; world diff EMPTY across all 74 actors (a ghost drawing cleared with `scene.update({drawings: []}, {recursive: false})` — runbook lesson) | #265 |
| 2026-09-06 23:21 | **Engine + map asset pushed live** (`module-src-sync.js push`, 2 copied) the minute bench 41 was merged | fable | — | — | engine `ee29b118…` = `main` at `6e657c3` (items 71, 75, 10 batch 2 now live), `thyrcross-nations.json` `c81a388b…` (item 61), 6 in sync, 0 hand-edited, safety copies kept. The REBUILD stack is still Ben's bat | — |
| 2026-09-06 23:21 | **Fix pass 10** (TODO **78** — the Ambush Bite render-timing FAIL) — `test-pass-fixes`, worktree; and item **77** (Power's negated side read) — `work-item` via `fable-worker`, worktree | opus + fable | — | — | dispatched together the minute bench 41 merged; both ENGINE-ONLY; two workers in flight | — |
| 2026-09-06 23:21 | Item **77** (negated actor-level side reads) — `work-item` via `fable-worker`, worktree | fable (medium) | 13 min, 99 turns | **2.0M** | **merged 23:35 after review** (gh `mergedAt` 03:35:0xZ), no trailers, no bounce; the item widened from one site to six of the same shape (the PM accepted the sweep as in scope — the brief asked for it); the worker merged `main` itself. ENGINE-ONLY → **pushed live 23:35** (no bench running); 🤖 row 77-1 for bench 42 | #267 |
| 2026-09-06 23:21 | **Fix pass 10** (TODO **78** — the Ambush Bite render-timing FAIL) — `test-pass-fixes`, worktree | opus | 28 min, 206 turns | **4.5M** | **merged 23:50 after review** (gh `mergedAt` 03:50:30Z), no trailers, no bounce; the worker merged `main` (item 77) itself. Root cause went one level deeper than bench 41's (the two formula strings of a Roll, and the system replacing `.message-content` after the hook) — fixed at message creation, family-wide, plus the rider fold; the acceptance string corrected with the reason. ENGINE-ONLY → **pushed live 23:50**, served hash `609c7e45…` = `main` 9f3f529, 6 in sync | #269 |
| 2026-09-06 23:51 | **Queue drained.** Nothing running, nothing dispatchable: 70 waits on R-83 (Ben), 62 lets its 🤖 row measure first (bench 42), 9 is blocked on the bridge / MST map rulings. The loop drops to an HOURLY fallback wake for Ben's deploy note (chat wins at any time); the shift's remaining work is bench run 42 after `deploy-to-foundry.bat` | fable | — | — | 50 PRs merged since 16:56; the evening's weighted usage is in `pm-usage.py` (≈97M at 23:50, workers included — Fable S/M 0.7–3.9M, Opus benches 14M, Opus fix passes 4.5–6.7M) | — |
| 2026-09-07 05:58 | **PM handoff — loop STOPPED at the last wake before Ben's hours.** Six hourly wakes (00:52 → 05:57) found nothing: clean tree, no PRs, module 6 in sync, served engine = `main`, inbox empty, no deploy note. Nothing running, nothing queued that a session can take: **70** waits on R-83, **62** reframes after its 🤖 row measures (bench 42), **9** is blocked on the bridge / MST map rulings. **First act of the next session: if Ben has run `deploy-to-foundry.bat`, verify `module-src-sync.js status` + the served hash, then dispatch bench run 42** (REBUILD rows 34a / 34c / 57 / 58 / 59 / 63 / 55-pack / 56's 2bW-18 + 19 / 67, the map picker, I10b2-1..4, 77-1, fix pass 10's row with the corrected string, the two rider-family rows, R-52 (c)(ii)). **Week's PM usage (`pm-usage.py`, 09-05 evening → now): this session 122.4M PM + 191.4M workers; the three earlier weekend sessions 54.2M PM + 165.0M workers — ≈ 533M weighted in all**, the price of ~95 merged PRs across the weekend. Next PM: the `edha-pm-weeknight` task at 21:00 ET Monday (MODEL GUARD) or Ben's hand-start | fable | shift 13 h | 122.4M (+191.4M workers) | the board, `pm-live.json` (status stopped) and the phone are the handoff; chat wins at any time | (this PR) |
| 2026-09-06 19:48 | Item **66** (negative rider join) — `work-item` via `fable-worker`, worktree | fable (medium) | 8 min, 57 turns | **1.0M** | reported 19:57, in review (PR #229): one pure `edhaJoinRiderTerm` beside `edhaTidyFormula`, both paths call it, positive damage riders byte-identical by design; 7 pins incl. a source scan; gates 10/10. The worker wrote its own TODO 66 entry (the PM's was still uncommitted in the main checkout) — the PM keeps the worker's on merge. Open question logged: label positive damage riders too? (a ruling if wanted — default no, keep byte-identity). **Merged 20:00** (gh `mergedAt` 23:59:48Z), 1.0M / 57 turns / 8 min | #229 |
| 2026-09-07 09:52 | **Hand-started Fable PM session (Ben, chat, in his own hours — chat wins).** Step 0: `main` 14f2aa9 clean, no PRs, no worktrees; served engine `609c7e45…` = repo, 6 in sync (Ben's deploy this morning); Foundry up. Inbox: 2 notes = R-83's row forwarded twice from the phone with no answer text → **R-83 still WAITING** (the 10:05 `seen` note that called it an answer is corrected by this row). Ben's dashboard paste (48 rulings rows, 12 bench ⚑ rows, 1 art row) → items 79–83, R-86 / R-87, 13 checklist retirements. **Rulings: PM-R8 REVOKED; PM-R16 = 3 Opus per day, Sonnet / Haiku unlimited.** Report given in chat before any change (Ben's ask); the 10 unmarked bench ⚑ rows (coin row, weapon picker, preview panel, Cinderbrock, Heat of the Flats, Dirgehounds, Crownox, Tollbird, two art-backlog rows) and the 5 world ⚑ rows are untouched pending his word | fable | — | 0.9M at step 0 | **Dispatched 10:45: bench run 42 (opus, worktree) + item 79 (sonnet, worktree).** Opus today 1 of 3 | — |
| 2026-09-07 10:45 | Item **79** (rulings close-out 2026-09-07) — `work-item`, worktree | sonnet | 22 min, 272 turns | **6.5M** | **merged 11:16 after review** (gh `mergedAt` 15:16Z), two PM corrections on the branch (misattributed two-GM remark; over-tight open-rulings pin), CI green on the corrected head e88e3bc; found: `parseArtWishlist` had no done-detection (fixed inline), REOPENED rulings invisible to the phone card → item 85. DOCS-ONLY (+ 10 lines of dashboard tooling) | #273 |
| 2026-09-07 10:45 | **Bench run 42** (the REBUILD stack live) — `bench-run`, worktree | opus | 79 min, 511 turns | **16.9M** | **merged 12:05 after review (squash, trailer-free)**; open 🤖 58 → 20, ⚑ 21 → 10; 38 retired, 8 BLOCKED with the blocker re-derived, 1 defect (item 84: weapon-borne `edha-on-hit` inert, proven by mutation, blast radius 6 rules), R-88 / R-89, item 62's numbers, Ben's 13 marks recorded, world diff EMPTY; runbook: the hidden-pane PIXI ticker freeze explains every "nothing moved" measurement. DOCS-ONLY; Ben owes ⟳ Sync Talents | #274 |
| 2026-09-07 12:15 | **Dispatched item 84** (opus, worktree — Opus 2 of 3 today) **and item 85** (sonnet, worktree) the minute #274 landed; both engine/tooling S items with pins. Opus budget after this: 1 slot left today (PM-R16) | fable | — | — | two workers in flight; the PM's own cost so far this session ≈ 2.5M | — |
| 2026-09-07 12:15 | Item **84** (weapon-borne `edha-on-hit` rules inert) — `work-item`, worktree | opus | 11 min, 117 turns | **1.8M** | **merged 12:22 after review** (gh `mergedAt` 16:22Z), no trailers, no bounce; predicate swap + two mutation pins + caller audit; **ENGINE-ONLY → pushed live 12:23** (served `0a677dad…` = `main` 1c52cfd), **Ben F5'd 12:25**. Found: `edhaListPlaceNotes` same gate → item 86 seed | #276 |
| 2026-09-07 12:30 | **Bench run 43 dispatched** (`bench-run`, opus, worktree — Opus 3 of 3 today, PM-R16) for the 20 remaining 🤖 rows + item 84's cue re-test, on the F5'd engine | fable | — | — | one Bench slot in flight; item 85 still running beside it | — |
| 2026-09-07 12:15 | Item **85** (REOPENED rulings on the phone card; Art tab bold garble) — `work-item`, worktree | sonnet | 20 min, 128+ turns | **5.6M** | **merged 12:31 after review** (gh `mergedAt` 16:31Z), no trailers, no bounce; last-marker-wins + code-span protection, three pins, mutation both ways, canon codex rebuilt (same helper). TOOLING-only. Found: bare `*Recommended*` style → item 87 seed | #277 |
| 2026-09-07 12:45 | Item **87** (bare `*Recommended*` default on the phone card) — `work-item`, worktree | sonnet | 12 min, 151 turns | **2.8M** | **merged 12:46 after review** (gh `mergedAt` 16:46Z), no trailers, no bounce; additive fallback, pins, byte-identical existing defaults. TOOLING-only | #279 |
| 2026-09-07 12:30 | **Bench run 43** (the 20 remaining 🤖 rows + item 84's cue re-test) — `bench-run`, worktree | opus | 54 min, 403 turns | **11.1M** | **merged 13:23 after review** (gh `mergedAt` 17:23Z), one themed commit, no trailers; open 🤖 20 → 10, all gated — queue drained; items 88 / 89 + R-90 / R-91; the harness-only recommendation → item 90; world diff EMPTY. DOCS-ONLY, no deploy owed | #280 |
| 2026-09-07 13:22 | **Dispatched item 86** (sonnet, worktree — engine-only, the last `edhaIsTalent` harvest loop) and, once #280 landed, **item 90** (sonnet, worktree — the five harness-only retirements). Opus 3 of 3 spent for the calendar day | fable | — | — | two Sonnet workers in flight; the PM's own cost this session ≈ 4M so far | — |
| 2026-09-07 13:28 | Item **90** (five harness-only rows retired) — `work-item`, worktree | sonnet | 8 min, 155 turns | **2.4M** | **merged 13:32 after review** (gh `mergedAt` 17:32Z), no trailers, no bounce; the worker's report arrived after the merge (it had paused on its own CI watch). DOCS-ONLY | #282 |
| 2026-09-07 13:22 | Item **86** (`edhaListPlaceNotes` rule-bearer gate) — `work-item`, worktree | sonnet | 14 min, 109+ turns | **3.3M** | **merged 13:40 after review**, no trailers; the PM merged `origin/main` on the branch (changelog conflict: both 86's and 90's deltas kept, newest first), regenerated the dashboard, gates 11/11, CI green on the merge head. **ENGINE-ONLY → pushed live 13:40** (served `a23e70d1bad4…`), F5 owed by Ben | #283 |
| 2026-09-07 13:35 | **Dispatched item 89** (sonnet, worktree — Unbreakable Line's missing `use` rule, DATA → REBUILD) | fable | — | — | the only queued row a Sonnet worker can take without a ruling; the rest wait on Opus (midnight) or Ben | — |
| 2026-09-07 13:35 | Item **89** (Unbreakable Line's missing `use` rule) — `work-item`, worktree | sonnet | 14 min, 110 turns | **4.1M** | **merged 13:54 after review** (gh `mergedAt`), no trailers, no bounce; contest-core rule on both blocks, activation aligned, pack built + inspected, `gates --ci` green. **DATA → REBUILD + ⟳ Sync Adversaries owed** | #285 |
| 2026-09-07 13:55 | **PM handoff — loop STOPPED: the queue is dry.** Day's tally (09:52 → 13:55, Ben's hours by his word): **13 PRs merged (#272 → #285)** — board ×6, items 79 / 84 / 85 / 86 / 87 / 89 / 90, bench runs 42 + 43 (48 rows retired on evidence, open 🤖 58 → 5, all five gated on R-90 / R-91 / a rebuild); engine pushed live twice (12:23 item 84, 13:40 item 86; served `a23e70d1…` = `main`); Ben F5'd once (12:25) and owes another F5, his ⟳ Sync Talents click, and ONE `deploy-to-foundry.bat` (item 89). Rulings filed R-86 → R-91; waiting on Ben: R-83, R-56 go/no-go, R-88, R-89, R-90, R-91. **Budget: PM-R8 revoked, PM-R16 = 3 Opus / day (3 used: bench 42 16.9M, item 84 1.8M, bench 43 11.1M), Sonnet unlimited (79 6.5M, 85 5.6M, 87 2.8M, 90 2.4M, 86 3.3M, 89 4.1M); PM 13.8M.** Next PM: `edha-pm-weeknight` at 21:00 ET (MODEL GUARD); nothing Opus until 00:00, then 80 → 81 → 62. Iron-rule-6 debt: the PM's own commits on #272 / #273-correction carry the harness trailer; everything after is clean | fable | 4 h | 13.8M (+ 54.7M workers) | the board, `pm-live.json` (status stopped) and the phone are the handoff; chat wins at any time | (this PR) |
| 2026-09-07 17:10 | **Hand-started Fable PM session (Ben, chat, in his own hours — chat wins: "You're the new pm session"); the 09:52 session RETIRED for the day (out of context), told by session message.** Step 0: `main` a278186 clean, no PRs, no worktrees; Foundry up (302); **packs rebuilt 17:03 by Ben's bat** (item 89 live), F5 pressed, ⟳ Sync Adversaries + Players clicked — nothing owed; engine 6 in sync (`a23e70d1…`); inbox empty. **Ben's dashboard paste 17:11:** R-83 (a) → item 70 unblocked; R-88 (a) → item 92; R-89 (a) → item 93; four ⚑ wizard-v2 rows PASS + the done-ticks → item 91; R-56 unchanged (still "let me know before changing" → WAITING on his go). Opus 3 of 3 spent today (PM-R16) → Sonnet now (91, 92, 93 in worktrees the moment this PR merges); Opus after 00:00: 70 → 80, push live, then bench 44 (89 (b), 70, 80) | fable | — | — | items 91–93 filed on this PR; the dispatch rows follow on the next board commit | #287 |
| 2026-09-07 17:20 | **Dispatched items 91, 92, 93** (sonnet ×3, worktrees) the minute #287 merged — Sonnet is unlimited under PM-R16; three disjoint scopes (rulings/checklist · one authored key · adversaries data + lint + build); changelog-top conflicts are the PM's to resolve at merge | fable | — | — | three workers in flight; the PM's own cost so far ≈ 1.5M | — |
| 2026-09-07 17:20 | Item **92** (Volatile Strike drops `whenDamageType: "impact"`, R-88 (a)) — `work-item`, worktree | sonnet | 6.5 min, 112 turns | **1.7M** | **merged 17:29 after review** (gh `mergedAt` 21:29:31Z), no trailers, no bounce; one key + one description string; pack parity: exactly ONE doc differs (365 talents / 136 items / 20 folders both sides); row 92-1 🤖 in Red; gates 11/11 in the worktree and CI green on the head. **REBUILD (leyline) + ⟳ Sync Talents owed.** Found: the changelog header's delta count ("95") and the README index ("94") disagree with the file (103 `## 2026-09` headers) — a docs nit for the next close-out | #288 |
| 2026-09-07 17:30 | **Phone inbox: seven notes (17:23–17:25), all `seen` 17:30** — R-83 / R-88 / R-89 (a) again; **R-90 (a)** → item 88 unblocked; **R-91 (a)** → item 95 filed; the waiting-list "done"; **R-56 (a) tapped = keep the Edha table** — contradicts the morning note, asked Ben in chat. Ben's phone-board bug (Sent ✓ marks wiped, scroll jumps) → **item 94** filed, root cause read from `docs/pm-board-mobile.html` (`tick()` → `renderNeedsYou` every 30 s; `pm/state` → `render()`); dispatch after this board PR merges | fable | — | — | items 94 / 95 filed; 91 / 93 still running | (this PR) |
